# 인수인계 — 마음 바다 탐험대 (2026-09-16 기준)

이 문서는 지금 상태와 남은 일을 한 장에 모은 것이다. 설계 결정과 근거는 `DESIGN.md`, 손으로 하는 확인 절차는 `TEST.md`,
심사용 시연 학급은 `DEMO.md`, 교사 대화 문구는 `PHRASES.md`, 마음 말 뜻은 `WORDS.md`에 있다. 이 문서는 그 넷을 대신하지 않는다.

- 저장소: `main` = `5d7e4c0` (origin에 푸시됨). 작업 트리 깨끗함. 제출 예정일 **2026-09-24**.
- 앱은 `index.html` 한 파일(React·Three.js 인라인, 빌드 없음) + `demo/`(시연 학급 생성기·검사기) + 정적 자산.
- 브랜치 `feat/character`·`feat/talk-mark`·`feat/tutorial`은 main에 합쳐졌거나 보류된 것이다. stash `character-rework`는 **풀지 않는다**(아래 "보류").

## 1. 9월 14일 이후 바뀐 것

날짜 · 커밋 · 내용. 세부는 `DESIGN.md`의 같은 날짜 절.

**9월 14일**
- `a5909c8` 첫 화면 "로그인 없이 체험하기" 간판이 시연 학생(`DEMO01`/`d07`) 자동 로그인으로 바뀜. 학생 화면에 "둘러보기 중 · ○○는 가상 학생이에요" 띠(§14 "체험 간판").
- `bed116b` `#seed` 패널 캐시 안내.

**9월 15일**
- 튜토리얼 엔진과 투어 다섯(내 섬·친구 섬·바다·광장·교사 대시보드) `3019ded`~`a8930eb`, `f0a8b2c`. 시작은 "처음 오셨나요?" 팻말, 본 뒤 localStorage(`tutIsle`·`tutFriend`·`tutSea`·`tutPlaza`·`tutTeacher`).
- 「말 걸어봤어요」 ①~④ `e484402`·`7029793`·`9d2faf1`(§9-⑤): 살펴봐 주세요 학생 옆 버튼, 저장은 `app/tpriv/<교사>/<해시>/talk/<학생id>=시각`(교사만 읽는 경로, 해시는 세션에만), 수업일 사흘 뒤 「다시 볼 때예요」 + 비교 세 줄, 시연 사례 두 명(23·24번).
- 바다 진입 성능 `753aebe`·`b836063`: 식생 배치 캐시, 조각 생성. 진입 5.3초 → 1.3초(맥 기준).
- 광장 오른쪽 세로 패널·친구 답 바로 보이기 `0a9b9ae`, 친구 섬 터치 걷기·투어 `06f863f`, 승선 연출·섬 고르기 카드 `3aaed31`, 반딧불 날아가는 연출 `5d2e675`, 화면 다듬기 `e38b8e7`.

**9월 16일**
- `07a8df4` 히어로 소개 섹션: 조각 팻말의 영역 이름을 빼고 두 축 이름 **"기운"(세로)·"긴장"(가로)**, 특장점 팻말 셋(어휘 30가지·표현 262개, 비폭력대화, 리포트). 내 섬 나침반 128px + 축 캡션 + **연 모양**(영역별 마음 말 종류 수, 친구 섬은 없음). 마음 말 패널을 나침반 모양 3×3 표로. **시연 학생 이름 25명을 바닷가 낱말로**(탐험이만 예외, 전 하준). §5 "나침반의 연 모양과 축 이름".
- `670b88d` 3D 조작: 계단 C·D 추가(1↔2, 2↔3 각각 둘), 걷기 2.6(전 1.7, 케이던스·발걸음 비례), 광장 걷기(카메라 고정, 터치 패드 공용 `WalkPad`), 바다 **더블클릭이면 카드 없이 출발**, 공감을 **같은 친구에게 하루에 여러 번(같은 마음 말은 한 번)** — 저장 키 `<날짜>_<보낸이>_<어휘키>`. §4 "3D 조작 손질".
- `5cbae3f` 광장 개편: 답이 관찰·느낌·바람 세 칸 → **한 칸 + 마음 말 칩(선택)**, 3D 게시판은 HTML 덧판(질문·오늘 우리 반 마음 TOP3·친구 답 차례로), 답 쓴 수만큼 서던 인형 제거(드로우콜 295→140). §4 "광장 개편".
- `d112338` 교사 화면: 「요즘 달라진 아이」 카드·판정 제거(기록만 남김), **오늘의 질문 30개가 순서대로 미리 채워짐**(그대로/고쳐서 올리기, 한 바퀴 돌면 처음으로), 교사 투어에 「말 걸어봤어요」 단계. 질문 아이디에 난수 꼬리. §4 "교사 화면 손질".
- `5d7e4c0` 리포트: 학생·교사 모두 **「마음 말이 늘어났어요」**(기간을 날짜로 반, 처음 쓰던 말 / 나중에 새로 쓴 말 + 첫 날짜·요일, 5편·7일 미만은 안 가름), 교사 리포트 "그때의 상황" 처음·나중 두 칸. §4 "리포트에 어휘 확장 흐름".

**9월 17일 (미커밋)**
- 로그인 창 「심사용」 체크박스: 켜진 채 열리고 시연 계정이 채워짐(학생 A · 선생님은 `DEMO_TEACHER.pw`를 넣어야), 끄면 빈 칸·직접 입력, 끈 것은 기기에 남음(localStorage `judge`). "계정이 없나요? 시연 학급으로 들어가기 →" 링크 제거. 교사 로그인도 창을 닫음. §14 "로그인 창 「심사용」 체크박스".
- 첫 화면 꼬리에 "출처와 저작권" 페이지(`CreditsPage`, 개인정보 처리방침과 같은 틀). 문구는 초안. `CREDITS.md`에 Twemoji(CC BY 4.0)·출처 미상 아이콘·라이브러리·이론 근거 추가, 고운돋움 제작자 정정. 음원 셋은 Pixabay(Pixabay Content License)로 채움. **120×120 아이콘의 출처는 아직 비어 있다.**

## 2. 데이터 구조가 바뀐 것 — 배포 때 챙길 것

| 바뀐 것 | 어디 | 해야 할 일 |
|---|---|---|
| 공감 키 `<날짜>_<보낸이>_<어휘키>` (전 `<날짜>_<보낸이>`) | `app/emp/<받는이>/` | 옛 키도 읽힌다. **Firebase 콘솔 규칙이 emp 자식 키 패턴을 검사한다면 갱신**(저장소 규칙 파일에는 emp 규칙이 없다) |
| 광장 답이 `o` 한 칸(f·w 빈 칸), 질문 노드에 `ai`(자동 순번), 질문 아이디 `q<시각>_<난수>` | `app/plaza/<코드>/` | 옛 세 칸 답은 `plazaAnsText`가 이어 읽는다. 규칙 변경 없음 |
| 시연 학생 이름 25명, 학교 이름 "마음 바다 체험 학급", 말 걸어봤어요 사례 | 실제 Firebase `DEMO01` | **`#seed` 패널 "전체 다시 만들기"를 교사 비밀번호로 한 번** — 명단·섬 요약·반딧불·광장이 새 이름·새 키로 다시 생긴다 |
| 교사 전용 경로 `app/tpriv/<교사>/<해시>/talk` | 규칙 | 사용자가 2026-09-15 콘솔에 넣었다고 기록됨. **실제 교사 계정으로 저장·재로그인 후 표시 유지를 아직 안 봤다** |

## 3. 남은 작업 (2026-09-16 기준)

### 3-1. 확인 안 된 것 — 하네스(헤드리스 크롬 + 메모리 DB)로만 봤고 실제로는 안 본 것

1. **실제 Firebase 시연 학급 재생성 + `TEST.md` 0-B 전체.** 이름·공감 키·광장 답 형식·학교 이름이 실제 DB에 없다. "규칙에 막힘" 로그가 한 줄도 없어야 한다.
2. **실제 교사 계정에서 `tpriv` 저장.** 「말 걸어봤어요」를 누르고 새로고침·재로그인해도 표시가 남는지, 수업일 사흘 뒤 「다시 볼 때예요」가 뜨는지. 시연 계정은 메모리에만 쓰므로 여기서는 못 본다.
3. **공감 두 번 보내기 실제 계정.** 같은 친구에게 다른 마음 말로 두 번(둘 다 남아야), 같은 말로 두 번("이 마음에는 이미 했어요", 하나만). 규칙이 새 키를 받는지.
4. **크롬북 실기기.** 내 섬 fps(맥 60fps·93 calls·37k tri), 광장(60fps·140 calls), 바다 진입 시간, 달리기 속도 체감(2.6이 빠르면 상수 하나), 터치 패드(내 섬·광장), 튜토리얼 다섯. `TEST.md` 8-1·8-6·D-5. 기준 수치(2026-09-07 21fps)는 그 뒤 바뀐 게 많아 다시 잡아야 한다.
5. **리포트 실제 브라우저.** 팝업 허용 뒤 열림, "PDF로 저장 / 인쇄"에서 「마음 말이 늘어났어요」가 페이지를 넘지 않는지(A4 폭 넘침 0은 하네스로 확인).
6. **계정 보호 `TEST.md` 0-A(A-3~A-14)**와 4단계 규칙(`RULES_STEP4.json`) 적용 여부.
7. **통합 테스트 `TEST.md` 1~9**(교사 1 + 학생 3). 아직 한 번도 실제로 돌리지 않았다.
8. 실제 학급으로 로그인하면 시연 띠가 **없어야** 한다(B-12).

### 3-2. 문서 갱신

- `TEST.md`의 옛 동작 항목(B-5·B-6·B-8·2-10·3-4·3-5·5-6·6-4·7 공감 키)과 `DEMO.md` 6번은 2026-09-16 저녁에 지금 동작으로 고쳤다. 항목 번호는 그대로.

### 3-3. 결정이 남은 것 / 보류

- **캐릭터 재작업**(stash `character-rework`, 2026-09-14): 세그먼트 A/B/C와 색군은 사용자가 고른다. 고르기 전까지 main 캐릭터 그대로. 제출 전 위험이 크면 그대로 둔다.
- 자동 질문 진행은 "올릴 때마다 다음"으로 확정(2026-09-16). 날짜 기준 자동 진행을 원하면 `plazaAutoNext`만 바꾼다.
- 광장 카메라는 고정으로 확정. 친구 섬 나침반에는 연을 그리지 않기로 했다(비교 금지, §7).
- 「요즘 달라진 아이」는 판정 코드가 없다. 되살리려면 §9-④·`PHRASES.md` 특수 상황 절에서 시작(시연 데이터의 사례 10·12·16번은 그대로 있다).
- PHRASES "긍정 계열이 급감했을 때"는 판정이 없다(예전부터).
- 바다 진입 성능의 더 큰 개선(O(1) 계산·먼 섬 축소·인스턴싱 합치기)은 제출 전엔 하지 않기로 했다(2026-09-15).
- 인증(Firebase Auth) 도입은 범위 밖 — §13 "인증 도입 단계에서 할 일".
- **시연 교사 비밀번호를 코드(`DEMO_TEACHER.pw`)에 넣을지**(2026-09-17): 넣으면 누구나 시연 교사 화면에 들어온다(페이지 소스가 공개, 저장소도 공개). 다른 곳에 안 쓰는 시연 전용 비밀번호로 `tcred/demoteacher`를 다시 만든 뒤 넣는 것을 권고. 넣기 전엔 선생님 탭 체크박스가 없다.
- 출처 페이지 문구 확정, 아이콘 출처 채우기(`CREDITS.md`).

### 3-4. 알고 있는 한계 (숨기지 않는다)

`DESIGN.md` §13 "알고 있는 한계"(기본 비밀번호 1234, 명단 공개 읽기, 익명 키 역산)와 §14 "알고 있는 한계"(기기 시간, 재생성이 심사위원 글을 지움, 교사 자격 재생성 불가). 컨설팅 답변은 "계정 정보는 막았고, 기록 접근 통제는 인증 도입 단계".

## 4. 검사·렌더 하네스 (로컬 전용)

- `node demo/check.js` — 시연 생성기 검사. 커밋 전마다 통과해야 한다(문장 라이브러리, 어휘 수, 살펴봐 둘, 말 걸어봤어요 사례, 반딧불, 광장, 이름 검사).
- `renders/harness/`(gitignore, 이 기기에만): 저장소 루트에서 `python3 -m http.server 8765 --bind 127.0.0.1 &` 뒤
  `cd renders/harness && HTTP=8765 node gen.js` → `h.html`(Firebase를 메모리 DB로 바꾼 사본). 장면은 `HTTP=8765 node shoot2.js scenes_*.json`
  (query에 `#dev`를 붙이면 `window.__isle`·`__sea`·`__plaza` 조작 API, `seed=1`이면 시연 학급 전체가 메모리에 생긴다).
  투어: `shootTutIsle.js`·`shootTutSea.js`(바다+광장)·`shootTutTeacher.js`. 3D 조작: `shootOps.js`. 리포트: `shootReport.js`. 콘솔 오류는 매 장면 `!! console:` 줄로 찍힌다.
  없어지면 `renders/README.md`와 메모리 노트대로 다시 만든다(반나절).
- 스크린샷 번호는 `renders/`에 이어서 붙인다(마지막 573).

## 5. 시연 계정 (DEMO.md 요약)

학교 코드 `DEMO01`, 학생 `d01`~`d25` / `1234`, 학생 A **탐험이** `d07`, 학생 B **햇살** `d21`, 교사 `demoteacher`(비밀번호는 저장소에 없음 · 제출 서류 별지).
살펴봐 주세요 진주(11)·노을(18), 「다시 볼 때예요」 안개(23)·이슬(24). 시연 시계는 2026-09-16 고정.
