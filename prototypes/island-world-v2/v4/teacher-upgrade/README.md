# 로그인·선생님 화면 개선 — 2026-09-19

현재 상태: **로컬 구현·검증 완료, 게시하지 않음.**

- 작업 브랜치: `codex/login-teacher-upgrade-20260919`
- 기준 게시 커밋: `81c473d10789bf1bbc496be3fcd6e71fad392874`
- 최종 후보: `7f24e1af0f23` (정적 파일 63개)
- 안전한 미리보기: http://127.0.0.1:8781/
- 루트 `index.html` 및 기존 게시 자산은 변경하지 않았다. 기존 미커밋 작업을 보존했다.

## 반영한 다섯 가지

1. 로그인: 밝은 크림/민트 카드, 진한 글자, 구분되는 입력칸, 큰 역할 선택과 로그인 버튼, 친근한 환영 문구. 입력 오류 안내·키보드 초점·진행 중 중복 클릭 방지를 유지/보강했다.
2. 동의서: 학생 등록 여부와 관계없이 교사 첫 화면에서 `동의서 PDF 내려받기` 제공. 빈 학급의 동의 안내도 같은 PDF를 사용한다. A4 한 장, 한글 글꼴 포함. 학교명/운영 주체/활동·보관 기간/문의처/대체 활동 등은 학교가 작성하는 빈칸이다. 법률 검수 완료 문서로 표시하지 않는다.
3. 학생 명단: `비우기` → `기록 전체 삭제`. 확인 문구에서 학생 계정은 남고 일기 및 연결 탐험 기록이 삭제됨을 명시한다. 삭제 동작 자체는 변경하지 않았다.
4. 교사 안내: `이 화면 안내` → `선생님 길잡이`. 기존 6단계 안내에 연결된다. 좁은 화면에서 제목과 버튼을 분리하고 심사용 안내 띠가 PDF 카드를 가리지 않도록 배치했다.
5. 교사 심사용: 로그인에서 `선생님` + `심사용`을 선택하면 가상 학급에 들어간다. 학생 심사 계정 `d07`(탐험이)과 교사 `demoteacher`가 같은 `DEMO01` 메모리 학급을 사용한다. 같은 탭에서 로그아웃/역할 변경 시 일기를 공유하며 새로고침하면 초기화된다.

## 심사 계정과 실제 자료의 경계

기존 `demo/seed-data.js` 및 순수 생성 함수 `demo/seed-gen.js`의 가상 학생 25명 시나리오를 재사용한다. 운영 계정을 만들거나 기존 Firebase의 시연 학급을 읽지 않는다. 심사용 비밀번호는 페이지 안에서 일시 생성되며 출력/배포 파일에 고정 비밀번호를 넣지 않는다. 선택된 학생과 교사의 인증 값만 준비하여 초기 진입 시간을 줄였다.

`V4ReviewClass`는 심사용 상태에서만 기존 `dbRef` 앞에서 별도 메모리 저장소를 반환한다. 일반 계정 모드는 기존 인증과 DB 경로를 사용한다. 새로고침 간 지속 저장이나 여러 탭/기기 사이의 심사 자료 공유는 구현 범위가 아니다.

## 검증한 결과

**실제 브라우저 조작** (Firebase 제거·네트워크 연결 차단 미리보기만 사용):

- 학생/선생님 심사용 선택, 학생 로그인, 교사 로그인과 가상 명단 25명 표시 확인.
- 일반 계정 모드에서 입력칸이 비워지고 직접 입력 가능, 빈 항목 로그인 오류 안내 확인. 실제 계정은 입력하지 않았다.
- 학생 심사 계정에서 가상 일기 저장 → 처음으로 → 선생님 심사 로그인 → 탐험이 항해 일지 → 탐험일지 보기에서 방금 쓴 본문 확인.
- 교사 PDF 링크의 실제 다운로드 이벤트 확인.
- `선생님 길잡이` 실행 시 기존 1/6 단계 표시와 종료 확인.
- `기록 전체 삭제` 표시는 확인했다. 삭제 실행·비밀번호 변경·실제 학생 등록은 하지 않았다.
- 1280×720 로그인 카드 전체 표시, 390×844 로그인 및 교사 PDF 카드 가독성 확인. 좁은 교사 화면의 제목/심사 안내 겹침 수정 후 재확인. 최종 교사 화면에서 가로 넘침 없음(뷰포트 390, 문서 폭 375).
- 최종 미리보기 브라우저 오류 로그 없음.

**코드/메모리 검사** (`check.cjs`):

- 후보 모든 인라인/외부 JS 구문과 63개 파일 해시 확인.
- 기존 교사/학생 인증 본체와 저장 함수, 기존 학생·첫 화면 자산 동일성 확인.
- 일반 계정 DB 연결 경로 보존, 심사 계정의 실제 DB 참조 **0회**.
- 실제 인증 함수를 메모리 환경에서 실행해 같은 학급/25명/학생 기록 저장 후 교사 역할 전환 공유 확인.
- 잘못된 메모리 경로 차단. 검증본 Firebase SDK/초기화 제거와 `connect-src 'none'` 확인.
- 루트 `index.html`이 기준 게시 커밋과 바이트 단위로 동일함을 확인.

**PDF 검수**: 한 페이지 A4 렌더를 눈으로 확인했다. 한글 추출 가능, 본문/서명란 잘림 없음. 참고한 개인정보 동의 항목 안내: https://www.privacy.go.kr/front/contents/cntntsView.do?contsNo=38 . 실제 학교 운영 조건은 학교가 기입/확인해야 한다.

**미확인/미수행**: 실제 휴대폰·학교 크롬북의 조작/성능, 운영 계정 로그인·학생 자료 조회, 운영 동의서 법률 검수, 커밋·푸시·실제 게시.

## 산출물 및 재실행

- 소스: `access.css`, `review-class.js`, `make-consent.py`, `prepare.py`, `check.cjs`
- PDF: `output/pdf/guardian-consent.pdf`
- 후보/격리 검증본/해시: `renders/releases/login-teacher-upgrade-20260919/`
- 화면 캡처: `renders/login-teacher-upgrade-20260919/` (`login-student-final.png`, `login-mobile.png`, `teacher-mobile-final.png`, `teacher-student-linked.png`)

```sh
python3 prototypes/island-world-v2/v4/teacher-upgrade/prepare.py
node prototypes/island-world-v2/v4/teacher-upgrade/check.cjs
python3 prototypes/island-world-v2/v4/release/serve.py login-teacher-upgrade-20260919 8781
```

`candidate/index.html`은 운영 연결 코드를 보존한 게시 후보이므로 직접 열지 않는다. `serve.py`가 제공하는 `memory-preview`만 검증한다. 후보 배포 여부는 `manifest.json`의 `deployed: false`와 같다.
