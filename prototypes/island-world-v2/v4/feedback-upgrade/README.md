# 2026-09-20 우체통·버드나무·소나무 적용과 피드백 배포

사용자가 GLB 세 개를 제공하고 “반영해주고 배포도 해줘”로 게시를 승인했다. 아래 이전 단계의 모델 대기·배포 보류 상태를 대체한다.

- 마음 대화하기: 조개를 새싹 우체통으로 교체. 원래 대화 열기/저장은 그대로이며 튜토리얼과 장소 설명도 우체통으로 맞췄다. 모델 로드 실패 시 단순 우체통이 대신 표시된다.
- 버드나무는 우체통 뒤, 소나무는 탐험 연습 공간 옆에 각각 하나씩 배치했다. 바닥 높이와 카메라 가림 완화를 적용했다. 다른 나무의 다양한 수형과 기존 일기 큰 나무도 유지한다.
- 앞 단계의 한글 이동, 휠 회전, 진행 문구, 기본 소품 6종/새 선물 3종, 부두 도착/재접근, 탐험 일지 이동, 친구 선택 공개 안내를 포함한다.
- `model-source/`에 다운로드 원본을 동일 바이트로 보존했다. 모델 삼각형 수는 우체통 4,652 / 버드나무 3,782 / 소나무 4,574. 형상·UV·노드·재질은 그대로 두고 내장 이미지 9개만 최대 1024로 축소했다. GLB 합계 약 7.45MB → 2.85MB. 최적화 사본은 `model-runtime/`, 출처/해시는 `models-manifest.json`에 있다.
- 배포 전 원격 main의 추가 변경 3건(감정 문구·질문·출처)을 발견하고 `d8bfbed`까지 fast-forward로 보존했다. 최종 HTML은 이 최신 원격 HTML에서 자산 버전 경로만 변경했다.

## 최종 후보와 검사

후보 `17bff4d0b27d`, 정적 파일 83개. 안전한 검증/복구 산출물은 `renders/releases/island-feedback-models-20260920/`에 있다. `before-work/index.html`과 `previous-static.zip`은 최신 원격 `d8bfbed`의 정적 화면 백업이며 학생 DB 백업이 아니다.

다음 코드 검사가 통과했다. 실제 사용자 자료·Firebase 접속은 없다.

```sh
node prototypes/island-world-v2/v4/feedback-upgrade/check.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-feedback.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-integration.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-land.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-models.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-tutorial.cjs
node prototypes/island-world-v2/v4/social-upgrade/check-sharing.cjs
```

83개 파일의 해시/JS 구문, 기존 인증/공개 경계, 한글/조합 입력과 휠, 부두 상태, 기본 6종과 선물 3종의 저장/실패/계정 분리, 기존 배치 29,644개와 활동 접근 경로를 검사했다. 새 실제 GLB 3개를 배포용 Three.js r147 로더로 파싱해 캐시·접지·우체통 충돌 범위를 확인했다. Node 모델 검사에서는 이미지 디코딩만 대역 처리하며 실제 텍스처는 브라우저에서 확인한다.

## 최종 실제 화면 검증

Firebase를 제거한 최종 후보 `17bff4d0b27d`, 컴퓨터 브라우저 936×910에서 확인했다.

- 새 GLB 세 개와 기존 모델 모두 로딩 완료. 우체통/버드나무/소나무가 텍스처와 함께 표시되고 활동 사이 길을 걸어갈 수 있다. 우체통에서 마음 대화 창이 열림도 확인했다(같은 장면 코드의 직전 후보). 교체 중 발견한 옛 벤치 변수 참조는 최종 후보에서 수정했다.
- 연습 장소까지 실제 캐릭터 이동과 E/흰 윤곽 표시. 내 섬 휠 yaw -0.2 → 0.4825, 거리 32 유지. 친구 섬 휠 yaw -0.2 → 0.4825, 거리 30 유지.
- 내 섬 부두 → 승선 → 우리 반 바다 → 이슬의 섬 항해. 친구 섬 최초 위치 (-7,26)에서 승선 버튼 없음. 게시판까지 이동한 후 부두 (-7.1,26.4) 재접근 시 캐릭터 위 E와 승선 표시, 배의 흰 윤곽 확인.
- 친구 섬 제목 아래 선택 공개 안내와 공개 일기의 기본 비공개/개별 공개/언제든 취소 안내, 미공개 빈 상태 설명 확인.
- 최종 8786 미리보기의 브라우저 오류 로그 없음. 실제 휴대폰·크롬북·물리 한국어 IME와 기기 성능은 미확인이다. 스크린샷과 실시간 계측값을 실제 기기 성능 보장으로 사용하지 않는다.

## 안전한 실행과 게시

```sh
# 게시 후에는 prepare.py를 다시 실행하지 않고 아래 검증본을 실행한다.
python3 prototypes/island-world-v2/v4/release/serve.py island-feedback-models-20260920 8786
```

미리보기 http://127.0.0.1:8786/ 는 Firebase SDK/초기화를 제거하고 `connect-src 'none'`과 메모리 DB를 적용했다. `?inspect=1`의 접을 수 있는 걷기 검수판은 실제 이동/충돌 코드를 호출하고 배포본에는 없다.

게시 절차는 `release/install.py island-feedback-models-20260920`으로 manifest 파일만 적용 → 관련 파일 선택 커밋 → 기존 `origin/main` 일반 푸시 → `feedback-upgrade/verify-public.py`로 Pages 작업 성공과 공개 83개 파일 해시 비교 순서다. 공개 검증은 정적 바이트만 다운로드하며 운영 앱을 실행하거나 DB를 조회하지 않는다. 게시 결과는 아래에 별도 기록한다.

# 이전 단계: 2026-09-20 조작·소품·부두 피드백

별도 브랜치 `codex/island-feedback-20260920`. 기존 미커밋 변경을 보존했다. 배포된 `02192a6ab4f5`를 바탕으로 새 로컬 후보를 만들며 루트 게시 파일과 운영 DB는 변경하지 않는다.

## 반영

- 내 섬/친구 섬 이동: WASD·방향키와 ㅈㅁㄴㅇ. 물리 키 코드도 읽어 한국어 조합 중 `Process/229` 및 이동 중 한영 전환 후 키를 놓는 경우를 처리한다. 입력창에서는 이동하지 않는다. 튜토리얼 설명은 늘리지 않았다.
- 내 섬/친구 섬 휠: 좌우 회전. 확대/축소 버튼과 터치 핀치는 유지한다. 우리 반 바다의 지도는 기존 조작을 유지한다.
- 왼쪽 진행 단계: 감정일기 쓰기 / 감정 분석하기 / 선물 받기.
- 기본 소품 6종: 햇살 꽃, 흰 꽃, 작은 돌등, 이끼 바위, 갈대, 목재 벤치. 그네·등대·꽃배는 Q01 완료 후 하나를 고르는 기존 선물 선택지에 추가했다. 이전에 배치하거나 보관한 소품, 이미 받은 선물은 유지하며 재선택으로 중복 지급하지 않는다.
- 기본 배경 나무를 가지가 보이는 납작한 수관·늘어지는 수관·층진 침엽수로 분화했다. 대화/연습 공간에 낮은 바닥과 돌·꽃 테두리를 추가했다. 일기 앞 기존 GLB 큰 나무는 유지한다.
- 탐험 일지는 동쪽 해안으로 옮겼다. 상자 위치 `(19,1.7)`, 접근 위치 `(17.7,1.7)`. 안내·충돌·꾸미기 금지 영역도 함께 수정했다.
- 친구 섬 도착 지점에서는 부두를 활성화하지 않는다. 부두에서 4.6 이상 벗어난 뒤 2.1 이내로 다시 접근하면 활성화한다. 2.55까지 유지해 경계의 깜빡임을 줄인다. 새 안내 버튼은 투영 좌표가 정해지기 전에는 보이지 않아 구석에서 날아오는 첫 프레임을 막는다.
- 친구 섬 제목 아래, 공개 일기 창, 빈 상태에 ‘친구가 일기마다 선택해서 공개한 글만 보인다’는 안내를 명시했다. 기존 공개 사본만 읽기·기본 비공개·철회·공감 조건을 바꾸지 않았다.

## 새 원화

`art/3d-reference-images.zip`은 내장 이미지 생성 도구로 만든 **PNG 참고 원화 3개**와 프롬프트다.

1. `01-talk-mailbox.png`: 마음 대화하기 조개 대체용 새싹 우체통.
2. `02-talk-willow.png`: 대화 장소 주변용 버드나무.
3. `03-practice-pine.png`: 연습 장소 주변용 해안 소나무.

새 GLB를 생성하거나 그림을 3D인 것처럼 세우지 않았다. 현재 앱의 조개 GLB는 아직 기존 모델이다. 새 모델을 받은 뒤 교체할 자료를 제공한 상태다. 소나무는 배경 정리 후 나온 최종 형태를 선택했다. 원본 Downloads 및 기존 모델은 변경하지 않았다.

## 안전한 실행

```sh
python3 prototypes/island-world-v2/v4/release/serve.py island-feedback-20260920 8785
```

미리보기: http://127.0.0.1:8785/ . 로그인 → 기본 체크된 심사용 → 로그인. 실제 Firebase 코드와 외부 연결이 제거된 메모리 학급이며 새로고침하면 초기화된다. 후보 HTML은 직접 실행하지 않는다.

`?inspect=1`에서는 검증 전용 ‘장소까지 걷기’ 버튼이 표시된다. 실제 길 찾기/충돌/캐릭터 이동 코드를 호출한다. 위치 순간이동이나 저장 우회 기능이 아니다. 검증 UI 파일 `__local-review.js`는 메모리 미리보기에만 포함하며 배포 후보에는 없다.

## 코드 검사

```sh
node prototypes/island-world-v2/v4/feedback-upgrade/check.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-feedback.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-integration.cjs
node prototypes/island-world-v2/v4/feedback-upgrade/check-land.cjs
node prototypes/island-world-v2/v4/social-upgrade/check-sharing.cjs
```

- 후보 79개 파일의 구문/해시/메모리 격리, 인증·게시판 익명 조건 보존.
- 두 섬의 실제 입력 처리 함수: 한글/영문/물리 키/조합 중 입력/키 놓기, 입력창 제외, 휠 회전과 창 열림 중 정지.
- 실제 부두 선택 코드: 도착 비활성, 벗어나기와 재접근, 경계 안정, 게시판에서 부두로 전환.
- 실제 배포용 상태/저장 모듈: 새 선물 3종 획득·배치·보관·재열기, 실패 복원, 중복 지급 방지, 계정 분리. 이전 배치·보관 소유권 유지.
- 직전 후보에서 유효하던 기본 9종의 배치 좌표 29,644개 모두 보존. 모든 활동에 접근 가능한 경로 존재. 새 기본 6종 동시 배치 및 23,025개 허용 좌표의 길/물 침범 없음.
- 가상 학급에서 공개/철회/수정/삭제 시 공개 사본 회수, 동시 수정, 타 학급 차단, 방문자 개인 일기 읽기 없음. 운영 DB 호출 없음.

## 실제 화면 검증

후보 `f70a65ddf7cd` (정적 파일 79개), Firebase를 제거한 로컬 미리보기, 컴퓨터 1280×720에서 확인했다.

- 왼쪽 진행 문구와 기본 소품 정확히 6종이 보임.
- 가상 이야기 → 감정 분석/피드백 → 탐험 완료 → 그네 선물 선택 → 실제 모델 미리보기 → 정원 배치 저장 성공. 그네는 선물 카드로 표시되고 새 기본 소품으로 나타나지 않음. 이 흐름은 최종 전 단계에서 수행했고 이후 변경은 선물 안내 조사/썸네일 표시와 공개 안내 CSS뿐이다.
- 새 탐험 일지 위치까지 실제 경로로 걸어가 도착, 상자 윤곽과 캐릭터 위 E 표시 확인.
- 내 섬 휠 yaw -0.2 → 0.448, 거리 32 유지. 친구 섬 휠 yaw -0.2 → 0.34, 거리 30 유지. DOM에 표시된 실시간 렌더링 계측값으로 비교.
- 내 섬 부두 승선 → 바다에서 이슬의 섬 선택 → 항해 → 친구 섬 도착. 도착 위치 (-7,26)에서 승선 표시 없음.
- 친구 게시판까지 걷기 → 부두 재접근 (-7.1,26.4)에서 캐릭터 위 승선 표시와 배의 흰 윤곽 확인 → 승선 → 우리 반 바다 → 내 섬 복귀 완료.
- 공개 일기 창에서 선택 공개 안내와 ‘일기가 있어도 공개하지 않으면 보이지 않는다’는 빈 상태 설명 확인.
- 최종 후보에서 친구 섬 제목 아래 공개 안내와 환영 상자가 서로 겹치지 않는 것을 확인했다.
- 위 검증 도중 브라우저 오류 로그 없음. 화면 캡처로 새 배치와 버튼 위치를 육안 확인했다.

실제 휴대폰·크롬북·물리 한국어 IME와 기기 성능은 미확인이다. 한글 이동은 실제 이벤트 처리 함수를 실행한 메모리 검사이며 물리 한영키 조작 검사와 구분한다. 화면 캡처와 실시간 FPS 계측값을 기기 성능 보장으로 사용하지 않는다. 커밋·푸시·배포는 하지 않았다.
