# 2026-09-23 제출 전 최종 점검

검증 대상은 `credits-registration-20260923`의 `ec5b3d8b9ae7` 후보(정적 파일 105개)다. 기존 미커밋 작업을 보존하고 `codex/submission-audit-20260923`에서 검사했다. 이번 점검에서 새 기능 오류는 발견되지 않았다. 모든 환경에서 버그가 없다는 보증은 아니다.

## 승인 범위

사용자의 제출 전 버그 점검 요청에 따라 기존 기능을 재검증했다. 후속 “오케이 배포도 해줘 끝나면”으로 이번 수정본의 선택 커밋·origin/main 일반 푸시·기존 GitHub Pages 게시가 승인되었다. 앞선 로컬 수정의 미배포 결정을 이번 범위에 한해 대체한다. 운영 앱 실행·Firebase·실제 학생 자료 접근은 수행하지 않는다.

## 자동 검사 — 16/16 통과

```sh
node prototypes/island-world-v2/v4/submission-audit/run-suite.cjs
```

기존 검사 16개를 최신 후보로 지정해 실행한다. 예전 버튼 이름과 하위 컴포넌트 스텁만 현재 화면에 맞췄으며 앱 동작을 교체하지 않는다.

- 신규 교사 등록 직후 조치 기록 조회·저장, 재로그인 후 재조회, 교사·학생·학급별 분리.
- 일반 계정 일기·진행·마음 대화·소품 저장, 날짜 범위·연도 변경·윤년, 저장 실패와 재시도.
- 일기 비공개 기본값, 공개·철회·수정·삭제의 경계, 친구 공개 자료 변경 시 재확인, 공감 중복과 실패 처리.
- 게시판 익명화·이름 포함 답 검토·감정 선택 제한, 교사 읽기/저장 실패 처리.
- 일일 활동 완료·보상 중복 방지, 소품 위치 29,644개·목적지 7곳, 시연 가상 자료 분리.
- 감정 사전·직접 수정·그래프·튜토리얼·음소거·효과음, 정적 파일 해시·스크립트 문법.

결과: `renders/submission-audit-20260923/suite-results.json`와 각 검사 로그. HTML/CSS의 로컬 참조 68개에서 누락 0개·외부 참조 0개(`static-references.json`). 동적으로 조합하는 자산 경로는 아래 실제 화면으로 별도 확인했다.

## 실제 컴퓨터 브라우저 확인

Firebase SDK/초기화 제거·CSP `connect-src 'none'`인 http://127.0.0.1:8797/ 메모리 검증본만 실행했다.

- 심사용 학생 로그인 → 내 섬 걷기 → 2026-09-23 일기 저장, 14편에서 15편으로 증가·내용·감정 확인.
- 저장한 일기가 분석/마음 대화에 연결됨. 분석 확인 → 다섯 단계 마음 대화 저장 → 오늘 탐험 완료.
- 완료 선물 ‘작은 등대’ 지급 → 회전 → 섬 배치 성공. 선물 선택 목록이 지급 상태로 바뀜.
- 내 섬 부두 걷기/승선 → 우리 반 바다 → 물결 선택/항해 → 친구 섬 입장·회전 → 공개 일기 공감 성공·당일 중복 버튼 비활성화 → 친구 부두에서 우리 반 바다 복귀.
- 마지막 내 섬 복귀의 항해 시작까지 확인했으나, 사용자 후속 입력 시 임시 탭이 닫혀 도착 후 화면은 이번 검사에서 미확인. 내 섬↔바다 전환·저장 보존은 자동 검사 통과.
- 심사용 교사 ‘살펴봐 주세요’ 정상 조회 → 가상 학생 ‘학생과 개별 대화’ 조치 저장 → 로그아웃/재로그인 후 같은 기록 표시. 교사 검수 탭 콘솔 오류 0개.
- 앞선 동일 후보 검사에서 신규 일반 교사 생성/학생 추가 직후 조회 오류 해소, 일반 계정 날짜/일기/교사 달력, 출처 83개 항목의 1280×800·390×844 표시를 확인했다. 상세는 `../credits-registration/README.md`와 `../login-copy/README.md`.

증거 화면: `renders/submission-audit-20260923/diary-saved.png`, `daily-completed.png`, `empathy-saved.png`, `teacher-care-saved.png`.

## 제출 폴더 주의

기존 `renders/review-polish-20260922/submission-preparation-1af4f73d23eb/`는 **이전 버전**으로 이번 문구·교사 수정·출처 화면이 없다. 최종 폴더링 시 최신 후보 `ec5b3d8b9ae7`을 기준으로 다시 구성해야 한다. `review-polish/prepare-submission.py`도 이전 후보를 가리키므로 그대로 실행하면 안 된다.

게시용 원본: `renders/releases/credits-registration-20260923/candidate/`. 안전한 로컬 검증본: 같은 경로의 `memory-preview/`. 최종 제출 폴더는 아직 생성하지 않았다. 검증용 스크립트·캡처·이전 후보 폴더를 실행본에 섞지 않는다. 메모리 검증본의 가상 기록은 새로고침 시 초기화되며 실제 계정의 영구 저장본이 아니다.

## 미확인 및 게시 확인

운영 Firebase 저장/보안 규칙, 실제 휴대폰·크롬북·물리 터치·성능, USB 직접 실행·다른 OS는 미확인이다. 기존 연구보고서·원자료·권리 증빙은 이번 앱 검사의 대상이 아니다.

게시 시 `python3 prototypes/island-world-v2/v4/release/install.py credits-registration-20260923`로 검증된 파일만 적용한다. 설치 뒤 생성기를 재실행하지 않는다. 기존 버전 자산은 유지한다. 원격 main은 적용 전 `cceff03`임을 확인했다. 공개 앱을 실행하지 않고 정적 응답 105개 해시와 Pages 작업 결과로 확인한다. 게시 영수증: `renders/releases/credits-registration-20260923/deployment-result.json`.
