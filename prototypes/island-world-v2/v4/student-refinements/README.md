# 학생 활동 후속 수정 — 2026-09-21

사용자의 전체 수정 목록과 자동 배포 요청에 따라 별도 `codex/student-refinements-20260921` 브랜치에서 작업했다. 기준은 `18e1e5f` / `e80ca551ac83`, 최종 검증 후보는 `61b6f13bb407`이다. 기존 미커밋 변경은 보존했다.

## 반영

- 원본 `347e610:index.html`과 실제 감정 사전을 비교했다. 변수 이름은 EMOTIONS_28이지만 실제 **30개(긍정 15·부정 15)**이며 목록·키워드·점수·구역이 모두 원본과 같다. 문제는 새 대화 화면이 모든 일기를 허용했던 것이었다. 원본과 같이 부정 감정이 하나라도 있는 개인 일기만 선택·저장하고, 학생이 직접 수정한 결과를 우선한다. 긍정만 있는 계정은 빈 상태를 표시한다. 가상 이야기는 명시적으로 골라 연습한다. 과거 대화 저장값은 삭제하지 않는다.
- 심사용 브라우저 메모리 학급의 탐험이에게 ‘친구들이 나를 빼고 놀이를 시작해서 속상하고 외로웠다’ 일기를 추가했다. 초기 대화에서 선택할 수 있다. 실제 시연 생성기나 운영 DB를 실행하지 않았다.
- 제목·도움말·질문을 요청한 문구로 바꾸고 5단계 선택지 아래 세 예시 및 지정 안내를 삭제했다. 희망 예시의 `설렜다.`를 제거하고 `기대된다.`를 사용한다. 유효한 희망 감정 사전은 보존하며 `설 다`는 인식되지 않음을 검사했다.
- 감정일기 쓰기에 최근 5편과 이전 일기 펼치기, 각 일기의 긍정/부정 비율, ‘내 감정은 달라요’를 추가했다. 여러 감정 선택, 기타 감정과 긍정/중립/부정·에너지 선택, 실패 시 입력 유지와 재시도를 제공한다. 원문과 자동 분석 근거를 보존한다. 직접 수정한 결과는 분석 및 마음 대화에 반영된다.
- 저장 자료를 다시 읽는 `safeAnalysis`에서 빠지던 직접 수정 표시·자동 분석 근거·선택 연도를 보존한다. 로그인 직후와 교사 재조회에서도 자동 분석으로 되돌아가지 않는다.
- 새 날짜는 기존 예시의 `M.D` 형식으로 저장한다. 분석 메타데이터의 `entryDate`에는 선택한 ISO 날짜를 보관해 과거 연도·윤년·달력·기간 검색을 유지한다. 시연에서는 기존 시연 시계를 사용한다. 기존 학생 자료 일괄 변경은 없다.
- 기록 보관함에 ‘내가 공개한 일기 보기’를 추가했다. 내 섬에서 기물에 접근해 열 수 있다. 기존 우리 반 공개 사본 조회와 명시적 공개/철회, 학급 경계는 그대로 사용한다.
- 분석 명칭·분석 소개·네 종류의 질문·확인 보상 안내·연습 안내·게시판 ‘체험 중’을 반영했다. 직접 수정 결과도 차트에 반영한다고 안내한다.
- 전체 보기 버튼이 아이콘용 고정 폭에 갇혀 글자가 깨져 보이던 문제를 텍스트 버튼 전용 폭과 줄바꿈 규칙으로 수정했다.

## 검사 — 통과

```sh
node prototypes/island-world-v2/v4/student-refinements/check.cjs
node prototypes/island-world-v2/v4/student-refinements/check-refinements.cjs
node prototypes/island-world-v2/v4/student-refinements/check-dialogue.cjs
node prototypes/island-world-v2/v4/student-refinements/check-persistence.cjs
node prototypes/island-world-v2/v4/check-student-tools.cjs
node prototypes/island-world-v2/v4/social-upgrade/check-sharing.cjs
```

검증 후보 89개 정적 파일 SHA-256/JS 및 HTML 구문, 인증·DB 관문 보존, Firebase 제거와 CSP `connect-src 'none'`, 원본 30개 감정 비교, 긍정만/부정/혼합/다른 사람/부정어 판정, 감정 수정→실제 읽기 정규화→재분석 보존, 중립·직접 입력·윤년/과거 연도, 실제 컴포넌트 저장 실패/재시도, 대화 자동 채움/계정 분리/과거 기록 호환, 공개/철회/동시 수정 방지, 날짜별 탐험과 기존 선물 보존을 검사했다.

## 실제 컴퓨터 브라우저 — 확인

Firebase SDK/초기화를 제거한 로컬 검증본에서만 조작했다.

- 심사용 학생 로그인 → 부정 예시 자동 선택 → 다섯 질문 → 예시 선택 → 대화 저장.
- 긍정 새 일기 → 최근 일기의 긍정 100%/부정 0% → 마음 대화 목록에서 제외.
- 분석 확인 → 부정 감정 없음 안내/대화 생략 → 정상 완료 및 ‘감정 분석하기’ 명칭.
- 최근 일기 → 슬픔으로 수정 → 부정 100% → 마음 대화 목록 포함. 원문 유지.
- 학생 로그아웃 → 심사용 교사 로그인 → 탐험이 탐험일지에서 신규 `9.16` 날짜 및 자동 감정/직접 감정 분리 → 한 일기 분석에서 수정한 ‘슬픔’ 유지.
- 내 섬 기록 보관함까지 실제 걷기 → 접근 버튼으로 열기 → 가상 일기 공개 → ‘내가 공개한 일기 보기’에서 조회 → 비공개 전환 후 목록에서 제거.
- 게시판 접근/열기 → 하단 ‘체험 중’. 부두까지 걷기/승선 → 바다 전체 보기 조작.
- 컴퓨터 390×844 창에서 최근 일기·감정 수정 및 전체 보기 버튼 확인. 전체 보기 버튼은 78px 폭/44px 높이, 줄바꿈 없음, 가로 넘침 없음. 이는 실제 휴대폰 검증이 아니다.

실제 휴대폰·크롬북·물리 터치·기기 성능 및 운영 계정 저장은 **미확인**이다. 자동 감정 분석은 기존 로컬 사전 방식이며 모든 한국어 문장의 정확도를 보장하지 않는다.

## 실행과 배포

```sh
# 설치 이후 prepare.py를 다시 실행하지 않는다.
python3 prototypes/island-world-v2/v4/release/serve.py student-refinements-20260921 8790
python3 prototypes/island-world-v2/v4/release/install.py student-refinements-20260921
python3 prototypes/island-world-v2/v4/student-refinements/verify-public.py
```

안전한 화면: http://127.0.0.1:8790/ . 로컬용 걷기 검수 버튼은 `?inspect=1`에서만 나타난다. 게시 후보에는 로컬 검수 파일을 포함하지 않는다. candidate HTML을 직접 실행하지 않는다.

검증 manifest와 이번 소스/승인 문서만 선택 커밋하고 origin/main에 일반 푸시한다. Pages 성공 및 공개 정적 파일 SHA-256으로 실제 게시를 확인한다. `renders/releases/student-refinements-20260921/previous-static.zip`은 직전 화면 정적 복구본이며 학생 DB 백업이 아니다. 이전 자산은 삭제하지 않는다.
