# 일기·게시판·교사 길잡이 수정 — 2026-09-21

기준 게시본 `7b17962` / `61b6f13bb407`, 최종 로컬 후보 `7a8468f313e6`. 브랜치 `codex/diary-board-fixes-20260921`. 기존 미커밋 파일과 게시된 `index.html`·정적 자산을 보존했다. **로컬 구현·검증 완료, 후속 사용자 요청으로 실제 게시 승인**.

## 요청별 반영

1. 들어가면 실제 오늘 날짜. 심사용 고정 9.16을 오늘로 바꾸고 다시 열 때 남은 날짜를 초기화한다. 직접 고른 과거 날짜·연도는 저장한다.
2. 저장 뒤 일기창 유지, 최근 일기 즉시 갱신·이동·성공 안내. 교사 1단계 설정으로 탐험이 완료돼도 창을 유지한다. 일기 본문 실패와 진행 저장 실패를 구분하고 후자는 같은 기록으로 재시도한다.
3. 일반 문장 일기의 기존 연결을 확인하고, 누락되던 직접 선택 감정을 명시적 학생 분석으로 반영한다. 자동 근거·원문은 보존한다. 알려지지 않은 직접 표현은 중립으로 보관하며 기존 ‘내 감정은 달라요’에서 직접 설정할 수 있다. 분석·마음 대화·탐험일지·섬 어휘·교사 재조회가 같은 저장 자료를 사용한다. ‘친구가 기다려 줘서 고마웠다’의 감사 누락도 원인절 판정으로 보완했다. 기존 원문/학생 자료 일괄 변경은 없다.
4. ‘안됐어.’ → ‘다음에는 어떻게 해볼까요?’ 입력칸. 저장 요약·다시 열기·이어서 쓰기 및 가상 연습에 반영한다. 기존 대화의 다음 계획을 삭제하지 않는다.
5. ‘나와 친구의 이름은 쓰지 않아요.’로 변경.
6. ‘색깔이 없는 감정 단어는 내가 아직 사용해보지 않은 단어에요. 누르면 뜻을 알 수 있어요.’로 변경.
7. 게시판 ‘기타’와 40자 입력칸. 기타를 포함해 5개, 빈 칸 안내, 저장/고치기 시 복원. 본문과 기타의 본인/친구 이름에 경고하며 재저장 시 공개 사본 없이 교사 확인 상태로 보관한다. 기존 익명 공개 문턱과 학급 경계는 유지한다.
8. 완료 뒤 마지막 목적지 별과 빛 제거. 이전 목적지 요청도 지워 재표시를 방지한다. 완료 후 사용자가 직접 밝힌 장소는 안내한다.
9. 교사 카드·게시/숨김 버튼·길잡이의 ‘광장’을 ‘우리반 게시판’으로 변경.
10. ‘선생님 길잡이’에 보상 난이도 카드 강조 단계 추가. 학년·반, 세 완료 조건, 저장, 대화 없는 날, 기존 선물 유지, 빈 반 안내를 설명한다.

## 코드·메모리 검사 — 통과

```sh
python3 prototypes/island-world-v2/v4/diary-board-fixes/prepare.py
node prototypes/island-world-v2/v4/diary-board-fixes/check.cjs
node prototypes/island-world-v2/v4/diary-board-fixes/check-flows.cjs
node prototypes/island-world-v2/v4/diary-board-fixes/check-dialogue.cjs
node prototypes/island-world-v2/v4/student-refinements/check-persistence.cjs diary-board-fixes-20260921
node prototypes/island-world-v2/v4/check-student-tools.cjs
node prototypes/island-world-v2/v4/social-upgrade/check-sharing.cjs
```

후보 89개 파일 해시·JS 구문, Firebase SDK/초기화 제거 및 CSP `connect-src 'none'`, 기존 인증·DB 관문·공개 조회 경계 보존을 검사했다. 실제 컴포넌트의 실패/재시도/중복 방지, 날짜/윤년, 선택 감정 정규화·그래프·대화·섬 어휘, 돌아보기 다음 계획과 기존 기록, 기타 감정의 빈 입력/5개 제한/재열기/이름 경고, 계정 분리와 동시 저장·선물 보존을 검증했다. 기존 학생 도움 검사도 바뀐 선택 감정의 분석 반영 조건으로 갱신했다.

## 실제 브라우저 조작 — 확인

운영 SDK가 제거된 `127.0.0.1:8792`에서 메모리 가상 계정만 사용했다.

- 최종 보완 후 ‘오늘 친구가 기다려 줘서 고마웠다.’를 실제 화면에서 다시 저장해 ‘감사 · 긍정 100%’와 열린 일기창을 확인했다. 브라우저 오류 로그는 없었다.
- 학생 일기의 기본 날짜 `2026-09-21`, 일반 문장 저장 뒤 창 유지·최근 1편 증가·긍정/부정 비율.
- 감정 도움에서 ‘친구’+‘슬픔’만 선택해 저장 → 최근 일기 부정 100% → 분석 목록/원형 그래프/부정 TOP 10 → 마음 대화 선택과 느낌 반영.
- ‘몽글몽글함’과 ‘슬픔’으로 저장 → 학생 최근 목록 및 로그아웃 후 가상 교사의 탐험일지 달력·선택 감정 재조회.
- 분석 확인 → 마음 대화 ‘안됐어.’ → 다음 행동 입력/저장 → 탐험 완료 → 섬 복귀. DOM 별 표시 0개, 3D 장면 진단의 destination=null.
- 게시판까지 실제 걷기 → 접근 버튼 → 지정 문구·기타 입력 → 저장 후 내 답 표시 → 고치기에서 입력 유지.
- 교사의 카드/버튼에 ‘우리반 게시판’, 선생님 길잡이 2/7 단계에서 ‘탐험 보상 난이도’ 설명·카드 강조.
- 교사 난이도 1단계 저장 → 학생 재로그인 → 일기 저장 시 완료 안내와 최근 일기를 같은 열린 창에서 확인.
- 1280×800 및 좁은 컴퓨터 창에서 입력칸·버튼·길잡이 화면을 확인했다. 화면 크기 검수는 실제 모바일/터치 성능 검증이 아니다.

실제 휴대폰·크롬북·물리 터치·기기 성능 및 운영 계정 저장은 **미확인**. 자동 분석은 로컬 사전 방식이다.

## 안전한 미리보기

```sh
python3 prototypes/island-world-v2/v4/release/serve.py diary-board-fixes-20260921 8792
```

http://127.0.0.1:8792/ 에서 심사용으로 로그인한다. `?inspect=1`은 메모리 검증본에만 걷기 버튼을 제공한다. 서버 종료는 실행 터미널에서 Ctrl+C. 후보 HTML은 Firebase 연결 코드가 있으므로 직접 실행하지 않는다. `prepare.py`는 기준 게시본이 일치할 때만 생성한다. 구현 단계에서는 운영 적용/커밋/푸시를 수행하지 않았으며, 후속 “배포도 해줘” 요청으로 아래 게시 절차를 승인했다.


## 2026-09-21 후속 배포 승인과 검증

사용자가 “배포도 해줘”로 이번 후보의 커밋·푸시·기존 GitHub Pages 게시를 요청했다. 원격 main `7b17962`와 기준 게시본 일치를 확인했다. 위 여섯 검사 모두 재통과했고, 후보 재생성 결과도 `7a8468f313e6`로 동일하다. 배포 직전 메모리 브라우저에서 심사용 로그인 → 감정일기 쓰기 → 오늘 날짜 → ‘오늘 친구가 기다려 줘서 고마웠다.’ 저장을 다시 조작했다. 일기창 유지·최근 일기 14→15편·감사/긍정 100% 표시를 확인했다.

```sh
python3 prototypes/island-world-v2/v4/release/install.py diary-board-fixes-20260921
python3 prototypes/island-world-v2/v4/diary-board-fixes/verify-public.py
```

설치 뒤에는 `prepare.py`를 재실행하지 않는다. manifest의 정적 파일 89개와 이번 소스/검사/승인 문서만 선택 커밋하고 origin/main에 일반 푸시한다. 다른 미커밋 작업은 보존한다. Pages 성공과 공개 파일 SHA-256 일치 결과는 `renders/releases/diary-board-fixes-20260921/deployment-result.json`에 남긴다. 운영 앱 스크립트를 실행하거나 Firebase에 접속하지 않는다.

직전 화면 복귀 기준은 `7b17962:index.html`, 정적 복구본은 `renders/releases/diary-board-fixes-20260921/previous-static.zip`이다. 학생 DB 백업이 아니다. 복귀 요청 시 이전 index를 별도 일반 커밋·푸시로 다시 게시한다. 이전 버전 자산을 삭제하거나 강제 푸시하지 않는다.
