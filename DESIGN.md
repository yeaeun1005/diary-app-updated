## 2026-09-20 사용자가 전달한 3D 모델 6종 적용 — 직전 요청의 후속 구현

사용자가 후속으로 섬 규모를 조금 확대하고 각 정원 배치 공간을 넓히며 이전 느낌의 튜토리얼 추가 후 배포를 명시했다. 바깥 지형 반경을 10% 넓히고 기존 여섯 정원의 허용 범위를 확장한다. 기존 말풍선/하이라이트 엔진을 재사용해 첫 방문 선택 안내·걷기 체험·활동/정원/부두 소개 및 다시 보기를 추가한다. 안내 여부는 계정별 이 브라우저에만 저장하며 일기/보상은 변경하지 않는다.

사용자가 앞서 요청한 상호작용/꾸미기 그림을 실제 GLB 여섯 개로 제공했다. 직전 6·8번의 3D 에셋 추가 작업을 이어서 적용하고 기존 배포 지시 범위에서 검증한 정적 파일을 게시한다.

- 파스텔 조개 → 마음 대화하기, 나침반 → 탐험연습하기, 보물함 → 탐험 일지보기. 기존 제목·활동·저장·접근 흐름을 유지하고 단순 대체 모형을 실제 GLB로 바꾼다.
- 그네·작은 등대·꽃배를 정원 기본 소품으로 추가한다. 종류별 한 개씩 배치/회전/보관하며 기존 보상과 학생이 이미 놓은 소품을 보존한다.
- 다운로드 원본은 수정하지 않는다. 보관 사본과 해시를 남기며 앱용 사본의 내장 텍스처만 1024로 축소한다. 형상·UV·재질·좌표 데이터는 바꾸지 않는다.
- Firebase 제거 메모리 후보로 저장/배치/충돌을 검사한다. 운영 자료나 DB 규칙은 변경하지 않는다. 브라우저 도구가 이번에도 초기화 오류를 반환했으므로 실제 화면 조작 확인과 모델 자체의 오프라인 렌더 검수를 구분한다.

구현/코드 검증 완료: 후보 `02192a6ab4f5` (79개 파일). GLB 6개 로더 파싱과 오프라인 시각 검수, 기본 소품 9종 저장/계정 분리, 기존 배치 13,480개 유지 및 새 허용 배치 29,644개, 모든 활동 접근 경로, 튜토리얼 6단계/다시 보기 검사 통과. 브라우저 도구 초기화 오류로 이번 실제 앱 클릭/화면 배치·기기 성능은 미확인이다. 상세 구분은 `prototypes/island-world-v2/v4/model-upgrade/README.md`. 사용자 배포 지시에 따라 이 제한을 기록하고 정적 게시한다.

## 2026-09-20 친구 섬·우리 반 바다·선택 일기 공개 및 배포 — 사용자 승인

> 2026-09-20 게시 완료: `e890be35c4e100df489f5258fc798d7930ab77a9`, 후보 `bc840ba1a34a`. GitHub Pages 작업 `35452342261` 성공 및 공개 정적 파일 70개 해시 일치. 운영 DB 접속 없음. 브라우저 도구 장애로 이번 화면 직접 클릭/캡처는 미확인. 상세 영수증: `renders/releases/friend-sea-upgrade-20260920/deployment-result.json`.

사용자가 다시 연습 제거, 게시판 위 떠 있는 간판 제거, 친구 섬/바다 개선, 친구 섬 부두에서 바다 복귀, 바다의 친구 섬을 내 섬 형태로 통일, 정원 확대, 공감 문구·별 연출, 학생이 고른 일기 공개/비공개, 3D 제작용 개별 그림 제공, 배포를 요청했다.

- 직전 내 마음섬·로그인/교사 개선을 포함해 검증 후 기존 GitHub Pages에 선택 커밋·일반 푸시로 게시한다. 앞선 두 단계의 배포 보류를 이번 승인으로 대체한다. 운영 자료 조회·수정/시연 학급 재생성은 하지 않는다.
- 친구 섬 부두 → 우리 반 바다 → 친구 섬 또는 내 섬 동선으로 바꾼다. 친구 섬 글꼴/도구/그림 E와 바다 미니어처를 새 내 섬 톤에 맞춘다.
- 기존 개인 일기 무조건 비공개 규칙을 **학생이 명시적으로 선택한 일기에 한해** 대체한다. 기본은 비공개이며 공개 전 내용·범위를 확인한다. 친구 방문은 개인 일기 경로를 읽지 않고 명시적 공개 사본만 읽는다. 일기 수정·삭제·비공개 전환 시 공개 사본도 제거한다. 기존 일기를 일괄 공개하지 않는다.
- 공감 조건과 중복 제한은 유지하면서 선택적인 짧은 응원 글과 성공 시 별 연출을 추가한다. 같은 학급 확인·입력 길이 제한·이름/연락처 안내·텍스트 렌더링을 적용한다. 별 연출은 동작 줄이기 설정을 존중한다.
- 활동 오브젝트 3종과 꾸미기 소품 3종을 단일 오브젝트 그림으로 제공한다. 사용자가 3D 에셋으로 변환할 자료이며 실제 GLB가 생성된 것처럼 보고하지 않는다. 내장 이미지 생성 도구만 사용한다.
- 정원을 더 늘리되 물/길/고정 물체를 막지 않게 검사한다. 실제 조작은 Firebase 제거 메모리 검증본에서 한다. 운영 인증/DB 규칙은 변경하지 않는다.

구현·코드 검증 완료: 후보 `bc840ba1a34a`(70개 정적 파일). 여섯 정원·친구 섬/바다 UI·새 미니어처·별 공감/응원 글·일기별 선택 공개를 반영했다. 실제 인증 함수와 가상 학급을 사용한 학생 저장/친구 공개본 읽기/공개 취소/교사 원문 확인, 기본 비공개·타 학급 거부·수정/삭제 원자적 철회·동시 수정 검사 통과. 3D 변환용 PNG 6개를 별도 제공했다. 브라우저 자동화가 초기화 파일 오류로 시작되지 않아 이번 실제 화면 조작/캡처는 미확인이다. 상세 내용은 `prototypes/island-world-v2/v4/social-upgrade/README.md`에 있다. 사용자 배포 요청에 따라 이 제한을 명시하고 정적 게시를 진행한다.

## 2026-09-19 내 마음섬 시인성·활성화·완료 연출 — 사용자 구현 승인

사용자가 내 마음섬의 닻 제거/그림 제목, 두꺼운 글꼴, 하단 도구 정렬, 조작 힌트 정리, 떨리는 장소 안내 수정, 퀘스트 완료감/중앙 버튼/글로우, 빛 끄기 제거, 소품 공간 확대, 게시판 글자 및 양쪽 접근, 답 자동 공개, 캐릭터 위 E 그림과 가까운 물체의 흰 테두리, 일기 앞 이동과 일기·분석 가독성 개선을 요청했다.

- 앞선 로그인·선생님 개선 후보를 이어받아 로컬에서 구현한다. 게시 파일·운영 자료는 변경하지 않는다.
- 일기/분석/대화/연습/일지의 기존 제목과 저장 흐름, 친구 섬·부두 동선, 비공개 일기와 공감 규칙은 유지한다. 화면에는 E만 보이고 한글 ㄷ 입력도 작동한다.
- 장소 안내는 프레임에 맞춰 부드럽게 갱신하고 접근 경계에서 선택이 깜빡이지 않게 한다. 활성 장소에는 흰 윤곽, 캐릭터 위에는 그림 키를 표시한다.
- 기본 소품을 놓을 정원을 확대하되 길·물·고정 물체를 막지 않게 검증한다. 일기 책상 앞 지형의 높이/접근 경계를 점검한다.
- **기존 교사 선별 후 게시 규칙을 이번 요청 범위에서 대체**한다. 새로 저장/수정한 일반 게시판 답은 익명으로 자동 공개한다. 친구 이름 경고·이름 포함 답 숨김, 교사의 숨기기, 기존 익명 최소 3개 조건은 유지한다. 과거 비공개 답을 일괄 공개하거나 운영 답을 변경하지 않는다.
- 퀘스트 완료를 전용 축하 화면과 완료 상태로 표시하며 이미 받은 보상을 중복 지급하지 않는다. 동작 줄이기 설정을 존중한다.
- Firebase 제거 메모리 미리보기에서 실제 조작과 코드 검증을 구분해 기록한다. 실제 기기 성능·운영 배포는 미확인으로 남긴다.

로컬 구현·검증 완료: 후보 `9d1b7a4a6d5f`. 그림 제목·굵은 글자·중앙 도구 모음, 안정된 장소 안내와 캐릭터 위 E/흰 테두리, 완료 축하, 정원 4곳, 게시판 양쪽 접근/일반 답 자동 공개, 일기·분석 가독성을 반영했다. 실제 가상 계정 조작과 메모리/인증 보존 검사를 통과했다. 검증 내용과 한계는 `prototypes/island-world-v2/v4/island-upgrade/README.md`에 기록했다. 실제 기기·한국어 IME 입력·성능은 미확인이고 운영 게시본을 변경하거나 커밋·푸시·배포하지 않았다.

## 2026-09-19 로그인·선생님 화면 개선 — 사용자 구현 승인

사용자가 로그인 창의 가독성/초등학생 친화성 개선, 교사용 보호자 동의서 PDF 다운로드, 학생 명단의 ‘비우기’ 표현 교체, ‘이 화면 안내’ 표현 교체, 심사용 학생과 연결되는 선생님 계정을 요청했다.

- 로그인은 현재 바다 표지와 어울리는 밝은 민트/크림, 또렷한 제목/입력칸, 큰 학생/선생님 선택과 버튼으로 다듬는다. 기존 실제 계정 인증 방식은 보존한다.
- 모든 교사 대시보드에서 보호자 동의서 PDF를 내려받게 한다. 학교명·수집 주체·기간·문의처는 학교가 작성할 수 있는 빈칸으로 두고 기존 활동 목적과 수집 항목을 반영한다.
- ‘비우기’는 실제 동작을 드러내는 ‘기록 전체 삭제’, ‘이 화면 안내’는 ‘선생님 길잡이’로 바꾼다. 삭제 실행은 검증 중 수행하지 않는다.
- 학생/선생님 심사용은 기존 DEMO01의 가상 시나리오를 재사용하는 동일한 메모리 학급에 연결한다. 같은 탭에서 역할을 바꿔도 가상 일기 등을 함께 볼 수 있고 새로고침하면 초기화된다. 운영 Firebase의 시연 학급/실제 학생 자료를 생성·조회·수정하지 않는다. 실제 계정에 대한 권한을 넓히거나 공개 교사 비밀번호를 추가하지 않는다.
- 변경 소스는 prototypes/island-world-v2/ 안에서 관리하고 Firebase를 제거한 미리보기로 확인한다. 이번 요청의 구체적인 결과를 로컬에서 먼저 완성한다. 앞선 학생 기능 복원 게시 승인은 이미 완료된 배포에 대한 것으로, 이번 변경은 별도 게시 요청 전에는 실제 게시하지 않는다.

로컬 구현·검증 완료: 후보 `7f24e1af0f23`. 로그인 가독성, 항상 보이는 교사용 동의서 PDF, ‘기록 전체 삭제’, ‘선생님 길잡이’, 연결된 학생/교사 메모리 심사 학급을 반영했다. 실제 브라우저에서 학생이 저장한 가상 일기를 교사 화면에서 읽기, PDF 다운로드, 길잡이 실행, 1280×720·390×844 배치를 확인했다. 메모리/인증 보존 검사가 통과했고 실제 DB 참조는 없었다. 좁은 교사 화면의 제목/버튼/심사 안내 겹침도 정리했다. `prototypes/island-world-v2/v4/teacher-upgrade/README.md`에 수행/미수행 검증을 구분했다. 운영 게시본은 유지하며 커밋·푸시·배포하지 않았다.

## 2026-09-19 학생 도움·분석·대화 기능 복원 및 게시 — 사용자 구현·배포 승인

사용자가 학생탭에 있던 일기 도움, 긍정/부정 감정 TOP 10, 상황 탐지, 긍정/부정 분석 그래프, 마음 대화의 자동 감정/상황 탐지와 행동 기술 예시 추천을 복원한 뒤 자동 배포하라고 요청했다. 앞선 첫 화면만 수정/배포하지 않음 범위를 이번 학생 기능 복원·검증 후 기존 GitHub Pages 게시까지 확대한다. 지금까지 승인된 첫 화면도 포함한다.

- 다섯 활동 제목을 ‘감정일기 쓰기 / 감정 분석하기 / 마음 대화하기 / 탐험연습하기 / 탐험 일지보기’로 통일한다. 장소별 진입·섬 산책·친구 방문·꾸미기 동선을 유지한다.
- 이전 감정/상황 사전과 분석/차트·행동 예시를 재사용해 현재 학생 활동 화면 안에 복원한다. 자동 분석과 학생이 직접 고른 말을 구분하며 원문을 임의 수정하거나 분석 결과를 정답으로 고정하지 않는다.
- 일기/대화 저장 실패 시 입력 보존, 기존 계정별 저장·공개 경계·교사 검토·보상 규칙을 보존한다. 운영 학생 자료 조회/변경이나 DB 이전은 하지 않는다.
- Firebase를 제거한 메모리 검증본에서 실제 조작과 기능/격리 검사를 마친 후보만 정적 파일로 적용·선택 커밋·일반 푸시한다. GitHub Pages 작업과 공개 파일 해시로 배포를 확인한다. 운영 앱 로그인이나 학생 자료 조회를 검증 수단으로 사용하지 않는다.

복원·로컬 검증 완료: 후보 `5927450af326`. 작성 도움·TOP 10·상황·원형/변화 그래프·자동 감정 탐지·행동 예시를 현재 학생 화면에 연결했다. 원문/직접 선택 분리, 실제 저장·실패 시 입력 유지, 기간 필터, 작은 화면 배치를 확인했다. 관련 검증과 한계는 `prototypes/island-world-v2/v4/STUDENT_TOOLS_REVIEW.md`에 기록했다. 게시 완료: 커밋 `81c473d`, GitHub Pages 작업 `35445519106` 성공. 공개 정적 파일 58개 모두 검증 후보와 SHA-256이 일치했다. 운영 앱/DB는 실행하지 않았다.

## 2026-09-19 조개 양옆 물방울 두들 — 사용자 구현 승인

사용자가 ‘물방울 반영해줘’로 직전 제안을 승인했다. 이전 절의 물방울 제안 단계 상태를 이번 구현 승인으로 대체한다. 조개 양옆에 크기가 다른 작은 물방울 세 개를 배치해 서로 다른 타이밍으로 천천히 떠올랐다 사라지게 한다. 흰 윤곽·투명한 하늘색·작은 빛 반사로 바다와 어울리는 두들 느낌을 낸다. 문구 영역과 클릭을 가리지 않으며, 좁은 창에서도 화면 안에 유지한다.

직전 4px 아래 문구 위치·수평 유지·조개 글로우·로그인 연결을 보존한다. 로그인 창/숨긴 탭에서 정지하고 동작 줄이기에서는 새 물방울을 숨긴다. 첫 화면 소스와 로컬 검증만 수정하며 실제 게시·학생 데이터 변경·커밋·푸시·배포는 하지 않는다.

로컬 완료: 후보 `45c5a12ae705`. 1280×720, 390×844, 320×740 브라우저에서 물방울 위치·시간에 따른 이동/투명도·문구와의 비겹침을 확인했다. 로그인 창을 열면 정지하고 닫으면 재개했다. 메모리 격리/기존 기능 보존 검사가 통과했다. 산출물은 `renders/home-cta-bubbles-20260919/`에 있다. 실제 기기·성능은 미확인이며 배포하지 않았다.

## 2026-09-19 조개 안 시작 문구 미세 위치 조정 — 사용자 구현 승인

사용자가 ‘로그인하고 시작하기’를 아주 조금 아래로 내려 조개 가운데에 맞추도록 요청했다. 직전 수평 정렬을 유지하며 버튼 안 문구와 화살표 묶음만 4px 아래로 옮긴다. 조개 크기·위치·수평 모션·로그인 연결과 나머지 화면은 유지한다. 앞선 버튼 내부 여백 결정을 이 범위에서 대체한다. 로컬 검증만 수행하고 게시 파일·학생 자료를 변경하거나 커밋·푸시·배포하지 않는다.

물방울 두들 추가는 사용자가 의견을 물은 제안 단계다. 작은 물방울 두세 개가 천천히 떠올랐다 사라지는 방향을 제안하며 이번 위치 조정에 신규 모션 변경은 포함하지 않는다.

로컬 완료: 후보 `38f16bfb309e`. 실제 브라우저 1280×720·390×844에서 글자/화살표의 여백과 로그인 창 열기/닫기를 확인했다. 기존 메모리 격리 검사를 통과했다. 캡처·조작 기록·수정 전 CSS/manifest는 `renders/home-cta-center-20260919/`에 보관한다. 배포·실제 휴대폰 검증은 하지 않았다.

## 2026-09-19 시작 버튼 수평 정렬·장면 모션 마감 — 사용자 구현 승인

사용자가 ‘로그인하고 시작하기’를 삐뚤지 않게 고치고, 완성도를 높일 모션과 장식을 추가하도록 요청했다. 이전 CTA 글자별 기울기·조개 회전 모션을 이번 결정으로 대체한다.

- 시작 문구의 모든 글자를 동일한 기준선에 수평으로 배치한다. 조개 전체의 회전을 제거하고 수직 부유·같은 비율 확대만 유지한다. 글자 잘림 방지·중앙 정렬·로그인 연결을 보존한다.
- 조개 안에만 스치는 진주빛, 수면의 엇갈린 작은 윤슬, 배 주변에 퍼지는 잔물결을 추가한다. 기존 구름·바다·별·조개 글로우와 조화되게 느린 주기와 짧은 노출로 조절한다.
- 사용자 제공 타이틀·오른쪽 질문·복원된 조개 모양·하단 버튼은 유지한다. 안내 문구/새 기능/새 동선을 추가하지 않는다.
- 모션은 로그인 창·숨겨진 탭에서 정지하고 기기 동작 줄이기 설정에서는 정적인 화면으로 표시한다.
- 첫 화면 소스·로컬 준비/검수 문서 범위만 수정한다. 8779 Firebase 제거 메모리 검증본에서 실제 배치와 모션·로그인 연결을 확인한다. 운영 자료·게시 파일·로그인 이후 구조는 보존한다. 커밋·푸시·배포는 하지 않는다. 실제 휴대폰·학교 크롬북 성능은 미확인이다.

로컬 구현·검수 완료: 후보 `b48fc441c2b0`. 글자별 기울기와 조개 회전을 제거하고, 진주빛·윤슬·배 잔물결을 추가했다. 1280×720, 390×844, 320×740 브라우저 화면에서 수평 정렬·배치와 클릭/Enter 로그인 연결을 확인했다. 로그인 창의 모션 정지/재개와 메모리 격리 검사도 통과했다. 자세한 구분과 산출물은 `prototypes/island-world-v2/v4/home/LEVEL_MOTION_REVIEW.md`에 기록했다. 실제 기기·성능은 미확인이며 배포하지 않았다.

## 2026-09-19 사용자 제공 타이틀·오른쪽 질문·이전 조개 복원 — 사용자 구현 승인

사용자의 세 가지 후속 수정 지시에 따른다. 직전 제목 비율·구름·조개 단계 중 제목 그림과 조개 모양을 이번 결정으로 대체한다.

- 사용자가 제공한 `ChatGPT Image 2026년 9월 19일 오후 08_54_47.png`를 타이틀로 사용한다. ‘감정찰칵! 감정일기를 통한’이 하나의 연결 부제인 제공 파일의 구성을 따른다. 원본을 보존하고 프로젝트에 동일 사본을 넣는다. 화면 배치에서 바깥 투명 여백만 정리한다.
- 오른쪽 빈 공간에 ‘나의 마음 바다에는 어떤 감정이 숨어 있을까?’를 장면 속 구름 질문으로 넣는다. 새 기능/클릭 버튼이 아닌 안내 문구다. 좁은 창에서도 제목·배·시작 버튼을 가리지 않게 배치한다.
- 조개는 입체 PNG 적용 이전의 둥근 산호색 SVG 형태로 복원한다. ‘로그인하고 시작하기’ 그림 글자의 잘림을 수정하고 여유 있는 영역에 온전히 표시한다.
- 기존 조개 부유·통통 반응·확산 글로우, 구름·수면 모션과 하단 일반 텍스트 버튼은 유지한다. 기존 로그인 동선과 로그인 뒤 기능을 보존한다.
- 수정은 첫 화면과 준비/검수 문서만 대상으로 한다. 8779 Firebase 제거 메모리 검증본에서 실제 배치·동작을 확인한다. 학생 데이터·게시 파일은 변경하지 않는다. 커밋·푸시·배포는 하지 않는다. 실제 휴대폰/학교 크롬북은 미확인이다.

로컬 구현·검수 완료: 후보 `16606b123209`. 사용자 타이틀 원본과 동일한 사본, 오른쪽 질문 구름, 이전 둥근 조개를 적용했다. 버튼 글자 윤곽에 맞춘 SVG 영역과 중앙 정렬로 잘림을 수정했다. 실제 화면/조작·코드 검증은 `prototypes/island-world-v2/v4/home/USER_TITLE_REVIEW.md`에 기록했다. 실제 배포와 학교 기기 확인은 수행하지 않았다.

## 2026-09-19 메인화면 제목 비율·구름·조개 모션 재조정 — 사용자 구현 승인

사용자가 현재 제목·요소의 크기 불균형, 하단 그림 글자, 오른쪽 고래를 지적하고 참고 그림의 느낌과 구름·떠다니는 요소·조개 및 글로우 모션을 요청했다. 직전 그림 글자·빛나는 조개 결정 중 다음 내용을 대체한다.

- 참고 이미지는 제목을 한 덩어리로 구성하는 방향으로 사용하며 그대로 정답으로 복제하지 않는다. 네 문구를 하나의 일관된 그림 로고로 다시 만들고 화면·섬과의 크기 비율을 조정한다. 기존 카메라 제거·메인 타이틀 계층은 유지한다.
- 하단 소리·개인정보 안내·저작권 안내는 그림 글자를 적용하기 전의 읽기 쉬운 텍스트 버튼으로 되돌린다. 기존 동작과 터치 영역을 유지한다.
- 오른쪽에 추가한 고래는 제거한다. 배·아이·감정 친구·중앙 섬은 유지한다. 구름을 별도 레이어로 배치하고 흐름·작은 떠다니는 요소를 실제 움직임으로 구현한다.
- ‘로그인하고 시작하기’ 조개 자체에 눈에 보이는 잔잔한 부유/통통 반응을 더하고, 주변 빛도 밝아지고 퍼졌다 돌아오는 모션으로 강화한다. 그림 스타일도 제목/장면과 맞춘다. 로그인 창, 기기 동작 줄이기, 숨긴 탭의 정지 처리는 보존한다.
- 첫 화면과 로컬 준비·검수 문서 범위만 수정한다. 8779 Firebase 제거 메모리 검증본에서 크기·실제 버튼·시간에 따른 모션을 확인한다. 운영 자료·로그인 후 기능·기존 미커밋 변경을 보존한다. 커밋·푸시·배포는 하지 않는다. 실제 휴대폰·학교 크롬북은 미확인이다.

로컬 구현·검수 완료: 후보 `15487bed631a`. 통합 그림 제목, 복원한 하단 텍스트 버튼, 떠다니는 구름과 조개·확산 글로우 모션을 적용했다. 고래는 제거했다. 기록은 `prototypes/island-world-v2/v4/home/BALANCE_REVIEW.md`에 있다. 실제 배포·학교 실기기 확인은 수행하지 않았다.

## 2026-09-19 메인화면 그림 글자·빛나는 조개 — 사용자 구현 승인

사용자의 후속 11개 수정 지시에 따른다. 같은 날 출항 장면 단계의 카메라, 나무 안내판, 상단 로그인/모션 제어, ‘로그인하고 탐험 시작하기’, 일반 글자 표시를 아래 내용으로 대체한다.

- 왼쪽 위와 타이틀의 카메라, 오른쪽 안내판 3개, 상단 로그인·일시정지/재생을 제거한다.
- 주 CTA는 ‘로그인하고 시작하기’로 바꾸고 따뜻한 글로우·hover/press 반응으로 강조한다. 기존 로그인 창을 여는 유일한 첫 화면 진입 버튼이다.
- 네 문구와 ‘마음 바다 탐험대’ 최상위 계층을 유지한다. 제목은 내장 이미지 생성으로 그림 로고를 만들고, 작은 문구·조개 버튼·안내 글자는 또렷하게 확대할 수 있는 그림 글자 자산으로 표시한다. ‘감정일기를 통한’은 크고 대비가 분명한 리본으로 보여준다. 접근 가능한 이름과 이미지 실패 시 읽을 수 있는 문구를 유지한다.
- 기존 바다·섬·배·아이·감정 친구 구도는 유지하며 작은 바다 친구, 반짝임·물방울 등 장식과 간헐적인 은은한 모션을 추가한다. 기기의 동작 줄이기 설정, 로그인 창/숨겨진 탭에서 정지는 유지한다. 별도 모션 버튼은 만들지 않는다.
- 소리·개인정보·저작권은 기존 동작과 48px 이상 조작 높이로 유지한다. 이번 범위는 첫 화면과 준비/검수 문서뿐이다. 로그인 후 구조·학생 자료·공개/저장 규칙·현재 게시본을 변경하지 않는다.
- 8779 Firebase 제거 메모리 검증본에서 실제 컴퓨터 조작과 좁은 창을 확인한다. 커밋·푸시·배포는 수행하지 않는다. 실제 휴대폰·학교 크롬북은 미확인이다.

로컬 구현·검수 완료: 후보 `91d46d44a9c4`. 그림 로고, 강조된 부제 리본, 빛나는 ‘로그인하고 시작하기’, 고래·물방울·반짝임을 반영했다. 카메라·표지판·상단 로그인/재생 제어는 제거했다. 실제 로컬 조작과 코드 검증의 구분은 `prototypes/island-world-v2/v4/home/POLISH_REVIEW.md`에 기록했다. 실제 배포와 학교 기기 확인은 수행하지 않았다.

## 2026-09-19 메인화면 동화 속 출항 인트로 — 사용자 구현 승인

사용자가 제공한 출항 장면 참고 이미지와 ‘다시 리디자인해줘’ 요청에 따라 첫 화면을 구현한다. 직전 전체 바다 단계의 기존 3D 섬 전경 중심 표지를, 아이와 감정 친구가 작은 배를 타고 섬을 향하는 동화풍 인트로로 대체한다. 참고 이미지의 시각 방향을 따르며 화면 문구·기능은 이번 사용자 요청을 기준으로 한다.

- 하늘·구름·잔잔한 바다·중앙 섬을 배경으로, 앞쪽에 배·아이·감정 친구를 배치한다. 내장 이미지 생성 도구로 첫 화면 전용 일러스트 자산을 만들고 로컬 프로젝트에 포함한다. 외부 유료 API나 패키지 설치는 사용하지 않는다.
- ‘초등학생을 위한 / 감정찰칵! / 감정일기를 통한 / 마음 바다 탐험대’를 유지하고 마지막 문구를 가장 큰 메인 제목으로 만든다. 컬러·두께·기울기·작은 항해 장식을 조합하되 실제 텍스트로 표시한다.
- ‘나의 감정 찾기 / 마음 기록하기 / 더 좋은 나에게’를 장면 속 나무 안내판으로 표현한다. 새 기능 버튼으로 오해하지 않도록 비대화형 안내로 구현한다.
- 메인 CTA는 ‘탐험 시작하기’를 크게 표시하고 작은 ‘로그인하고’ 문구로 진입을 설명한다. 상단 로그인과 동일한 기존 로그인 창을 연다. 체험 CTA를 되살리지 않는다.
- 바다·배·구름의 작은 모션과 CTA의 눌림 반응을 적용하고, 동작 줄이기/모션 정지/로그인 뒤 정지를 보존한다. 소리·개인정보·저작권 버튼을 48px 이상 조작 영역으로 유지한다.
- 첫 화면과 `prototypes/island-world-v2/v4/home/` 준비 코드·문서에 한정한다. 로그인 후 기능·학생 데이터·공개/공감/저장 규칙은 보존한다. 8779 Firebase 제거 메모리 검증본에서 컴퓨터와 좁은 창을 확인한다. 커밋·푸시·배포는 이번 승인에 포함하지 않는다. 실제 휴대폰·크롬북은 미확인이다.

로컬 구현·검수 완료: 후보 `855cabea3919`. 배·아이·감정 친구의 출항 장면, 컬러 로고, 나무 안내판, 조개 CTA와 기존 로그인 연결을 적용했다. 실제 컴퓨터/좁은 창 검증은 `prototypes/island-world-v2/v4/home/VOYAGE_REVIEW.md`에 기록했다. 실제 배포·학교 기기 확인은 수행하지 않았다.

## 2026-09-19 메인화면 전체 바다 탐험 장면 — 사용자 구현 승인

사용자가 확정한 전체 화면형 탐험 시작 화면을 구현한다. 같은 날 앞선 브랜드 톤 단계의 왼쪽 텍스트/오른쪽 섬 배치와 ‘감정찰칵!’을 가장 크게 두던 위계를 이번 결정으로 대체한다.

- 화면 전체에 하나의 바다와 섬을 펼치고 중앙 타이틀·섬·부두·시작 버튼을 연결한다. 기존 섬의 탐험 공간을 유지하며 첫 화면의 구도·색·장식만 조정한다.
- ‘초등학생을 위한’은 작은 대상 문구, ‘감정찰칵!’은 카메라와 함께 브랜드 표식, ‘감정일기를 통한’은 연결 부제, ‘마음 바다 탐험대’는 가장 크고 눈에 띄는 메인 타이틀로 표시한다.
- 주 CTA는 ‘탐험 시작하기’ 하나다. 우측 상단 로그인과 동일한 기존 로그인 화면을 연다. 체험하기는 완전히 제거한 상태를 유지한다.
- 둥글고 눌리는 버튼, 따뜻한 민트·크림·산호·노랑의 균형, 잔잔한 바다·작은 식생/배 움직임·한 번의 타이틀 등장·hover/press 반응을 적용한다. 동작 줄이기·정지 기능을 보존한다.
- 소리·개인정보 안내·저작권 안내는 하단/모서리에 읽기 쉬운 글자와 48px 이상 터치 높이로 정리한다. 좁은 창에서도 전체 장면의 중심 구조를 유지한다.
- 구현은 `prototypes/island-world-v2/v4/home/`와 승인/검수 문서에 한정한다. 로그인 후 구조·저장·공개/공감 규칙·기존 미커밋 작업을 보존한다. Firebase가 제거된 8779 메모리 검증본에서 화면과 실제 버튼을 확인한다. 커밋·푸시·배포는 이번 승인에 포함하지 않는다. 실제 휴대폰·크롬북은 미확인이다.

로컬 구현·검수 완료: 후보 `6d8742272e8f`. 전체 장면·타이틀 계층·모션·로그인 연결과 컴퓨터/좁은 창 검증을 `prototypes/island-world-v2/v4/home/SCENE_REVIEW.md`에 기록했다. 실제 배포와 학교 기기 검증은 수행하지 않았다.

## 2026-09-19 메인화면 브랜드 톤 재디자인 — 사용자 구현 승인

사용자의 ‘조금 더 통통 튀고 귀엽고 기억에 남는 느낌’, ‘메인화면의 톤 자체를 한 단계 더 업그레이드’ 요청에 따른 로컬 구현이다. 직전 단순화 단계의 차분한 제목·큰 하단 안내 상자를 이번 표현으로 대체한다. 텍스트 최소화와 로그인 연결은 유지한다.

- ‘초등학생을 위한’, ‘감정찰칵!’, ‘감정일기를 통한 마음바다 탐험대’를 유지한다. 색·크림색 윤곽·두께·글자별 기울기와 카메라 심벌을 함께 사용해 첫 제목을 하나의 브랜드로 구성한다.
- 기존 섬을 유지하면서 첫 화면 전용 조명·민트 바다·식생 색을 다듬는다. 작은 감정 표정 장식을 더하되 학습 결과·학생 기록으로 표시하지 않는다.
- 타이틀의 짧은 등장, 잔잔한 물결, 나뭇잎·배·시점의 미세한 움직임, 버튼 hover/press 반응을 적용한다. 동작 줄이기 설정과 화면의 움직임 멈추기를 지원하며 로그인 창 뒤에서는 정지한다.
- 하단은 소리·개인정보·저작권의 개별 버튼으로 가볍게 나누고, 48px 터치 높이를 유지한다. 상단 로그인과 ‘탐험 시작하기’ 모두 기존 로그인 창을 연다. 체험 CTA는 추가하지 않는다.
- `prototypes/island-world-v2/v4/home/` 범위에서 수정한다. 운영과 분리된 8779 검증본, 컴퓨터·좁은 창에서 실제 표현·모션·버튼을 확인한다. 기존 미커밋 변경·데이터·로그인 이후 섬을 보존하며 커밋·푸시·배포는 수행하지 않는다. 실제 학교 기기는 미확인이다.

## 2026-09-19 메인화면 단순화 — 사용자 구현 승인

사용자의 열 가지 메인화면 수정 요청에 따라 첫 화면의 소개형 구성을 간결한 섬 중심 화면으로 대체한다. 이전 첫 화면의 소개·감성 문구와 로그인 없는 체험 CTA를 이번 범위에서 갱신한다.

- 보조 문구는 ‘초등학생을 위한’ 하나만 사용한다. 제목은 ‘감정찰칵! / 감정일기를 통한 / 마음바다 탐험대’의 세 줄로 표시한다.
- 긴 소개, 체험 보조 문구, 하단 감성 문구, 우측 하단 설명, 중복 브랜드 문구를 제거한다. 시작 CTA ‘탐험 시작하기’와 우측 상단 ‘로그인’은 모두 기존 로그인 창을 연다.
- 기존 섬 비주얼이 주인공이 되도록 제목 크기·여백·흐림 범위를 조정하고, 더 둥근 주아 서체와 따뜻한 색을 사용한다. Google Fonts 공식 무료 서체를 필요한 글자만 담은 로컬 사본과 라이선스로 포함해 외부 글꼴 연결 없이 표시한다. 작은 카메라 심벌로 ‘찰칵’ 이미지를 더한다.
- 소리·개인정보·저작권 안내의 기존 동작과 내용을 유지하고, 안내 글자와 버튼의 터치 영역을 키운다. 좁은 화면에서도 제목·섬·CTA·안내를 구분한다.
- `prototypes/island-world-v2/v4/home/`에서 구현하고 최신 게시본에 첫 화면만 적용한 후보와 Firebase가 제거된 메모리 검증본을 만든다. 기존 미커밋 변경·로그인 처리·학생 데이터·실제 게시 파일은 보존한다. 이번 요청은 커밋·푸시·배포 승인이 아니다.
- 컴퓨터 실제 조작과 좁은 데스크톱 화면을 확인한다. 실제 휴대폰·크롬북은 별도 미확인으로 남긴다.

## 2026-09-19 V4 후속 게시 승인 — 학생 활동·소품 꾸미기

사용자의 “오케이 배포해줘”에 따라 직전까지 로컬 검수한 학생 활동 화면과 소품 6종·정원 편집을 기존 GitHub Pages에 게시한다. 위 기능의 앞선 로컬 전용 및 커밋·푸시·배포 보류를 이번 게시에 한해 갱신한다.

- 기존 계정별 `_mindIslandV4` 저장 경로와 트랜잭션에 새 배치 필드를 연결한다. 이전 저장본·Q01 1회 보상·공개 경계를 보존하고 기존 기록을 이전하거나 삭제하지 않는다.
- 버전별 정적 파일과 필요한 구현/배포 연결 소스만 선택적으로 커밋하고 기존 origin/main에 일반 푸시한다. 원화 원본·검수용 가상 자료·로컬 캡처·무관한 기존 수정은 제외한다.
- 게시 후보에서 Firebase를 제거한 메모리 검증본으로 소품 저장/재진입·계정 분리·충돌/실패·활동 화면을 확인한다. 기존 정적 파일과 index 복구본을 보관한다.
- GitHub Pages의 대상 커밋 완료와 공개 정적 파일의 해시 일치로 게시를 확인한다. 운영 앱 로그인·학생 DB 접속/변경·시연 재생성·규칙 수정은 이번 승인에 포함하지 않는다. 실제 휴대폰·학교 크롬북은 미확인이다.

게시 완료: `5a431bdfbbd9170c3d7231b8d69885295205b4f8`를 기존 main에 일반 푸시했다. GitHub Pages 작업 `35416975186`이 성공했고 공개 HTML·새 자산 총 46개 SHA-256이 버전 `cc85b475f8e3` 검수본과 일치했다. 운영 앱·학생 DB는 실행/조회하지 않았다. 기록은 `prototypes/island-world-v2/v4/release/DECOR_DEPLOYMENT.md`, 최종 증거는 로컬 `renders/releases/mind-island-v4-decor-20260919/deployment-result.json`에 있다.

## 2026-09-19 V4 후속 승인 — 소품 추가와 꾸미기 확장

사용자의 “소품 추가와 꾸미기 확장 이걸로 가자” 및 “꾸미기 구역 안에서 위치·방향을 직접 바꾸기” 선택에 따른 로컬 구현이다. 이전 D03의 자유 배치 보류를 제한된 정원 안의 위치·회전 편집으로 갱신한다.

- 제공받은 꽃 2종·돌등·이끼 바위·갈대·목재 벤치의 원본을 보존하고 가벼운 텍스처 사본을 적용한다. 기본 소품은 각 1개씩 즉시 꾸밀 수 있게 하며, Q01의 기존 1회 선택 선물은 별도로 유지한다. 새 점수·과금·반복 보상은 추가하지 않는다.
- 정원 안에서 바닥 선택·미세 이동·회전으로 미리 본 뒤 저장한다. 취소 시 기존 배치, 보관 시 소품 소유를 보존한다. 길·활동 장소·다른 소품과 겹치는 배치를 막는다.
- 기존 Q01 상태와 보상을 유지하면서 학생별 로컬 상태에 선택적 배치 필드를 추가한다. 이전 저장본은 강제 이전 없이 읽으며 기존 준비된 자리의 소품은 편집 전까지 그대로 둔다.
- 꾸미기는 내 섬에만 적용하며 기록으로 자라는 풍경·친구 공개·공감·게시판·항해 규칙은 보존한다. 기존 학생 활동 화면의 미커밋 작업도 보존한다.
- 검증은 Firebase가 제거된 8775 메모리 하네스에서 한다. 운영 접속·자료 변경·새 모델 생성·패키지 설치·커밋·푸시·배포는 수행하지 않는다. 실제 휴대폰·크롬북은 확인 전까지 미확인이다.

구현·검수 결과는 `prototypes/island-world-v2/v4/review/DECORATION_REVIEW.md`에 기록했다. 기본 소품 6종·정원 안의 위치/회전·보관/취소·실패 처리·기존 Q01 선물 연결을 로컬에서 확인했다. 컴퓨터 1366×768 및 좁은 데스크톱 창 390×844 조작과 별도 16초 성능 측정을 마쳤다. 최종 화면 승인·실제 학교 기기·운영 저장·게시 완료와는 구분한다.

## 2026-09-19 V4 후속 승인 — 학생 활동 화면

사용자의 “오케이 그러면 렛츠고”에 따라 직전에 제안한 마음 대화·연습·달력·리포트의 화면 완성도와 활동 흐름 개선을 로컬에서 진행한다. 이전의 해당 활동을 기존 화면 연결로 남긴 범위를 이번 단계에서 갱신한다.

- 내 섬의 장소별 단일 활동과 빛 표시를 유지한다. 학생이 고른 기록을 중심으로 대화·달력·리포트를 연결하고, 이동이 어려운 경우 장소 메뉴에서 같은 활동을 바로 열 수 있게 한다.
- 대화는 그때의 마음·중요했던 것·필요한 도움/다음 행동을 선택적으로 살펴본다. 모름·중단을 허용하며 기존 대화 기록 형식을 보존한다. 개인 일기와 가상 이야기, 자동으로 찾은 마음 말과 학생 선택을 구분한다.
- 연습은 기존 게임을 유지하면서 마음 말의 뜻을 천천히 비교할 수 있는 입구를 제공한다. 새 탐험 보상·점수 정책은 추가하지 않는다.
- 일기 모아보기의 달력·원문 수정/삭제·기간별 리포트를 같은 화면 톤으로 다듬는다. 날짜와 실제 기록에 근거하며 표현 수를 능력 향상 판정으로 쓰지 않는다.
- 안전한 8775 메모리 하네스에서 실제 컴퓨터 화면·좁은 화면·입력·저장 실패·기록 연결을 검수한다. 운영 Firebase·학생 자료·기존 게시 파일·원본 모델·패키지 설치·커밋·푸시·배포는 이번 범위가 아니다. 학교 실기기는 미확인으로 남긴다.

구현·검수 결과는 `prototypes/island-world-v2/v4/review/ACTIVITY_REVIEW.md`에 기록한다. 컴퓨터 및 좁은 데스크톱 화면에서 확인한 로컬 결과이며 최종 디자인 승인·학교 실기기 확인·게시 완료와 구분한다.

## 2026-09-19 V4 실제 게시 승인

사용자의 “오케이 배포해줘”에 따라 검수한 V4 내 마음섬·우리 반 바다·친구 섬을 기존 GitHub Pages 사이트에 게시한다. 이전 V4 절의 로컬 전용/커밋·푸시·배포 보류를 이번 배포에 한해 대체한다.

- 현재 화면과 승인된 소품 사본을 버전별 정적 자산으로 묶고 기존 로그인·일기·공개/공감·게시판·교사 기능을 보존한다. 게시에 필요한 선택적 커밋과 기존 main으로의 일반 푸시를 허용한다.
- 로컬 전용 Q01 진행도·꾸미기는 로그인한 학생의 기존 일기 소유 경로 아래 별도 메타데이터에 연결한다. 원문/학습 응답/학생 선택을 구분하고, 체험·시연 계정의 쓰기는 세션 메모리로 유지한다. 기존 학생 기록을 이전·삭제하거나 공개 범위를 넓히지 않는다.
- 배포 후보에서 Firebase를 제거한 메모리 검증본으로 로그인 연결·계정 분리·새로고침에 해당하는 상태 복원·보상 중복·실패 처리·기존 기능을 검사한다. 실제 학생 자료 조회/수정, 시연 학급 재생성, DB 규칙 변경, 패키지 설치는 수행하지 않는다.
- 미커밋 개발 자료와 원본 모델을 보존한다. 원화 원본·검수용 가상 자료·로컬 캡처·무관한 문서 변경은 게시 커밋에 섞지 않는다. 이전 index와 정적 파일 복구본을 남긴다.
- 게시 성공은 GitHub Pages의 해당 커밋 완료와 공개 정적 파일 해시로 확인한다. 운영 앱 로그인/DB 동작과 실제 학교 기기는 별도 미확인 사항으로 남긴다.

게시 결과: `e92499fe29841a31012e64b52b5170308a4161fa`를 기존 main에 일반 푸시했고 GitHub Pages 작업 `35368947779`가 성공했다. 공개 사이트의 index와 신규 실행 자산 총 33개 SHA-256이 검수본과 일치했다. 운영 스크립트 실행·로그인·학생 DB 조회는 하지 않았다. 증거는 `renders/releases/mind-island-v4-20260919/deployment-result.json`에 보관한다.

## 2026-09-19 V4 후속 승인 — 친구 섬 산책 화면

사용자의 “좋아 다음으로 고고”에 따라, 직전에 안내한 친구 섬 내부의 시각 톤 개선을 로컬에서 진행한다. 이전 바다 지도 단계에서 보류한 친구 섬 내부 표현을 이번 범위로 갱신한다.

- 친구 섬의 연속 지형·다리·이동·감정별 식생 생성 위치와 조건은 보존한다. 조명·색·배경 수목·목재·바다 표현과 UI를 내 마음섬 톤으로 맞춘다.
- 승인된 나무·게시판 사본을 기존 Three r147 정적 로더 캐시로 재사용한다. 신규 모델 생성·원본 수정·유료 API 호출은 포함하지 않는다.
- 공개 마음 말과 뜻, 공감 조건·중복 방지, 게시판 공개 조건, 친구 개인 공간 잠금, 부두 승선·복귀를 보존한다. 구역 이름을 다시 붙이지 않는다.
- 목적지 표시를 빛으로 보여 주되 직접 걷기·바닥 클릭 이동·360도 회전과 기존 승선 기능은 유지한다.
- 8775 메모리 하네스에서 실제 화면과 동작·성능을 검수한다. 운영 자료·시연 재생성·패키지 설치·커밋·푸시·배포는 수행하지 않는다.

## 2026-09-19 V4 후속 승인 — 우리 반 바다 지도

사용자의 “오케이 일단 이정도 하고 다음으로”에 따라, 앞서 안내한 다음 단계인 우리 반 바다의 로컬 시각 개선을 진행한다. 이전 절의 바다 지도 보류 범위를 이 단계에 한해 대체한다.

- 내 섬의 현재 구현을 유지하고 바다 색·조명·섬 이름표·선택 표시·출항 카드를 같은 톤으로 맞춘다.
- 바다 진입 시 학급 섬을 함께 보여 주며 친구 선택 시 가까이 보여 준다. 내 섬의 지도 모형은 현재 V4 배치와 승인된 소품 사본을 따른다.
- 안정된 섬 좌표, 공개 상태 조회, 추천·항해·방문·복귀·공감 처리는 보존한다. 친구 섬의 실제 걷기 화면과 데이터 구조는 이번에 재작성하지 않는다.
- 검증된 8775 메모리 하네스에서만 구현·확인한다. 운영 데이터·새 모델·패키지 설치·커밋·푸시·배포 승인은 아니다.

## 2026-09-18 V4 후속 사용자 수정 승인 — 장소별 활동과 빛 표시

사용자의 “길안내보다는 어디로 가야하는지 빛났으면”, 책·망원경 확대와 주변 개선, 섬이 작다는 의견, 장소별 단일 활동, 획득 연출·모션 요청에 따른 로컬 구현 승인이다.

- 이전 V4의 목적지 자동 걷기 안내와 장소 안의 공통 5개 탭을 대체한다. 목적지에 은은한 빛을 표시하며, 직접 걷기와 바닥 클릭 이동은 유지한다.
- 책상은 마음 쓰기, 관찰대는 마음 살펴보기만 연다. 저장 후 관찰대를 밝히고 섬으로 돌아오며 다른 활동을 같은 장소에서 열지 않는다.
- 섬 외곽과 산책 공간을 넓히고 책·망원경의 크기와 주변 조경을 다듬는다. 네 풍경의 색과 감정·공개·학습 규칙은 유지한다.
- Q01 지급 조건과 한 번 선택하는 소품 수는 유지한다. 저장 성공 후 획득 카드, 실제 배치 소품과 같은 3D 미리보기, 부드러운 등장과 빛 표시를 추가한다. 동작 줄이기 설정을 존중한다.
- 이번 범위는 내 섬 피드백 구현이다. 전체 바다 지도도 같은 톤으로 맞출 후속 대상이나 이번 변경에서 지도·친구 섬을 재작성하지 않는다.
- 원본 GLB, 운영 Firebase·학생 자료, 패키지 설치, 커밋·푸시·배포는 승인 범위에 포함하지 않는다. 로컬 8775 메모리 하네스에서 확인한다. 화면 완성도와 실제 학교 기기 성능은 별도 검토한다.

# 마음바다탐험대 — 3D 섬 기능 설계서

## 최신 승인 — 2026-09-18 (V4 1차 로컬 시제품)

사용자가 P0 보고서와 D01~D03 권장안을 확인한 뒤 “승인 고고”라고 승인했다. 승인 범위는 운영과 분리된 로컬 1차 시제품이다.

- D01: 탐험 하나씩 추적, 다음에 하기/이어하기, 강제 순서 없음.
- D02: Q01 응답 저장과 피드백 확인 후 선택권 1회. 자기 경험/가상 이야기 동등, 같은 Q01 중복 지급 없음.
- D03: 꽃·등불·관목 중 하나 선택, 준비된 자리 또는 보관. 자유 배치 편집은 후속.
- 내 섬 V4 경로에서 중앙 마당·대표 나무·책상·관찰대와 Q01 전체 흐름을 구현한다. 최신 플레이어/카메라/친구 방문/공개 경계/게시판 기능은 보존한다.
- V3.1의 감정별 식생 고정 규칙 유지와 달리 V4 내 섬에서는 풍경·감정 경험·학습·꾸미기를 분리한다. 기존 기록과 친구 섬 규칙을 자동 이전하거나 삭제하지 않는다. 원문/자동 분석/학생 선택을 구분한다.
- 고밀도 책상 원본(약 185만 삼각형)은 직접 적용하지 않는다. 가벼운 3D 기본형으로 기능을 유지한다. 기존 나무/게시판 사본 재사용, 책/망원경의 비파괴 1K 사본·실제 렌더 검수를 허용한다.
- 실제 화면 품질은 결과로 검토하며 구현 승인과 최종 디자인 승인을 구분한다.
- 운영 Firebase·학생 자료·시연 재생성·패키지 설치·유료 서비스·커밋·푸시·배포는 승인하지 않았다.

## 최신 추가 원화 승인 — 2026-09-18 (V4 2차 9장)

사용자의 “오케이 2차도 같이” 요청에 따라 앞서 제시한 2차 후보 9종(목재 벤치·기록 보관함·조개·빈 게시판·탐험배·흰 꽃·라벤더풍 꽃·갈대·이끼 바위)을 각각 한 장씩 생성한다. 최신 1차 수량 원칙인 종류별 한 장을 이어받으며 기존 6장은 보존한다.

- 1차와 같은 따뜻한 무광 토이 질감·단독 소품·3/4 시점·아이보리 배경·글자 없음 기준을 유지한다.
- `tripo/concepts/v4_phase2/`에 개별 PNG, 프롬프트, 출처·실측 기록을 보관한다. 2차 9장 ZIP과 1·2차 합본을 별도로 제공한다.
- 앞선 2차 생성 대기 상태를 원화 제작 승인으로 갱신한다. Tripo 업로드·GLB 생성·앱 적용·중단된 게시판 검증 재개를 포함하지 않는다.

결과: 2차 개별 PNG 9장 생성 및 육안 검수, 모두 1,254×1,254. 원본 바이트 동일 보관과 PNG 무결성 검사 완료. 2차 9장 및 전체 15장 ZIP 구성 확인. 1차 원화·기존 앱·게시판/GLB를 포함한 보호 파일 38개 해시 유지 확인.

## 최신 수량 변경 승인 — 2026-09-18 (V4 원화 6장)

사용자가 “이미지 생성 해줘 각 1개씩만 총 6장 만들자”라고 수정했다. 직전 V4 12장 계획의 수량을 대체하여 6종 각각 단정한 A안 하나만 생성한다. B안 및 2차 후보는 생성하지 않는다. 그 밖의 스타일·단독 소품·개별 파일·기존 앱/게시판/GLB 보존 범위는 유지한다.

결과: 내장 image_gen으로 개별 PNG 6장 생성, 모두 1,254×1,254. `tripo/concepts/v4/`에 원본 동일 복사 및 프롬프트·출처·검수 기록 보관. 원화 이외 기존 앱·게시판·GLB 보호 대상 26개 파일의 해시가 유지됨을 확인했다.

## 최신 원화 제작 승인 — 2026-09-18 (V4 Tripo 입력용 1차 12장)

사용자가 V4 에셋 원화 요청서를 직접 제시했다. 작은 목재 책상·펼친 책·받침대 망원경·둥근 꽃·낮은 정원 등불·둥근 관목을 각각 A/B 두 안으로 생성한다.

- 각 PNG는 단독 소품 하나, 약간 위에서 본 3/4 시점, 정사각형, 연한 단색 배경, 무광·둥근 토이 질감으로 제작한다. 그림 안에 글자·캐릭터·풍경·UI를 넣지 않는다.
- 이번 승인은 1차 원화 12장과 정리 문서에 한정한다. 2차 후보는 1차 톤 검토 뒤 진행할 대기 목록이다. GLB 생성·업로드·앱 적용 승인이 아니다.
- 기존 V3.1 원화와 GLB를 덮어쓰지 않고 `prototypes/island-world-v2/tripo/concepts/v4/`에 새로 보관한다. 생성 프롬프트·실측 크기·출처·파일 해시를 기록한다.
- 중단된 게시판 시험 적용은 현 상태로 보존하며 이번 원화 제작과 섞지 않는다. 앱 기능·감정/공개 규칙·운영 데이터·배포를 수정하지 않는다.
- V4 명칭은 이번 에셋 원화 기준에 적용한다. 이 승인만으로 기존 제품 기능 설계를 새 버전으로 대체하지 않는다.

## 최신 후속 승인 — 2026-09-18 (Tripo 게시판 한 개 로컬 시험)

사용자는 검수한 소품 외형을 마음에 들어 했으며, 게시판 한 개부터 로컬 섬에 시험 적용하자는 제안에 “오케이 하나하나씩 해보자!!!”라고 승인했다. 우선 이번 단계는 게시판 한 개에 한정한다.

- `wooden signboard 3d model.glb` 원본을 보존하고 1K 텍스처 사본을 만들어 기존 내 섬 게시판 위치의 외형만 비교한다. 게시판 제목·본문 기능·공개/익명/교사 검토 규칙·이동 충돌과 V3.1 구역명 미표시는 유지한다.
- 기존 Firebase 제거/메모리 DB의 V3.1 검증본에서 파생한 별도 로컬 시험만 실행한다. 운영 index·자산·Firebase·학생 자료·배포는 변경하지 않는다.
- 앱과 같은 카메라·조명으로 적용 전후, 옆/뒤·지면 접촉·그림자, 로딩 실패/지연 시 기존 소품 복구, 실제 사용 수 한 개와 재진입 안정성을 확인한다. 성능은 실제 측정값과 미확인 기기를 구분한다.
- 새 나무·배·꽃·벤치 등 다른 소품을 이번 변경에 섞지 않는다. 기존 승인된 나무 시험본은 보존한다. 이번 승인은 앞선 수령/검수만의 범위를 게시판 한 개의 최적화와 로컬 시험에 한해 확장한다.

## 후속 오류 수정 — 2026-09-18 (검수 화면 직접 열기)

사용자가 검수 화면의 `TypeError: Failed to fetch`를 보고했다. GLB 검수 화면만 수정한다. 파일로 직접 연 경우 저장 요청을 생략하고 화면 미리보기를 제공하며, 로컬 저장 실패를 모델 로딩 실패와 분리한다. 원본 GLB·앱·이전 적용 승인 범위는 그대로 유지한다.

## 후속 수령·검수 기록 — 2026-09-18 (Tripo GLB 13종)

사용자가 13개 GLB를 전달했다. 기존에 정한 제작·검수 절차에 따라 파일 보관과 검수를 진행하며, 이번 파일 전달을 새 소품의 일괄 앱 적용 승인으로 해석하지 않는다.

- 원본 GLB를 `prototypes/island-world-v2/tripo/intake/2026-09-18/source/`에 동일 바이트로 보존했다. 정적 검사와 Three.js r147의 8방향 실제 렌더 검수만 진행했다.
- 기존 나무 시험본·로더·원화·앱 코드·감정/어휘/공감/공개 규칙·운영 자료는 유지한다. 새 cartoon tree는 이전에 승인한 tree-round-01과 다른 후보이며 자동 교체하지 않는다.
- 14종 원화 중 낮고 둥근 관목 한 종은 이번 수령 목록에 없다. 열매나무의 앞면에 몰린 열매는 외형 보완 후보로 기록한다.
- 검수 결과와 최적화 목표는 `tripo/intake/2026-09-18/REVIEW.md`에 구분한다. 최적화·실제 섬 적용·배포 승인은 확대하지 않는다.

## 최신 후속 승인 — 2026-09-18 (추가 소품 원화 5장)

사용자 요청: “오케이 좀 뽑아 줄래? 조그만 더 뽑아주면 돼 원화로”. 앞서 제안한 분홍 꽃나무·열매나무·낮고 둥근 관목·넓은 잎 풀·작은 목재 벤치의 원화 제작을 진행한다.

- 기존 9종을 보존하고 위 5종만 추가해 총 14종의 이미지 원화를 준비한다. 아래의 9종 제작 범위는 원화 수에 한해 확장하며 그 밖의 앱/모델 적용 범위는 유지한다.
- 내장 image_gen으로 개별 투명 PNG를 만든다. 둥근 무광 질감·따뜻한 목재·차분한 초록색을 공유하고, 글자·구역명·별칭·지면·주변 풍경을 넣지 않는다.
- 관목·풀·벤치는 배경 장식 후보다. 기록으로 자라는 식생·감정 분류·생성 조건·어휘·공감·공개 규칙은 변경하지 않는다.
- 저장 위치: `prototypes/island-world-v2/tripo/concepts/v3_1_additions/`. 원본·프롬프트·생성일·사용 조건 기록·실측 검사값을 보관한다.
- 원화 제작 승인과 최종 외형/GLB 제작·교체 승인은 구분한다. 이번에는 이미지와 제작 기록만 추가하고 앱 코드·기존 모델·운영 데이터·배포는 변경하지 않는다.

## 최신 후속 승인 — 2026-09-18 (Tripo 입력 원화 일괄 제작)

사용자 요청: “tripo로 뽑아야 하는 그림 이미지를 전부 만들어 줄 수 있어?”에 따라 V3.1 별도 에셋 목록의 이미지 원화를 준비한다.

- 목록 9종 중 기존 초록 나무 원화는 보존·재사용하고, 게시판·작은 배·빈 팻말·따뜻한 꽃·흰 수변 꽃·갈대·돌 등불·이끼 바위 8종은 내장 image_gen으로 각각 독립 PNG 한 장씩 제작한다.
- 이번 요청은 여러 **이미지 원화**의 일괄 제작 승인이다. 기존의 한 소품씩 확인하는 Tripo GLB 생성·검수·적용 절차나 앱 에셋 교체 승인을 확대하지 않는다.
- 둥글고 부드러운 무광 형태, 따뜻한 목재와 차분한 초록색을 공유한다. 한 이미지에 하나의 소품만 넣으며 투명 배경·글자 없는 빈 표면을 사용한다. 특정 참고 게임 소품을 그대로 복제하지 않는다.
- 구역명 팻말·A/B/C/D·감정 구역 별칭·학생 이름·게시판 글자는 이미지에 넣지 않는다. 캐릭터·섬 전체·지형·부두·다리·UI·음향 제작은 이번 이미지 목록에 추가하지 않는다.
- 저장 위치: `prototypes/island-world-v2/tripo/concepts/v3_1/`. 생성 원본과 프롬프트, 날짜·파일 검사값·수정 내역을 기록하고 Tripo 제작 문서에는 원화와 GLB 완료 여부를 나눠 적는다.
- 앱 코드·기존 GLB·운영 Firebase·학생 자료·커밋·푸시·배포는 변경하지 않는다. 이번 원화의 최종 외형 승인을 받은 것으로 표시하지 않는다.

## 최신 후속 승인 — 2026-09-18 (V3.1 구역 이름 미표시만 적용)

사용자 요청: “추가 수정: 구역 이름만 제거해줘.” 현재 에셋을 제작 중이며, 이번 수정에 새 모델 생성·에셋 교체를 섞지 않도록 명시했다.

- 최신 기획 기준은 [FINAL_REDESIGN_V3_1.md](prototypes/island-world-v2/v3_1/FINAL_REDESIGN_V3_1.md)로 갱신한다. Tripo 제작은 [TRIPO_ASSETS_V3_1.md](prototypes/island-world-v2/v3_1/TRIPO_ASSETS_V3_1.md)에서 별도 관리한다.
- 구역명 팻말·말풍선·지도 라벨·범례·튜토리얼의 구역 이름을 표시하지 않으며 새 별칭/A·B·C·D로 대체하지 않는다. 실제 마음 말과 뜻, 기능/장소 이름, 친구 섬 이름, 작성자 표시를 보존한다.
- 네 영역의 내부 식별자·색·풍경·배치·감정 분류·식생 생성·공감·공개 규칙을 보존한다. 나침반의 긍정/부정·에너지 높음/낮음 축 사양은 유지한다.
- 이번 구현은 표시 제거만이다. 첨부 CODEX_START.txt의 과거 A단계 실행 지시를 이번 전체 리디자인 승인으로 확대하지 않는다. 최신 로컬 코드에 없는 V3 기능은 구현 완료로 표시하지 않는다.
- V3.1은 과거의 구역명 표시 기획을 대체한다. 그 밖의 V3 요구는 누적 기획으로 보존하며, 이번에 작성자 공개 정책·지형 재배치·축 도형·속도·에셋을 변경하지 않는다. 현재 옛 축 캡션(기운/긴장)과 기존 익명 게시판은 아직 V3 구현 전 상태다.
- 기존 Tripo 나무 시험본을 기반으로 Firebase 제거/메모리 DB 검증본에서만 적용한다. 원본 GLB·웹용 GLB·기존 Tripo 로더와 최적화 결과는 그대로 둔다.
- 운영 Firebase·학생 자료·운영 index/자산·커밋·푸시·배포는 이번 범위가 아니다. 실제 학교 기기 확인 여부는 별도 기록한다.

## 최신 후속 승인 — 2026-09-18 (Tripo 나무 로컬 시험 적용)

사용자는 새 `tree 3d model.glb` 검수 결과를 확인한 뒤, “로컬 섬에 한 그루를 적용한 뒤 11그루 반복 배치와 적용 전후 비교까지 진행할까요?”에 **“좋은데 한번해줘”**라고 승인했다.

- 사용자 원본을 보존하고, 복사본의 텍스처를 2K에서 1K로 줄여 비교한다. 현재 4,666개 삼각형은 우선 유지한다.
- 일반형 배경 나무 한 그루와 같은 유형 11그루에 한정한다. 버드나무·야자수·기록에서 자라는 식물, 감정별 조건·어휘·공감·공개 범위, 이동 충돌·지형·카메라 동작은 유지한다.
- 현재 Three.js r147과 호환되는 로컬 GLTFLoader를 사용한다. 모델 로딩 실패·지연 시 기존 나무를 표시한다. Tripo API·키는 앱에 넣지 않는다.
- 운영 파일과 배포용 자산을 수정하지 않고 `prototypes/island-world-v2/tripo/` 및 별도 Firebase 제거 메모리 검증본에서만 시험한다. 기존 8766 생성기와 release/prepare.py를 운영 index 대상으로 재실행하지 않는다.
- 같은 시점·조명에서 적용 전/한 그루/11그루를 촬영하고 로컬 PC의 프레임 간격·렌더링 수치를 실제로 측정한다. 실제 크롬북·터치 기기 성능은 미확인으로 남긴다.
- 생성 출처·날짜·사용 조건·수정 내역을 기록하되 확인하지 못한 항목을 추정하지 않는다. 이번 승인은 운영 Firebase·학생 자료·커밋·푸시·배포 승인이 아니다.

이 절은 2026-09-17 탐험가 단계의 외부 모델 미사용 범위를 **이번 로컬 나무 시험에 한해** 갱신한다. 배·게시판·캐릭터 교체와 최종 배포는 승인 범위에 포함하지 않는다.

## 최신 후속 승인 — 2026-09-17 (실제 배포·커밋·푸시)

검증한 후보를 마음바다탐험대.kr에 반영하기 위해 필요한 커밋·푸시까지 진행할지 확인했고, 사용자가 **“커밋 푸시 고고”**라고 승인했다. 이번 V2 후보 502991c9f22e의 운영 파일 적용, 커밋, origin의 작업 브랜치와 main 푸시, 기존 GitHub Pages 게시를 허용한다. 이전의 커밋·푸시·배포 금지는 **이번 검증한 후보의 게시에 한해** 이 승인으로 대체한다.

- 기존 미커밋 문서 수정은 보존하고, 이번 새 디자인과 관련 문서만 커밋한다. character-rework stash를 복원하지 않는다.
- 학습 저장 구조와 공개 범위는 유지한다. 운영 Firebase 조회·학생 자료 수정·시연 학급 생성은 이번 승인에 포함되지 않는다.
- 기존 버전 347e610e6af3ad94f19c41834932ac3731428e04와 이전 정적 화면 복구본을 기준으로 복귀할 수 있게 한다. 파일 교체·재게시와 학생 DB 복원을 구분한다.
- 실제 게시 검증은 GitHub Pages 작업 결과와 공개 HTML·자산 내용 비교로 한다. 운영 로그인이나 실제 학생 자료를 열어 확인하지 않는다. 실제 휴대폰·크롬북은 미확인이다.

## 최신 후속 승인 — 2026-09-17 (배포 준비·계정 연결 검증)

사용자가 배포 후 수정·복귀 가능 여부를 물었고, “배포 준비와 연결 검증을 진행한 뒤 실제로 써보면서 다듬자”는 제안에 “오케이 그렇게 해줘”라고 답했다. 그 승인에 따라 배포 후보와 이전 화면 복구본을 준비한다. 이전의 커밋·푸시 금지와 실제 게시 범위는 별도 확인 대상으로 남기며, 준비 완료를 배포 완료라고 표현하지 않는다.

- 시제품의 가상 학생 고정 연결을 배포용에서 제거한다. 실제 AppMain이 로그인으로 받은 학생 id·학교 코드·기록을 새 화면에 전달한다. 학급 명단으로 친구 소속을 확인하고 공개 요약만 사용한다.
- 새 ‘내 안으로’ 입구를 **기존 학생 활동 화면 전체**에 연결한다. 일기·분석·마음 대화·탐험 연습·탐험일지·리포트·비밀번호 변경을 보존하며, 기존 인증/일기 저장/누적 섬 요약/학급 온도계 함수를 그대로 쓴다. 새 저장 구조나 DB 이전은 없다.
- 내가 보는 내 섬과 친구가 방문해 보는 같은 섬의 기록 식물은 같은 학생 기준으로 배치한다. 직접 입력한 마음 말도 빠뜨리지 않고 배치한다. 숨김 여부로 다른 마음 말의 자리를 바꾸지 않는다.
- 첫 화면과 게시판은 새 디자인을 적용하고, 실제 계정 화면에 ‘새로고침하면 기록이 사라짐’ 같은 로컬 전용 문구를 내보내지 않는다. 체험·시연의 저장하지 않는 규칙과 안내는 유지한다.
- 검증 중 발견한 기존 감정 수정 완료 안내 오류는 배포 후보의 화면 표시만 보완한다. 분석·저장 함수는 바꾸지 않는다. 이는 새 학습 기능 추가가 아닌 연결 검증에서 발견한 표시 오류 수정이다.
- 배포 후보, 메모리 DB 검증본, 이전 정적 화면 복구본을 분리한다. 운영 Firebase와 학생 자료에 접속하지 않고 가상 계정의 저장·재로그인·학급 게시판·친구 방문으로 검증한다. GitHub의 공개 배포 이력은 읽기만 한다.
- **이전 절에서 후속으로 남겨둔 운영 연결 준비가 이번 범위에 들어온다.** 기존 로컬 시제품은 보존한다. 아직 운영 첫 화면을 교체하거나 저장소에 커밋·푸시하지 않았다. 실제 휴대폰·크롬북은 여전히 미확인이다.

진행 결과와 복구 절차: `prototypes/island-world-v2/RELEASE_REVIEW.md`, `prototypes/island-world-v2/release/README.md`.

## 최신 후속 승인 — 2026-09-17 (친구 섬과 로그인 첫 화면)

사용자의 “남은 것: 친구 섬의 새 지형과 로그인 첫 화면 개선… 해줄래?”에 따라 로컬 시제품에서 두 화면을 구현한다. 기기 확인 질문에 “지금은 컴퓨터에서만 확인할게요”라고 답했으므로 이번 검증은 컴퓨터에 한정한다. 완성한 외형은 실제 실행 결과를 보고 판단할 시안이며 최종 외형 승인을 뜻하지 않는다.

- 내 섬과 친구 섬은 같은 해안·연못·다리·산책길·카메라 체계를 사용한다. 학생별 기록과 공개 설정으로 기록 식물이 달라지며, 기록이 없으면 기록 식물도 없다.
- 친구 방문은 내 섬 부두 → 기존 우리 반 바다 선택·항해 → 새 친구 섬 걷기·회전·공감 → 배를 타고 귀환한다. 친구 섬을 내 섬 안의 집이나 카드로 합치지 않는다. 바다에 멀리 보이는 섬 모형은 이번에는 기존 것을 재사용한다.
- 친구 섬에는 공개된 마음 요약만 전달한다. 개인 일기 진입은 차단하고, 숨긴 마음은 배치하지 않는다. 기존 같은 감정 구역 경험 조건, 같은 친구·마음 말에 하루 한 번 공감, 반딧불 표시 제한을 유지한다. 내 섬의 숨김 설정은 식물을 삭제하지 않는다.
- 첫 화면에 실제 섬 전경, 탐험 시작, 학생·선생님 로그인 입구를 둔다. 로그인 창 외형을 다듬되 기존 인증 처리 함수와 학습 화면을 재사용한다. 로컬 연습 계정만 메모리 DB에 준비하며 운영 계정은 입력하지 않는다.
- 컴퓨터에서 표준/가벼운 화질을 선택할 수 있게 한다. 가벼운 화질은 그림자와 해상도를 낮추고 기록·지형·이동 범위는 보존한다. 좁은 화면 배치 확인을 실제 휴대폰 조작·성능 확인으로 표현하지 않는다.
- **아래 탐험가 단계에서 후속으로 남겼던 친구 섬 새 지형과 로그인 첫 화면이 이번 구현 범위로 바뀐다.** ‘로그인 없이 바로 시작’은 기본 주소의 동작에서 대체한다. 운영 적용·배포, 실제 휴대폰·크롬북 확인, 나머지 세 구역의 상세 아트는 여전히 이번 완료 범위가 아니다.

변경과 실제 검증: `prototypes/island-world-v2/VISIT_REVIEW.md`. 운영 index.html과 저장 구조는 보존한다. 이 절이 아래 단계의 작업 범위보다 최신이다.

## 최신 후속 승인 — 2026-09-17 (탐험가와 승선·잠수)

사용자의 “괜찮아. 계속 가자”에 따라, 앞서 요청한 기본 캐릭터 개선과 배 타기·잠수 동작을 로컬 시제품에서 진행한다. 섬의 큰 틀과 완료된 해안 작업은 유지한다. 외형의 세부 디자인은 이번 실행 결과를 보고 검토할 시안이며, 최종 외형 승인을 받았다고 간주하지 않는다.

- 모자·목수건·배낭을 갖춘 작은 탐험가로 기본 캐릭터를 개선한다. 걷기, 방향 전환, 쉬는 동작과 눈 깜박임을 포함한다. 외부 모델 다운로드·Tripo 생성·유료 API는 사용하지 않는다.
- 부두의 행동 버튼은 **부두 끝까지 걷기 → 배에 오르기 → 출항 → 기존 우리 반 바다**로 연결한다. 잠수 입구의 행동 버튼은 **입구 접근 → 뛰어들기 → 물결 → 기존 개인 활동**으로 연결한다.
- 연출은 건너뛰기/취소를 제공하고, 기존 동작 줄이기 설정에서는 즉시 연결한다. 복귀할 위치를 보존한다. 상단 ‘내 안으로’ 바로가기는 연출 없이 활동을 연다.
- 기존 바다·친구 방문에도 같은 탐험가가 보이도록 크기와 캐릭터 호출 규약을 맞춘다. 이번 단계에서 친구 섬의 지형이나 방문·공감 규칙을 개편하지 않는다.
- **아래 해안 단계에서 후속으로 남겼던 캐릭터·승선·잠수는 이번 구현 범위로 바뀐다.** 친구 섬 새 디자인, 로그인 첫 화면, 운영 적용·배포는 계속 후속 범위다. 학습/저장/익명/공개 범위는 보존한다.

실제 확인과 한계: `prototypes/island-world-v2/EXPLORER_REVIEW.md`. 이 절이 아래 해안 단계의 작업 범위보다 최신이다.

## 최신 승인 결정 — 2026-09-17 (내 섬 V2)

사용자가 이번 대화에서 승인한 결정이다. 아래의 과거 구현 기록은 삭제하지 않고 남긴다. 충돌하면 이 절과 ISLAND_WORLD_V2_BRIEF.md의 새 공간 구조를 우선한다.

- **완성도가 최우선.** 화면과 실제 조작으로 판단하며, 의견 논의 단계에서는 파일을 수정하지 않는다. 구현 승인 뒤 정한 범위만 수정한다.
- 큰 틀은 유지하되 **정원 중심 인상을 바다에 둘러싸인 개인 섬으로 개선**한다. 해안선·모래사장·작은 만·해안 바위·부두를 드러내고, 정원은 섬 안의 일부로 둔다. 선명한 청록 바다·초록·따뜻한 모래·산호색 포인트를 사용한다.
- 개인마다 독립된 섬 하나, 섬 안에 활기/평온/슬픔/떨림 네 구역. 기록과 공개 설정에 따라 풍경이 달라진다. 배경 장식은 학습 식생으로 세지 않는다.
- 캐릭터 추적 높은 3인칭 시점. 좌우 360도·상하·확대축소·현재 위치 기준 복귀. 지형과 걷기/충돌을 함께 관리한다.
- 게시판은 내 섬의 우리 반 게시판으로 연다. 별도 광장은 최종 동선에서 제외한다. 질문/답변/익명/교사 검토/공개 조건은 보존한다.
- 친구 방문은 **내 섬 부두 → 우리 반 바다 → 친구 선택·항해 → 친구 섬 걷기·회전·공감 → 귀환**. 바다를 카드 목록으로 대체하거나 친구 섬을 내 섬의 구역으로 합치지 않는다.
- **이번 승인 범위:** 로컬 시제품의 섬·해안·바다 인상, 이동 때 뒤따라 흔들리는 글자 수정, 왼쪽 엄지 이동/오른쪽 시점 조작과 접을 수 있는 지도. 바다 선택·항해 흐름은 기존 기능을 재사용한다.
- 캐릭터 외형 후보/외부 모델 도입, 승선·잠수 전환 연출, 친구 섬의 새 외형, 로그인 첫 화면 확장은 다음 범위로 남긴다. 구현 완료로 간주하지 않는다.
- 운영 데이터 변경·배포는 별도 승인. 현재는 Firebase SDK·설정·시연 생성기를 제거한 메모리 DB 하네스만 실행한다.

**이전 내용에서 바뀐 점:** 아래 §3의 계단식 3층과 별도 광장 이동, §4의 고정 조감 카메라는 V2 목표에서 대체됐다. 떠다니며 뒤늦게 따라오는 장소 이름표는 없애고 세계 안 간판과 가까이 있을 때의 고정 행동 안내로 정리한다. 기존 로그인과 학습 화면은 삭제하지 않았으며, 현재 시제품만 로그인 없이 바로 시작한다. 크롬북 관련 과거 실측은 새 시제품 성능을 입증하지 않는다.

실행/변경/검증 기록: `prototypes/island-world-v2/README.md`, `prototypes/island-world-v2/COAST_REVIEW.md`. 운영 index.html은 이 단계에서 수정하지 않는다.

---

이 문서는 기존 감정일기 웹앱(`index.html`)에 3D 섬 세계를 추가하기 위한 설계서다.
Claude Code 작업 시 이 문서를 먼저 읽고, 여기 정해진 원칙에서 벗어나지 않도록 한다.

---

## 1. 대상 환경

| 항목 | 기준 |
|---|---|
| 주 사용 기기 | **크롬북** (학교 보급 기기, 사양이 낮다) |
| 보조 기기 | 태블릿, PC |
| 학급 규모 | **최대 30명** 기준으로 맵을 구성한다 |
| 사용 시간 | 아침 활동 10분 내외 |

**크롬북이 기준선이다.** 다만 실제로 학교 크롬북에서 Three.js 공식 예제를 확인한 결과,
**웬만한 예제는 무리 없이 돌아간다.** 성능은 이 프로젝트의 주요 병목이 아니다.
따라서 겁먹고 단순하게 만들 필요는 없다.

**가볍게 만드는 것과 저퀄리티는 다르다.**
3D의 인상은 폴리곤 수가 아니라 **조명, 색, 구성, 움직임**에서 나온다.
형태가 단순해도 조명이 좋으면 근사하고, 정교한 모델도 조명이 밋밋하면 싸구려로 보인다.
**질을 만드는 데 시간을 쓰되, 그 시간을 조명과 색에 쓴다.**

여유가 있으므로 다음은 넉넉히 써도 된다:
- **그림자** — 길고 부드러운 그림자가 입체감을 만든다. 아끼지 않는다
- **식생 밀도** — 같은 오브젝트를 크기·색·회전을 조금씩 달리해 반복 배치 (인스턴싱)
- **물 표현** — 얕은 바다의 투명감, 파도 가장자리

여전히 조심할 것:
- **후처리(bloom, DOF, SSAO)** — 화면 전체를 다시 그리므로 부담이 다르다. 넣기 전 반드시 측정
- **대용량 텍스처** — 텍스처를 거의 쓰지 않고 **색과 조명으로만** 질감을 만든다 (가볍고 통일감도 좋다)
- **섬 30개 동시 렌더링** — 어떤 예제로도 미리 확인이 안 된다.
  ③ 단계에서 섬 하나를 만든 직후 **30개로 복제해 크롬북에서 열어볼 것.**
  멀리 있는 섬은 단순화하거나 안개로 가려 그리는 양을 줄인다

각 단계를 만들 때마다 **실제 크롬북에서 확인**한다.

---

## 2. 세계관

> 각자의 마음 섬이 모여 하나의 바다를 이룬다.
> 내 안을 들여다볼 때는 아래로 내려가고, 친구와 만날 때는 바다를 건넌다.

- **수면 위** = 밖으로 드러나는 나 (섬, 친구, 광장)
- **수면 아래** = 안에 있는 나 (감정일기, 분석, 마음대화)
- 기존 앱의 "마음 바다 탐험" 컨셉을 유지하며, 깊이라는 축을 그대로 살린다.

---

## 3. 화면 구조

**내 섬이 곧 월드다.** 별도의 월드 화면을 만들지 않는다.
아이는 내 섬 위에서 **캐릭터를 직접 조작해 걸어다니고**, 가고 싶은 곳으로 걸어간다.

```
🏝️ 내 섬 = 첫 화면이자 허브 (캐릭터 조작, 계단식 3층)
 │
 ├─ ↓ 잠수 지점으로 걸어가면  → 감정일기 화면 (내 안으로)
 │      · 감정일기쓰기 / 감정분석하기 / 마음대화하기
 │      · 탐험연습하기 / 탐험일지보기
 │
 ├─ → 부두로 걸어가면        → 바다 화면 (친구 섬 선택)
 │      · 바다 색 = 학급 온도계
 │      └─ 친구 섬 탭 → 배가 항해 → 친구 섬 도착
 │
 └─ → 광장 표지로 걸어가면    → 광장 화면
        · 오늘의 질문 (교사가 올릴 때만) — 관찰/느낌/바람, 교사가 고른 답만 익명으로
        · 우리 반이 오늘 많이 쓴 감정 어휘 — 광장 섬에 식생으로 자란다 (⑤, 2026-09-07)

교사 화면 (기존 + 광장 질문 올리기)
  · 오늘의 현황 / 개별 현황 / 살펴봐주세요
```

**핵심 구분 — 깊이와 수평은 뜻이 다르다.**

| 방향 | 뜻 | 가는 곳 |
|---|---|---|
| **아래로 내려간다** | 내 안을 들여다본다 | 감정일기 |
| **수평으로 건너간다** | 친구에게 간다 | 바다 → 친구 섬 |

같은 바다지만 **잠수는 나를 향하고 항해는 친구를 향한다.**
이 구분이 §2의 세계관(수면 위 = 밖으로 드러나는 나 / 수면 아래 = 안에 있는 나)을
조작으로 체험하게 만든다.

**랜드마크는 층을 나눠 배치한다.** 한 곳에 모아두면 걸어다닐 이유가 없다.
1층의 둘도 서로 반대편에 둔다.

| 층 | 랜드마크 | 형태 (③-8) |
|---|---|---|
| 1층 (물가, 가장 넓음) | **부두**(바다로) | 기존 부두 + 끝에 묶인 작은 배. ⑥-1에서 이 배가 항해한다 |
| 1층 (부두 반대편) | **잠수 지점**(내 안으로) | 돌로 두른 웅덩이 + **사다리**. 평온 영역 물웅덩이와 둘 다 둥근 물이라 위에서 보면 헷갈린다. 사다리로 세로 요소를 주고, 웅덩이를 더 크고 물을 더 짙게 한다 |
| 3층 (가장 높고 좁음) | **광장 표지**(광장으로) | 팻말 + 별도의 **깃대**(높이 2.05). 팻말은 가까이서 읽고 깃발은 멀리서 보라고 있다. 1.45로는 나무들 사이에 파묻혔다 |

**셋 다 같은 밝은 디딤판 위에 서고, 같은 이름 팻말을 세운다.**
**팻말이 있는 곳 = 갈 수 있는 곳**이 공통 규칙이다. 규칙 하나를 배우면
셋을 다 알아보고, 다가가지 않아도 멀리서 보인다.
⑥-2 친구 섬과 ⑤ 광장에서도 같은 판과 팻말을 쓴다.

팻말 문구는 이름이 아니라 **어디로 가는지**다 — "바다로" / "내 안으로" /
"우리 반 광장". 글자는 작은 캔버스 텍스처 세 장뿐이고 문구가 세 가지라
모듈 수준에 캐시한다. §4의 "텍스처를 쓰지 않는다"는 표면 질감 이야기이고,
이름은 읽혀야 한다.

팻말 판은 **항상 카메라 쪽을 본다**(아이소메트릭이 고정이라 각도가 상수다).
그러면 빛을 등지므로 판이 어두워져 글자가 안 읽힌다. 약한 자체 발광을 줘
각도와 무관하게 읽히게 한다.

**다가가면** 디딤판이 밝아지고(세계 안에서 먼저 반응한다) 팻말이 들리며 팻말 바로 밑에 작은 나무 표시가 붙는다(아래).
머리 위 말풍선은 쓰지 않는다 — 디오라마 인상과 싸우고 빌보딩이 필요하다.

**역삼각형과 랜드마크 반응** (2026-09-14). 무엇을 만질 수 있는지, 여러 캐릭터 중 누가 나인지가 안 보였다.
- **내 캐릭터 머리 위 다이아몬드.** 아래가 뾰족한 작은 사각 쌍뿔(면마다 법선, 드로우콜 1)이 머리 bbox 꼭대기 +0.06에 떠서 4.5초
  주기로 ±0.03만 움직인다 — 깜빡임·회전 없음. 납작한 노란 역삼각형은 경고 표지처럼 보여 2차에서 바꿨다(2026-09-14 같은 날).
  높이는 몸 bbox에서 계산하므로(`attachMineMarker`) 캐릭터 형태가 바뀌어도 따라온다.
  depthTest를 꺼서 층 뒤에 숨어도 보인다. 내 것에만 붙는다 — 광장의 다른 아이들에는 없고, 친구 섬에서 걷는 것은 나이므로 붙는다.
  위 "머리 위 말풍선은 쓰지 않는다"는 문구 말풍선에 대한 것이고, 이 표시는 글자 없는 작은 기호라 예외로 둔다.
  색은 **산호 주황 `#ff7a59`**(결정, renders 439 후보 산호·진분홍·하늘 중). 캐릭터 남보라와 대비되고 붉은 나무·초록·흰·회색·노랑(경고색)으로 안 읽힌다.
- **다가가면**(`LM_NEAR` = 1.3, 세 곳 공통, 둘 안이면 가까운 쪽만) 디딤판 밝기·흰 외곽선·팻말 강조·팻말 아래 표시가 0.35초로 함께 페이드한다(`lmFxTick`).
- **외곽선은 스텐실 실루엣**(방식 B, 결정). 메시 둘이다 — 랜드마크 메시를 그대로 합친 **마스크**가 색·깊이 없이 스텐실에 1을 찍고(깊이 검사 없음:
  가려진 부분도 실루엣 안이다), 메시마다 bbox 중심에서 축별로 `LM_OUTLINE_W`만큼 키워 합친 **띠**가 스텐실이 1이 아닌 곳(실루엣 바깥)에만
  흰색으로 그려진다. **띠는 깊이 검사를 켠다** — 앞에 선 나무·캐릭터 뒤로 숨는다. 실루엣 안은 스텐실이 막으므로 부품끼리 겹치는 안쪽 선이 없고,
  얇은 원판(물) 위에도 띠가 생긴다(뒤집힌 껍데기 방식의 두 한계가 이걸로 없어졌다). 후처리(가장자리 검출)는 크롬북에서 전체 화면 두 번 읽기라 뺐다.
  두께 **0.035 → 0.09**(1280×800에서 약 3px → 약 8px). 0.035는 게임 크기에서 거의 안 보였다. 부두 판자처럼 얇은 판에서도 띠 폭이 같다(renders 445).
  켜진 동안만 드로우콜 +2. 처음 다가갈 때 만들고(먼 섬·함대는 안 만든다) 배(움직인다)는 뺀다. 남은 한계: 토러스(돌 테두리)는 안쪽으로 조금 두꺼워진다.
- **이름 팻말 자체를 강조한다**(2차). 1차에서 접근하면 작은 팻말이 따로 떠올랐는데 이름 팻말과 같은 말을 두 번 하게 되어 뺐다.
  대신 판이 0.08 들리고 글자면 발광이 0.45 → 0.9로 밝아진다(+외곽선). 팻말 생김새는 첫 화면 나무 간판(sign-login.webp)과 같은 톤으로:
  붉은 나무 틀 + 밝은 나무 판(옅은 결) + 둥근 기둥 둘. 전의 크림색 네모 하나에 기둥 하나는 웹 툴팁처럼 보였다. 판 몸통과 글자면을
  나눠 팻말 하나가 드로우콜 5(전 7, 면마다 재질). 키 표시(E 같은 것)는 세계 안에 두지 않는다.
  - **기둥은 판과 같은 회전에, 판 뒤에.** 처음엔 기둥을 랜드마크 그룹 좌표(−th 회전)에 두고 판만 카메라를 보게 돌려서, 섬 각도에 따라
    기둥이 판 앞으로 나와 글자를 덮었다("바다로"의 끝, "우리 반으로"의 앞이 잘렸다). 글자 너비 상한은 판의 78%(전 89%)라 틀과 글자 사이가 빈다.
    긴 문구("우리 반으로")는 글자를 줄여 담는다 — 판을 늘리면 팻말끼리 크기가 달라진다.
  - **광장 표지 판만 0.45 높다.** 3층에 있어도 앞(2층)의 큰 나무 관에 오른쪽 끝이 묻혔다. 3층은 좁아 시선 방향을 비울 수 없으니 판을 관 위로 올렸다.
- **스페이스 안내는 팻말 바로 밑에**(화면 아래 배너는 없앴다). 판 아래 모서리에 붙는 작은 HTML 표시로, 팻말과 같은 나무 톤(붉은 틀·밝은 판·
  어두운 갈색 글자)이다 — 흰 알약·회색 네모는 안 쓴다. 자리는 렌더 루프가 매 프레임 판의 월드 좌표를 투영해 잡으므로 판이 들리면 같이 오른다.
  키보드에서는 "스페이스", **터치 기기에서는 이 표시가 곧 버튼**이라 "눌러서 가기"(103×40px, 탭하면 전환 시작 확인). 친구 섬의 잠수 지점은
  "친구의 안으로는 못 가요"만 적고 누를 수 없다. 안내가 대상 옆에 있어야 "이 팻말에서 누른다"가 읽힌다.
- 수치: 내 섬 93 calls, 접근 중 95(외곽선 마스크+띠). 광장 34명 445, 바다 96. 37k tri, 60fps, 콘솔 오류 0. renders 426~434(1차)·435~440(2차)·441~447(3차).

**들어가는 것은 스페이스/Enter 또는 팻말 아래 표시 탭이다.** 걸어가면 바로 넘어가는 방식은
1층이 좁아 실수로 넘어가고, 머무르면 자동인 방식도 좁은 곳에서 오작동한다.
확인 팝업은 흐름을 끊는다. 표시가 곧 버튼이라 터치 기기에서도 한 번이면 된다.
터치 기기에서는 "스페이스" 대신 "눌러서 가기"라고 적는다 — 누를 키가 없다.

**부두 전환에는 캐릭터가 배를 타고 떠난다.** 부두 판자를 걸어 나가(0~42%)
배에 오르고(42~55%) 배가 섬을 떠난다(55~100%). 카메라는 배보다 조금 느리게
따라가 배가 멀어지는 것이 보이게 한다. 덮개는 출항이 보인 뒤에 내려온다.
`userData.boat`와 `userData.boatDir`을 열어두었으니 ⑥-1이 그 방향에서
항해를 이어받으면 된다.

**잠수 전환에는 캐릭터가 직접 뛰어든다.** 카메라만 내려가면 무엇이 일어났는지
알 수 없다. 웅덩이로 달려가 살짝 뛰어오르고, 물에 닿는 순간 물보라가 퍼진 뒤
가라앉는다.

**전환 연출이 축의 뜻을 말한다.** 덮개 방향만 다르게 해서는 모자라서
**카메라 자체를 움직인다.** 잠수는 카메라가 아래로 내려가 세상이 화면 위로
흐르고(캐릭터도 함께 가라앉는다), 부두·광장은 카메라가 옆으로 미끄러진다. 세 화면이 같은 방식으로 열리면
§2의 깊이/수평 구분이 조작에서 사라진다.

**돌아오면 캐릭터는 그 랜드마크 앞에 그대로 서 있다.** 자리가 초기화되면
다시 걸어가야 한다.

**섬 화면은 브라우저 창을 꽉 채운다** (2026-09-05). 상자 안에 넣으면
캔버스만 작아지고 아래가 흰 여백이 된다. 제목과 UI(나침반, 어휘 수, 배너)는
캔버스 위에 겹쳐 얹는다 — 자리를 차지하면 그만큼 섬이 줄어든다.
**카메라는 섬의 실제 크기를 카메라 좌표계에서 재서 맞춘다.**
넓은 화면에서는 세로가, 세로로 긴 화면에서는 가로가 기준이 된다.
한쪽 기준으로 고정하면 반대 비율에서 바다 여백만 커지고 섬이 작아진다.
바다는 잘려도 된다.

**섬을 원기둥으로 잡으면 안 된다.** 처음엔 모든 각도에서 깃대 높이까지
기둥을 세워 쟀는데, 그러면 섬 가장자리에 깃대가 서 있다고 가정하는 셈이라
세로 반폭이 6.56으로 부풀고 화면이 물러난다(고치기 전과 사실상 같았다).
층별로 나눠 실제 실루엣에 가깝게 재면 4.74가 되고 섬이 1.44배 커진다.

**내 섬이 첫 화면인 이유**: 접속하자마자 자기 섬이 어떻게 자랐는지 보게 된다.
매일 접속할 이유가 생기고, 세계관의 중심이 "나"에 놓인다.

**⚠ 허브 승격은 맨 마지막에 한다** (2026-09-05 결정).
크롬북 성능 확인(③-4)을 통과하기 전까지 내 섬은 **별도 페이지로 유지한다.**
3D가 앱의 입구가 되면, 크롬북에서 느릴 때 **일기조차 쓸 수 없게 된다.**
지금처럼 별도 페이지면 3D가 느려도 일기 기능은 멀쩡하다.
승격은 한 줄이고 되돌리기도 한 줄이므로 마지막에 붙인다.

**✅ 승격 완료 (2026-09-07).** 스위치는 `var ISLE_HUB = true;` 한 줄이다(`CHAR_LAST` 바로 앞).
`false`로 바꾸면 예전대로 일기 화면이 먼저 뜨고 내 섬은 "내 섬 보기" 뒤의 별도 페이지가 된다.
- 로그인 → `hubStart()` → 내 섬. 거기서 걸어 잠수(일기)·부두(바다)·표지(광장)로 간다.
- 내 섬 오른쪽 위 "돌아가기"는 갈 곳이 없어졌으므로 **로그아웃**(확인 한 번)이 됐다.
  일기 화면의 "내 섬 보기"는 방향이 반대가 되어 **"내 섬으로"**다. 리포트 저장·비밀번호 변경·로그아웃은 일기 화면에 그대로 있다.
- 교사 로그인은 손대지 않았다. 그대로 교사 대시보드로 간다.
- **3D가 안 뜨는 기기 — §3이 걱정한 상황.** 세 겹이다.
  ① WebGL이 없으면(`webglOK()`) 섬 화면을 거치지 않고 로그인 즉시 일기로 가고, 일기 화면에 "이 기기에서는 3D 섬을 볼 수 없어 일기 화면으로 왔어요" 한 줄과 함께 "내 섬으로" 버튼이 빠진다.
  ② 3D 라이브러리나 렌더러가 늦게 실패하면 `IslandView`의 `err`가 `onNo3D`를 불러 같은 곳으로 보낸다.
  ③ 뜨기는 하는데 느리면(만든 뒤 5초 지나 **10fps 미만이 6초**) 섬 위에 "섬이 느리네요. 일기로 바로 갈까요?" 배너가 뜬다.
  처음엔 15fps·4초였는데 크롬북 실측 21fps와 여유가 6뿐이었다. 잘못 뜨면 아이가 3D를 못 보므로 10으로 내렸다(여유 11).
  10fps 아래는 실제로 걸어서 잠수 지점까지 가기 힘든 속도다. **"괜찮아요"는 그날 하루 다시 안 뜬다**(섬을 나갔다 와도).
  이미 내려둔 기기에서 스스로 섬에 들어온 경우에도 안 띄운다.
  "일기로 가기"를 고르면 **이 기기는 다음 로그인부터 일기가 먼저**다(`localStorage.isle3d = off`, 일기 화면 버튼은 "내 섬 보기"로 돌아간다).
  그 기기에서 섬을 다시 열어 25fps 이상이 4초 이어지면 내림이 조용히 풀린다. "괜찮아요"는 이번만 넘긴다.
  감시는 fps 상태의 effect가 아니라 렌더 루프가 표본마다 직접 센다 — 같은 값이 반복되면 React가 렌더를 건너뛰어 effect가 안 돈다.

---

## 4. 시각 방향

| 항목 | 결정 |
|---|---|
| 카메라 | **아이소메트릭 고정** (위에서 비스듬히 내려다봄, 섬 전체가 한 화면에) |
| 질감 | 클레이(점토) 느낌 — 반사 낮게, 표면 부드럽게 |
| 형태 | **평평한 층이 쌓인 계단식** (2026-09-05 수정). 모서리는 **둥글게 깎는다**(베벨). 각진 로우폴리 아님 |
| 색 | **따뜻하고 선명하게.** 채도를 빼지 않는다 (2026-09-04 수정). 잔디만은 크림빛 도는 따뜻한 초록으로 한 단계 내렸다 — grassLow 0.47/0.61/0.31·grassHigh 0.64/0.75/0.41(전 0.44/0.62/0.27·0.61/0.75/0.36). 채도가 높으면 플라스틱처럼 보였다(2026-09-13, renders 402·406·407) |
| 조명 | 부드러운 그림자. **인상의 절반이 조명에서 나온다.** 그림자는 VSM(반지름 5·표본 12·지도 1024)으로 가장자리를 흐린다 — 반지름 9는 나무가 떠 보였다. 그늘 색은 바탕 계열: 바닥 반구광 풀빛 0x5f6b3c·채움광 연두 0xb9cfae라 그늘이 회색이 아니라 초록 어둠이다(2026-09-13, renders 408~413) |
| 라이브러리 | Three.js (React Three Fiber 검토) |

**지형은 자연 지형이 아니라 정원·마을에 가깝다** (2026-09-05 수정).
원래는 자연스러운 구릉으로 만들었으나, **캐릭터가 걸어다녀야 하므로** 바꾼다.
경사면은 걷기 어렵고 어디를 밟을 수 있는지도 불분명하다.

- **평평한 층 3개**가 쌓인 계단식. 맨 아래가 가장 넓고 위로 갈수록 좁아진다.
- 층 위는 **완전히 평평**하다. 여기가 걸어다니는 곳이다.
- 층 사이는 **계단이나 다리**로 연결한다. 시각 요소이면서 이동 경로다.
- 층의 윤곽은 여전히 **자연스러운 섬 모양**이다. 사각형 화단이 아니다.
- 클레이 디오라마 톤은 그대로다. 오히려 평평한 면과 수직 벽이
  **명암을 또렷하게 갈라 입체감이 더 살아난다.** (③-5에서 실측으로 확인:
  밝은 면과 그늘의 대비가 구릉 지형보다 오히려 올라갔다)

**③-5에서 정한 수치** (다시 만들 때 여기서 출발한다):

| 항목 | 값 | 이유 |
|---|---|---|
| 섬 반지름 | **섬마다 5.7~6.6** (2026-09-06, 기준 6.0) | 캐릭터(키 1.1)에 견주어 걸어다닐 공간. 하한 5.7은 시드 400개에서 폭·도달 검사를 통과한 값이다. 카메라·바다·그림자·함대 간격은 6.0 기준 `ISLE_K` 배율 그대로다 |
| 층 높이 | 0.34 / 0.90 / 1.46 | 단 0.56. 더 낮으면 벽이 얇아져 경사면으로 읽힌다. 캐릭터 키 대비 비율이 좋아 반지름을 키울 때도 그대로 두었다 |
| 층 가장자리 | 해안선의 0.74~0.88 / 0.52~0.66 / 0.26~0.40 배, 위층은 최대 1.2 치우침 (기준 0.82 / 0.585 / 0.32, 2026-09-06부터 섬마다 다름) | 해안선 굴곡을 층마다 다르게 눌러 담고, 층을 치우쳐 웨딩케이크를 피한다 |
| 어깨 베벨 | 걷기 0.075 · **보이는 면 0.12** (2026-09-12) | 클레이 인상은 모서리에서 난다. 크면 계단이 물러진다. 0.075는 아이소메트릭에서 2px라 칼로 자른 듯 보였다 |
| 벽 기울기 | 0.05 바깥으로 눕힘 | 완전 수직이면 기계처럼 보인다 |
| 밑동 필렛 | 걷기 0.085 · **보이는 면 0.12** (2026-09-12) | 벽과 아래층 평지를 접선으로 잇는다 |
| 평지 최소 폭 | 0.85 목표 | 해안선이 파인 방향에서 층이 실처럼 좁아지면 못 지나간다 |
| 계단 | 층마다 5단, 디딤면 0.16, 폭 0.62 | 단은 얇은 판이 아니라 위층까지 파고드는 상자다. 아래층이 좁으면 그만큼 가팔라진다 |
| 다리 | 1층↔3층, 활 높이 0.58, 폭 0.68 | 계단 둘의 반대편. 3층에서 1층까지 계단만으로는 너무 돌아간다 |
| 사람이 만든 나무 | `0xb05a3a` (붉은 쪽, 색상 약 16도) | 부두·다리·사다리·팻말 기둥·깃대를 한 계열로 묶는다. 전에 쓰던 갈색은 흙벽과 색상이 사실상 같아 다리가 통째로 묻혔다 |
| 이름 팻말 | 틀 1.42×0.51(판 1.30×0.39), 기둥 둘 · 판 중심 높이 0.97(광장 표지 1.42) (2026-09-14) | 작으면 안 읽힌다. 카메라 쪽으로 빼고 팻말 자리에도 여유 구역을 둬 식생에 가리지 않게 한다. 기둥은 판과 같은 회전에 두고 판 뒤에 세운다(§3) |
| 전환 시간 | 잠수 1400ms · 항해 950ms | 짧으면 쉭 지나간 느낌만 남고 방향이 안 읽힌다 |
| 캐릭터 기준 크기 | 키 1.1 · 지름 0.4 | 층 폭·계단·걷기가 전부 이 값에 맞춰진다 |
| 캐릭터 형태 | 학생 — 머리·머리카락·몸통·팔 둘·다리 둘 | 아이가 고르지 않고 하나로 고정. 얼굴은 넣지 않는다. 캡슐과 구만 써서 모서리가 둥글다(섬과 같은 클레이 톤). 뒷모습이 주로 보이므로 머리카락이 사람으로 읽히게 하는 부분이다 |
| 걷기 동작 | 부품 몇 개를 흔든다 | 뼈대 애니메이션은 쓰지 않는다. 리깅된 모델을 들여오면 §4의 라이선스·톤 통일 문제가 생기고, 직접 만들면 이 프로젝트에서 제일 큰 작업이 된다. 다리 번갈아 흔들기 + 몸통 위아래 튕김이면 충분하고 성능 부담은 0이다 |

**지면은 둘이다 — 걷기면 `profileAt`과 보이는 면 `visAt`** (2026-09-12).
베벨을 키우려면 평지 가장자리를 깎아야 하는데, `profileAt`을 건드리면 걷기·식생·계단·길·랜드마크가
전부 따라 움직인다. 그래서 `profileAt`(어깨 0.075·밑동 0.085)은 그대로 두고, 메시 정점만 쓰는
`visAt`(어깨·밑동 0.12)을 옆에 두었다. 둘은 벽 위치와 평지 높이가 같고 어깨 안쪽 띠(0.045)와 밑동
바깥 띠(0.035)에서만 갈린다. 링은 호 0.05마다 하나(먼 섬은 둘), 해변은 5→8링(모양은 그대로, 표본만).
갈색은 어깨 바깥 55%·밑동 안쪽 60%에만 둔다 — 갈색 띠가 넓어지면 다시 경사처럼 보인다.

0.12/0.12 · 0.18/0.16 · 0.24/0.20을 하준(어휘 15)·윤서(25) 섬으로 견줬다(renders 198~209).
0.18부터는 벽의 곧은 부분이 줄어 층 구분이 뭉개지고 계단식 구조가 흐려져 0.12로 정했다.
지형 메시 5,928 → 6,312 tri(+6%), 전체 33k 그대로, 60 fps.

알고 있는 한계 (2026-09-12 수용): 캐릭터가 벽 끝까지 밀착하면 발이 보이는 면보다 최대 0.035
(키 1.1의 2.7%) 떠 보이고, 밑동에서는 최대 0.027 묻힌다. 걷기 판정이 그대로라 평소 걷기에서는
나지 않고 벽에 딱 붙였을 때만 난다. 0.18이면 0.085(8%), 0.24면 0.137(12%)이라 그쪽도 0.12를 고른
이유다. 확인: walkY 체크섬(격자 15,373점)이 수정 전과 동일하다(TEST 8-3).

**평지 최소 폭 0.85는 반지름 6.0에서 두 층 모두 지켜진다** (2026-09-05).
반지름 4.2일 때는 1층에서만 지켜졌고, 2층은 둘레의 13%에서 0.54까지 눌렸다.
가장 좁은 방향의 해안선에서 해변 + 1층 0.85 + 2층 0.85 + 3층을 다 담을 수
없었기 때문이다. 반지름을 6.0으로 키워 해결했다. 시드 400개로 확인.

부수 효과로 **윤곽을 억지로 펴는 최소 폭 잠금이 둘레의 45% → 3%로 줄었다.**
층 윤곽이 그만큼 더 자연스러워졌다.

**층 사이는 계단 둘과 다리 하나로 잇는다** (③-8 추가).
다리는 계단 둘의 반대편에 두어 한 방향으로 돌면 제자리로 오는 순환 경로가
된다. 부두 → 광장 표지가 평균 8.7초 → 5.7초로 줄었다(최단 경로 기준).

다리는 가운데가 2층 위를 지나므로 직선으로 놓으면 2층 턱을 스친다.
활처럼 띄우고, 걷기 판정도 같은 곡선을 쓴다.

**딛는 면은 "한 걸음 안에 닿는 가장 높은 면"으로 고른다** (③-8에서 배운 것).
늘 높은 쪽을 고르면 다리 밑 2층을 걷던 아이가 상판으로 끌려 올라간다.
그렇다고 "지금 높이에 가까운 쪽"을 고르면 계단을 못 오른다 — 오를 때는
발밑의 평평한 지면이 늘 더 가깝기 때문이다.
한 걸음 한계(0.28)는 다리가 2층 위로 띄운 높이(0.42~0.54)보다 작아야 한다.

**섬 크기는 `ISLE_R` 하나로 바꾼다.** 카메라 거리·정사영 폭, 바다 반지름,
그림자 상자, 함대 간격이 모두 `ISLE_K = ISLE_R / 4.2`를 곱해 따라오므로,
반지름만 바꿔도 아이소메트릭 구도와 화면 점유 비율이 유지된다.
반대로 캐릭터 크기, 층 높이, 베벨, 계단 단 크기, 부두 판자는
**아이 몸 기준이라 절대값으로 둔다.**

**캐릭터가 위층에 가려질 때**는 같은 원기둥을 `depthTest: false` 단색으로
한 번 더 그려 비쳐 보이게 한다. 본체를 그 위에 덧그리므로 안 가려질 때는
티가 나지 않는다. 실루엣 색은 **본체와 같은 색상에 밝기만 올린 것**이다.
살구색으로 두었더니 모래·계단 돌과 겹쳐 돌기둥으로 보였다.

**③-6 걷기 수치**

| 항목 | 값 | 이유 |
|---|---|---|
| 속도 | 1.7 / 초 (섬 크기와 무관하게 고정) | 반지름 6.0에서 1층 가로지르기 약 5.8초. 섬이 커진 것을 걸어서 느끼게 하려는 것이고, 저학년 조작에도 느린 쪽이 맞다 (2026-09-05 결정) |
| 오를 수 있는 기울기 | 2.0 이하 | 벽은 11 이상, 계단 0.88, 해변 최대 1.2 |
| 물가 한계 | 지면 높이 0.06 | 이보다 낮으면 못 간다 |
| 계단 배제 반경 | 1.2 | 계단 발치·상단 둘레에는 식생을 놓지 않는다 |

**기울기는 걸음이 아니라 지면에서 잰다** (2026-09-05, 실제로 겪은 문제).
처음에는 한 걸음의 높이차를 걸음 길이로 나눠 판정했다. 그러면 벽을 비스듬히
지그재그로 오를 수 있다. 한 걸음이 거의 접선 방향이면 높이차가 작게 나오는데,
그 조금씩이 쌓여 캐릭터가 벽 위에 올라선다.
목적지 **지면 자체의 기울기**로 판정해야 방향과 무관하게 막힌다.

**벽에 막히면 등고선 방향으로 미끄러뜨린다.** 축을 하나씩 시도하는 흔한 방법은
판정이 지면 기울기라 통하지 않는다. x만 움직여도 여전히 급한 자리에 떨어진다.

`#dev` + **G**: 2층이 가장 좁아지는 지점으로 캐릭터를 옮기고 좌우 벽을 칠한다.

지면 높이는 `profileAt(r, th)` **한 함수**가 정한다. 메시도 식생도 계단도
걷기 판정도 전부 이 함수를 부르므로, 보이는 지면과 판정이 어긋날 수 없다.

**참고 방향**: 작은 모형 세트를 위에서 내려다보는 **디오라마** 느낌.
각진 면이 그대로 드러나는 로우폴리 스타일과는 다르다.
표면이 부드럽고 모서리가 둥글게 깎여 있어야 점토 인상이 난다.
텍스처를 쓰지 않고 **색과 조명만으로** 질감을 만드는 것이 이 스타일의 핵심이다.

**얼룩은 셰이더 값 잡음으로** (2026-09-13). 이미지 없이 지형 재질에 world 좌표 값 잡음(두 옥타브 0.9·0.35 단위)을 끼워
밝기만 흔든다(`landShade`, 색상은 그대로): 잔디 ±5%, 모래 ±3%, 흙길 리본 ±4%, 흙벽은 가로로만 변하는 잡음으로 세로 결 ±4.5%.
멀리서는 단색, 가까이서 결. 정점마다 주던 흰 잡음(±3.5%)은 ±1.5%로 줄였다. 비용은 픽셀당 산술 30개, 삼각형·드로우콜 변화 없음(내 섬 98 calls·37k tri·60fps, 섬 30개 1880·791k·60fps). renders 413~415.

레퍼런스별로 가져올 것:
- **전체 구도** — 섬 하나가 화면에 통째로 들어오고, 주변 얕은 바다에 산호가 흩어진 구성
- **지면 디테일** — 길게 깔린 부드러운 그림자, 같은 덤불을 크기·색 달리해 반복 배치한 밀도
- **높낮이** — 평평한 섬보다 **살짝 언덕이 있어야** 섬처럼 보인다. 부두도 둔다 (배를 타고 나가므로)
- **질감·색** — 클레이 무광, 파스텔

**색 주의** (2026-09-04 수정): 원래는 "채도를 낮춘 차분한 파스텔"로 적었으나,
실제로 만들어 보니 **창백하고 색이 빠진 것처럼** 보였다. 방향을 바꾼다.

- **차분함은 채도가 아니라 톤 통일과 그림자에서 나온다.**
  클레이 디오라마 레퍼런스를 보면 잔디는 선명한 초록이고 나무 갈색도 진하다.
  채도가 낮지 않다.
- **따뜻하고 선명하게** 간다. 잔디의 노란기는 살린다.
- 여전히 피할 것은 **남국 리조트 톤**이다. 그건 채도가 아니라 밝기와
  청록 편향의 문제다.
- 밝은 톤만으로는 **슬픔 영역이 표현되지 않는다.** 이건 그대로 유효하다.
  슬픔 영역은 채도를 빼는 대신 **어둡고 차가운 쪽**으로 간다.

**조명 수치를 잴 때 지면 마스크를 색으로 잡으면 안 된다** (2026-09-05, ③-8에서 발견).
바다를 색으로 걸러내면, 조명을 바꾸는 순간 어떤 픽셀이 "바다"로 빠지는지가
달라져 설정끼리 비교가 성립하지 않는다. 실제로 그 때문에 빈 섬 대비가
26%p로 나와 목표(25)에 턱걸이인 줄 알았고, 조명을 손대야 한다고 두 번 적었다.
바다를 감추고 배경을 마젠타로 한 번 더 그려 **기하로 마스크를 뜨면
빈 섬 대비는 44.8%p**다. 조명은 ③-2 값 그대로 두어도 된다.

**조명 원칙** (③-2에서 정함):
- 빛은 화면 기준 **왼쪽 위**에서. 그림자는 오른쪽 아래로 일관되게.
- 고도는 낮게(약 33도). 높으면 그림자가 짧아 형태가 드러나지 않는다.
- 채움광은 낮게 유지한다. 높이면 그늘이 떠서 대비가 죽는다.
- 목표 수치: 밝은 면과 그늘의 대비 25%p 이상, 명암 폭 25%p 이상,
  화면 채도 25% 이상, 어떤 채널도 255로 잘리지 않을 것.
- **그늘 바닥값도 함께 본다** (③-8에서 더함). 대비를 올리려다 그늘을
  새까맣게 죽이면 안 된다. 어두운 쪽 하위 5% 밝기가 8% 아래로 내려가지 않을 것.
- ③-8 실측(기하 마스크): 어휘 0/4/16가지에서 대비 44.8 / 50.1 / 51.4%p,
  명암폭 58.7 / 62.8 / 61.4%p, 채도 48.9 / 49.3 / 50.2%, 그늘 p5 18.2 / 15.2 / 14.0%.

**붉은 나무 = 사람이 만든 것, 초록·흰·회백색 = 자란 것** (2026-09-12에 명시. 규칙 자체는 ③-8에서
다리 색을 흙벽과 떼어낼 때부터 있었고, 새로 만든 것이 아니라 있던 것을 적는다)

섬은 감정 어휘의 지도다(§5). 아이가 무엇이 어휘로 자란 것이고 무엇이 그냥 장식인지 한눈에 갈라야
지도가 선다. 앞으로 무엇을 더하든 이 기준으로 판단한다.

- **사람이 만든 것**(부두·다리·사다리·팻말 기둥·깃대·소품)은 붉은 나무색 `0xb05a3a` 한 계열이다.
  판만 이름 팻말과 같은 크림색. 형태는 직선·판·통. 발광하지 않는다.
- **자란 것**(감정 식생)과 **주어진 것**(중립 식생)은 초록·꽃색·흰색·회백색이고 형태는 구·잎·줄기다.
- 판단 기준: 처음 보는 아이가 "이것도 자란 건가?" 하고 물을 수 있는 색이나 형태면 소품으로 쓰지 않는다.
  헷갈리는 순간 "섬은 감정 어휘의 지도"라는 설계가 무너진다.

**등불은 소품으로 쓸 수 없다.** 등불은 슬픔 영역 3층의 감정 식생이다(§5 표). "어두운 감정도 섬을 아름답게
만든다"가 가장 잘 보이는 자리라 거기 두었고, 같은 이유로 공감 표식으로도 쓰지 않는다. 길가에 등불을
반복하면 아이가 슬픔이 자란 것으로 읽는다. 발광하지 않는 가로등으로 만들어도 이름과 실루엣이 겹친다.
그루터기 의자(나무 줄기색 = 평온의 나무)와 화분(식물이 든다)도 같은 이유로 뺐다.

**소품** (2026-09-12) — 섬당 12~20개. 감정과 무관한 장식이며 어휘 수와 무관하게 섬이 만들어질 때
시드(`"props:" + 시드`)로 놓이고 같은 섬은 늘 같다. 식생보다 먼저 놓여 `keepOuts`에 들어가므로 어휘가
늘어도 움직이지 않고 식생이 그 위에 서지 않는다. 길·마당·계단·랜드마크 여유 구역은 식생과 같은 판정으로
피한다. 걷기는 지형만 보므로 소품이 물리적으로 막지는 않지만 길·층 띠에 0.6(캐릭터 지름 + 여유)을
남긴다. 높이는 0.44 이하(캐릭터 1.1). 등불 대신 길가 말뚝이 "사람이 다니는 길"의 리듬을 만든다.

| 소품 | 개수 | 어디 |
|---|---|---|
| 길가 말뚝 | 최대 8 (시드 40개 평균 6.9) | 1층 길을 따라 1.2 간격, **그려진 리본 가장자리에서 0.10 밖, 카메라 반대쪽(먼 쪽)**, 막히면 가까운 쪽 — 벽 밑동에 붙어 도는 구간은 먼 쪽이 필렛뿐이라 말뚝만 필렛 위를 허용한다(높이는 보이는 면 visAt 기준). 높이 0.26·굵기 0.055로 흰 들꽃(0.28~0.44)보다 낮다. 처음에 원 폴리라인 기준 0.25 밖에 세웠더니 잔디 한가운데 흩어진 기둥으로 보였다 — "길을 따라 세운 것"으로 읽히려면 바짝 붙어야 한다. 붙인 뒤에는 발치가 리본 밖(전부 0.25)인데도 카메라 쪽 기둥이 화면에서 길을 덮어 길 위에 선 것처럼 보였다 — 아이소메트릭에서 앞쪽 기둥은 뒤의 지면을 가린다. 0.36 높이는 배경이 아니라 주인공처럼 보였다. 길이 벽 밑동에 붙어 도는 구간은 한쪽이 늘 막힌다 |
| 울타리 | 2줄 × 기둥 4 + 가로대 2단 | 1층 바깥 가장자리·2층 가장자리 한 구간씩, 가장자리 0.24 안쪽 |
| 이정표 | 1 (40개 전부) | 길이 갈라지는 시작점 곁 0.56~0.86, 없으면 1.3까지, 그래도 없으면 첫 길을 따라가며. 글자 없는 화살판 둘 + 화살촉. **랜드마크가 아니라 그리로 가는 길의 첫 구간(시작점에서 1.0 지점)을 가리킨다** — 길은 층 띠를 따라 돌아가므로 랜드마크를 직선으로 가리키면 길 없는 쪽을 가리키게 된다 |
| 벤치 | 1 (40개 전부) | 광장 마당 가장자리, 없으면 잠수 지점 → 부두, 그래도 없으면 3층·2층·1층 아무 평지에서 가장 가까운 랜드마크를 본다. **랜드마크와 같은 층에서만** — 층을 안 보면 위아래 층에 떨어진다(나무통이 부두 위 2층에 선 적이 있다) |
| 나무통 | 1~2 (40개 전부, 평균 1.45) | 부두 표지 곁 1층, 없으면 더 멀리, 그래도 없으면 1층 아무 평지 |

**다섯 종류가 모든 섬에 있다.** 처음에는 벤치 34·이정표 38(40개 중)이었다. 아이들이 섬을 견줄 때
"내 섬엔 벤치가 없네"가 되면 소품이 감정과 무관하다는 말이 서지 않으므로, 마지막 단계에서 자리를
넓혀 전부 놓이게 했다. 개수(말뚝 5~8, 나무통 1~2)는 섬마다 다를 수 있고 그것은 지형 탓으로 읽힌다.

전부 인스턴스 메시 둘(둥근 상자 `roundedSlab`·원기둥) — 섬당 드로우콜 +2, 약 3k tri.
내 섬 33k → 36k, 60 fps. 빈 섬(어휘 0) 31k. 먼 섬(detail < 0.6)은 계단·다리·길과 같이 생략한다.
성능 확인 모드(섬 30개 전부 최고 디테일) 691k → 768k tri, 1755 → 1805 calls, 60 fps.
확인: renders 210~217(소품 옆에 식생을 나란히 둔 근접 211·214·215, 이정표 212), TEST 8-4.

**참고와 모방의 선** (2026-09-10 수정)

양식은 참고해도 된다. 나무 간판 UI, 두꺼운 테두리 글자, 클레이 질감 같은 표현 방식은
저작권 대상이 아니며 널리 쓰이는 관용적 방법이다.

하지 않는 것은 다음이다.
- 특정 작품의 캐릭터·로고·아이콘·배경을 그대로 옮기는 것
- 화면 구성을 통째로 베껴 원작이 곧바로 떠오르게 하는 것
- 저작권 있는 에셋을 그대로 가져다 쓰는 것

판단 기준: 만든 결과를 처음 보는 사람이 특정 게임의 이름을 떠올린다면 다시 만든다.
양식이 닮은 것은 괜찮고, 그 작품으로 착각되는 것은 안 된다.

외부 3D 모델·이미지·음원은 CC0 등 라이선스가 명확한 것만 쓰고 출처를 기록한다.
생성 도구로 만든 자료도 도구명을 출처에 적는다.
기록은 `CREDITS.md`(서체·그림)와 `sounds/CREDITS.md`(음원)에 한다.

---

### 3D 조작 손질 (2026-09-16)
- **계단 넷.** 계단 A(1→2)·B(2→3)·다리(1↔3)만으로는 1↔2가 A 하나, 2↔3이 B 하나라 내려올 때 되짚어야 했다. 계단 C(1→2)·D(2→3)를 더했다(`stairC`·`stairD`).
  자리는 난수를 더 뽑지 않고 이미 정해진 것들 사이 **가장 넓은 빈 각도의 한가운데**(`gapMidTh`): C는 부두·A·B 발치·다리·잠수 지점을, D는 B·다리·광장 표지·A 상단·C 상단을 피한다.
  같은 인스턴스 메시라 드로우콜은 그대로(+10 인스턴스). 발치·상단 여유 구역(1.2)은 기존 코드가 넣어 식생·소품이 피한다. 흙길은 A·B만 잇는다(C·D는 길 없음).
- **달리기.** `WALK_SPEED` 1.7 → 2.6. 발걸음 음원과 팔다리 스윙은 1.7에 맞춰 둔 것이라 `WALK_CADENCE = 8.5 × 속도/1.7`, `WALK_STEPS_RATE = 속도/1.7`(preservesPitch)로 함께 간다.
  튜토리얼 "걷는 법" 조건(`TUT_WALK`, 걸은 시간 2초 + 방향 2번 / 6초)은 시간 기준이라 그대로 맞다.
- **광장 걷기.** 내 캐릭터(`buildCharacter`)가 0번 자리에 늘 서고 방향키·WASD·터치 패드(`WalkPad`, 내 섬과 같은 컴포넌트)로 걷는다. 바닥 세 단은 한 걸음에 오르는 턱, 나무 테두리 밖·모닥불·화분·깃발 기둥은 못 간다(`PLZ_WALK`).
  답을 쓴 아이들 캐릭터는 1번 자리부터(`buildPlazaFigures` skipMine). 카드의 글 입력란에 있을 때는 키를 먹지 않는다. **카메라는 고정** — 광장 전체가 한 화면에 들어오므로 따라갈 것이 없고, 오른쪽 패널·투어 앵커가 흔들리지 않는다.
- **바다 더블클릭.** 위 표(바다에서 고르기).
- **공감 여러 번.** 위 표(횟수·저장 경로).

### 광장 개편 (2026-09-16)
- **세 칸 → 한 칸.** 관찰·느낌·바람 세 칸(관찰 필수, 느낌은 칩 또는 한 줄, 바람 선택)을 답 한 칸(자유 글, 240자)으로 바꿨다. 마음 말 칩(다섯 개까지)은 그대로지만 선택이다.
  저장은 그대로 `o`에 넣고 `f`·`w`는 빈 칸 — 옛 세 칸 답은 `plazaAnsText`가 이어 읽는다. 이름 검사("친구로 바꾸기"·두 번째 저장은 숨김)와 교사 승인(비공개/광장/숨김)은 그대로. 일기의 "마음 대화하기"는 건드리지 않았다.
- **게시판을 실제로 쓴다.** 3D 판(5.0×2.3, 전 2.5×1.15)은 늘 서고 글은 **HTML 덧판**(`PlazaBoard`, `data-tut="plz-board"`)이 쓴다. 렌더 루프가 판 앞면 네 귀를 투영해 덧판을 맞추고(`placePlazaBoard`) 글자 크기는 판 폭에 비례.
  3D 텍스처는 쓰지 않는다 — 한글 글꼴·해상도·갱신마다 다시 그리기 위험. 내용: 오늘의 질문(없는 날 "오늘은 질문이 없는 날") · 오늘 우리 반 마음 말 TOP3(많이 쓴 말이 크게, `dayWords`가 돌려주는 c 비율만 쓰고 숫자는 없다) · 친구들의 답(내 답을 쓴 뒤 광장에 올라간 답이 4초마다 하나씩, 쓰기 전에는 "N명이 답했어요").
  판 폭 220px 아래(800 폭 이하·폰)는 덧판을 숨기고 카드가 TOP3 한 줄을 대신 보인다(`boardShown`). 투어 첫 단계가 게시판이고 폰에서는 skipMissing으로 빠진다.
- **인형을 뺐다.** 답을 쓴 아이 수만큼 서던 캐릭터(`buildPlazaFigures`)를 지웠다. 그 수는 카드와 게시판의 "N명이 답했어요"로 남는다(`n/` 경로 그대로). 걷는 내 캐릭터만 선다. 드로우콜 295 → 140(18명 답한 시연 광장, 1280×800, 60fps 그대로).
- 광장 안내 팻말(투어 시작 버튼)은 넓은 화면에서 왼쪽 아래(시연 띠 위)로 — 왼쪽 위는 게시판이 차지한다. 폰은 전처럼 왼쪽 위. 투어는 4단계(게시판·질문·이름·광장과 섬), renders 559~566.

### 교사 화면 손질 (2026-09-16)
- **「요즘 달라진 아이」 제거.** 카드·판정(`patternShifts`·`SHIFT`·`PHRASES_SHIFT`)·투어 단계·check.js 검사를 지웠다(§9-④에 기록만 남김). 「말 걸어봤어요」는 살펴봐 주세요에만 있고 사흘 뒤 「다시 볼 때예요」 흐름은 그대로. 시연 데이터 사례(10·12·16번)는 그대로.
- **오늘의 질문 자동.** 전에는 교사가 분류(감정 인식·타인 이해·관계와 바람·어휘 확장)를 고르고 예시를 누르거나 직접 써서 올렸다. 이제 `PLAZA_AUTO_QS` 목록에서 **순서대로** 다음 질문이 광장 카드에 미리 채워지고(`plazaAutoNext`), [그대로 올리기]·[고쳐서 올리기]. 학급 데이터를 보고 고르지 않는다(§9). 순서는 질문 노드의 `ai`로 잇는다 — 고쳐 올려도 그대로, 직접 쓴 질문(ai 없음)은 세지 않는다. 한 바퀴 돌면 처음으로 돌아가고 "한 바퀴 돌아 처음 질문으로 돌아왔어요"를 보인다. 오늘 이미 올렸으면 자동 블록을 접는다. 목록 30개는 2026-09-16 사용자 확정(가벼운 것·어휘 유도·조금 깊은 것을 세 개에 하나쯤 번갈아 배열). 진행은 올릴 때마다 다음으로(날짜 기준 아님, 사용자 결정). 질문 아이디는 `q<시각>_<난수 4자>` — 같은 밀리초에 두 번 누르면 아이디가 겹쳐 앞 질문이 덮였다(2026-09-16, 하네스에서 발견).
- **교사 투어**: 요즘 달라진 아이 단계를 빼고 「말 걸어봤어요」 단계(`t-talk`: 「다시 볼 때예요」 띠 또는 첫 버튼)를 넣었다. 그 단계와 광장 단계 문구는 ②(짧게)로 확정(2026-09-16).

### 리포트에 어휘 확장 흐름 (2026-09-16)
앱의 목적은 감정 어휘 확장인데 리포트는 집계(상태)뿐이었다. 학생·교사 리포트의 "감정 사분면 분포"와 "자주 나온 감정" 사이에 **「마음 말이 늘어났어요」** 한 섹션을 넣었다.
- **가르기**: 고른 기간(첫 일기~마지막 일기)을 **날짜로 반**씩. 편수로 가르면 날짜가 고르지 않고, 첫 2주·최근 2주로 하면 가운데가 빠지고 기간 길이에 따라 달라진다. 일기 **5편·7일** 미만이면 가르지 않고 "지금까지 쓴 마음 말"을 첫 날짜와 함께 보인다.
- 왼쪽 "처음에 쓰던 말"(첫 등장 순), 오른쪽 "나중에 새로 쓴 말 · 처음 쓴 날"(날짜·요일). 새 말이 없으면 "새로 쓴 말은 없어요. 처음 쓰던 말을 계속 쓰고 있어요." 개수는 쓰지 않는다 — 말이 주인공.
- 계산은 `labelsFirstSeen`(순수 함수) 하나를 「말 걸어봤어요」의 새로 나온 긍정 감정과 리포트가 같이 쓴다. 리포트 창은 별도 문서라 함수 소스를 그대로 넣는다(`toString`). 바깥 것을 참조하지 않아야 한다.
- 교사 리포트는 같은 섹션에 더해 **"그때의 상황"을 처음·나중 두 칸**으로(같은 경계). 살펴봐 주세요 이력·일기 본문·섬 그림·주별 그래프는 넣지 않는다(§9-③, 사용자 결정).
- 인쇄: `.grow`는 페이지를 넘지 않게(break-inside avoid). A4 폭(794px)에서 넘치는 요소 0. 하네스 `shootReport.js`(학생 많음·적음·5편 미만·새 말 없음, 교사 둘) renders 569~571.

## 5. 내 섬 — 감정 어휘의 시각화

### 자라는 기준
- **감정 어휘의 "종류"** 기준으로만 자란다.
- 일기를 쓴 **횟수**로는 자라지 않는다. (양치기 방지)
- 같은 어휘를 반복해도 늘지 않는다.
- **누적이다. 한 번 생긴 식생은 사라지지 않는다.** (2026-09-04 확정)
  - 섬은 "요즘 기분"이 아니라 **내가 알게 된 감정 어휘의 지도**다.
  - 기간을 두어 식생이 사라지게 하면, 아이가 섬을 유지하려고 억지로 그 단어를
    쓰게 된다. 그것이 바로 양치기다.
  - "우울한 날을 기록해도 섬이 나빠지지 않아야 한다"는 원칙과도 정면으로 충돌한다.
- **어휘 하나당 식생 3개, 어휘 수와 무관하게 고정.** (2026-09-10 결정)
  - 전에는 어휘 수에 따라 어휘당 6개(8개 이하)·4개(16개 이하)·3개(26개 이하)·2개로 줄이는
    계단식이었다. 적을 때 풍성하게 보이려는 뜻이었지만, 문턱을 넘는 날 기존 어휘의 식생이
    사라졌다 — 8개에서 9개가 되면 합계 48개가 36개로 줄고, 16→17·26→27에서도 같은 일이
    났다. 아이가 새 말을 배운 날 섬이 초라해지는 것이라 위의 "사라지지 않는다"와 어긋난다.
    계단식은 문턱을 어디에 두어도 이 하락을 피할 수 없다.
  - 고정 3개면 어휘가 늘 때 감정 식생은 늘기만 한다(먼저 얻은 어휘부터 놓고 자리는 어휘
    이름으로 정하므로 새 어휘가 기존 것을 건드리지 않는다). 첫날의 풍성함은 중립 식생(풀·자갈·
    덤불·노두)이 남는 자리를 채워 지키고, 어휘가 많아 자리가 모자라면 배치 실패로 자연히 멈춘다.
  - 확인(renders 190~197, vegcheck_2026-09-10.txt): 시드 400개 × 어휘 30개에서 빠진 어휘 0·침범 0. 어휘를
    0→30 하나씩 늘릴 때 감정 식생은 늘기만 한다(옛 규칙은 9·17·27에서 32→28처럼 줄었다). 어휘 최대치는
    감정 목록 30개. 첫날(0개)은 중립 46개로 휑하지 않고, 30개는 80개로 빽빽하되 길·여유 구역은 비어 있다.
  - **어휘 누락 문제와 구조 4·5단계** (2026-09-10, 같은 맥락): "어휘 하나가 통째로 빠지면 안 된다"는 약속을 지키는
    구조 단계가 셋(작게·더 작게·간격 봐주기)뿐이었고, 셋 다 제 방위 ±0.85rad 안에서만 자리를 찾았다. 어휘 30개면
    한 영역에 24개까지 몰려 그 방위가 꽉 차고, 시드 120개 중 18개(15%)에서 한 어휘가 섬에 흔적도 없이 빠졌다.
    옛 계단식에서도 어휘 26개부터 같은 문제가 있었다. 이웃 방위까지 훑는 4단계(±2.5rad, 시도 120번), 섬 전체를
    훑고 물가 자리가 없으면 물결 대신 그 영역의 다음 층 식생(바위)을 놓는 5단계를 더해 400개 시드에서 0으로.
  - **메모 · 식생 배치 비용** (2026-09-10 측정): `buildVegetation`이 어휘 25개면 섬당 약 0.54초, 5개면 0.17초,
    0개(중립만)도 0.10초다(`buildIsland`는 0.01초). 자리 찾기마다 `tierBand`가 반지름을 0.01 간격으로 훑기
    때문이다. 내 섬 한 장면에서는 문제없지만, **바다 화면(⑥-1)은 친구 섬마다 식생까지 만드는 구조**라 학급 30명이면
    합쳐서 몇 초가 될 수 있다. 크롬북에서 바다 화면 진입 시간을 재고, 길면 `tierBand` 캐시(각도별 층 구간 표)나
    먼 섬 식생 생략을 검토한다. 이번 변경(어휘당 3개 고정)으로 늘어난 비용은 아니다.
    → **2026-09-15 조치.** 바다 진입이 맥에서 5.3초(친구 섬 24개 × 0.1~0.6초, CPU 4배 느리면 22초) 멎는 것을 확인. CPU 프로파일에서 `tierBand`가
    반지름마다 `edgesAt`(각도만의 함수)을 다시 계산하는 것이 72%였다. ① `tierBand`·`spotOnShore`가 각도당 한 번만 구해 `profileAt(r, th, E)`로 넘긴다
    — 훑는 눈금은 그대로라 식생 위치는 한 자리도 안 바뀐다(하네스로 25개 섬 1862개 대조). 섬당 213→47ms, 진입 5.3→1.3초(CPU 4배 22→5.4초).
    ② `vegCached`: 시드|detail|어휘 서명 키로 배치 결과를 메모리에 둔다(섬당 ~18KB, 상한 120개 ≈ 2MB). 바다↔친구 섬을 오가는 두 번째 진입은 배치 0번.
    ③ 같은 날 저녁, 조각 생성: `buildVegetationGen`(어휘 하나·중립 8개마다 yield)을 바다 화면 `buildStep`이 프레임 예산 `SEA_BUILD_MS`(8ms) 안에서만
    돌린다. 가까운 섬부터(refreshNear 거리순), 안 만들어진 섬은 원판, 원판→온전한 섬으로 바뀔 때 식생이 `SEA_GROW_MS`(450ms) 동안 자라 올라온다.
    진입 중 가장 느린 프레임 147→26ms(CPU 4배 615→71ms), 전부 서기까지 맥 2.2초·CPU 4배 7.6초, 식생 위치 동일(1862개), 두 번째 진입은 캐시로 그대로.
    진입 중 섬을 누르면 그 섬만 그 자리에서 통째로 만든다(한 번 ≤70ms). 스캔 없이 직접 계산하는 안(섬당 20~30ms)과 먼 섬 식생 축소는 보류.

### 데이터 출처
기존 코드에 이미 저장되고 있는 값을 그대로 쓴다. 새 수집 없음.

```
일기 저장 시 analysis.hits = [
  { name, label, q, val9, aro9, valStd, aroStd, token }
]
```

- `label` — 감정 이름 → 어휘 종류 판별
- `valStd`, `aroStd` — Russell 원형모형 좌표(-1~1) → **섬에서의 위치 결정**

### Russell 네 영역 ↔ 섬 식생

**영역은 방위로 나누고 층을 가로지른다** (③-7, 2026-09-05).
층마다 다른 영역을 주지 않는다. 나침반이 방위에 걸려 있고, 높이에는 이미
"안으로 내려간다"는 뜻이 있으며(§2), 층 면적이 크게 달라 영역이 층을
소유하면 어휘 분포에 따라 섬이 기운다. ③-8 랜드마크와도 자리 싸움이 난다.
**층은 같은 영역 안에서 무엇이 자라는지만 바꾼다.**

| 영역 (방위) | 1층 (물가) | 2층 | 3층 (꼭대기) |
|---|---|---|---|
| 활기 (위) | 밝은 꽃 | 열매나무 | 꽃 + 열매 |
| 평온 (오른쪽) | 물웅덩이 | 큰 나무 | 나무 |
| 슬픔 (아래) | 갈대 | 흰 들꽃 | **등불** |
| 떨림 (왼쪽) | **파도** (물가 한정) | 바위 | 바위 |

파도를 1층 물가로 못 박아 잔디 위 흰 원판 문제가 사라졌고, 등불을 3층에
올려 "어두운 감정도 섬을 아름답게 만든다"가 가장 잘 보이는 자리에 놓였다.

**한 어휘는 자기 방위의 세 층에 걸쳐 놓인다.** 어휘 하나만 있어도 그 방향이
위아래로 살아난다. 노린 층이 꽉 차면 같은 영역의 다른 층으로 옮긴다 —
층보다 방위가 중요하다.

**어휘를 얻어도 기존 어휘가 움직이면 안 된다.** 아이가 매일 다른 섬을 보게 된다.
각 어휘의 난수는 자기 이름에서만 나오고(개수와 무관), 먼저 얻은 어휘부터
놓아 나중 어휘가 비켜난다. 자동 검사에 넣어두었다.

**중립 식생** (감정과 무관, 섬 시드로만 결정). 일기가 없는 첫날 섬도 휑하지
않아야 한다. 풀 포기·자갈·관목·바위 노두를 섬 면적에 비례해 깐다.
감정 식생을 다 놓은 뒤 남는 자리에만 깔리므로, 자란 것이 늘면 풀이 물러난다.

**중립 색은 잔디보다 밝다.** 처음에는 채도만 낮췄더니 잔디보다 어두워
풀밭의 결이 아니라 때 얼룩으로 보였다. 명도를 잔디(grassHigh, 약 177) 위로
올리고 노란기를 준다. 자갈·노두도 검은 돌이 아니라 회백색이다.

**중립과 자란 것은 크기와 형태로 갈린다.** 중립은 높이 0.22 이하이고
발자국 0.30 이하다. 감정 식생은 그 둘을 동시에 만족하지 않는다(등불만
예외 — 낮고 작지만 발광한다). 채도로 가르던 것은 버렸다. 중립을 밝게
만들면서 채도가 같이 올랐고, 애초에 어두워서 문제였지 채도 탓이 아니었다.
자동 검사가 실제 메시 높이를 재서 확인한다. 이 검사가 떨림 영역의 바위를
잡아냈다 — 낮은 자갈이라 중립과 구분되지 않아 바윗덩이로 키웠다.

**소품은 식생이 아니다** (2026-09-12). 말뚝·울타리·이정표·벤치·나무통은 감정과 무관하고 어휘 수와도
무관하다. 붉은 나무색과 직선·판 형태로 식생과 갈린다(§4 색 규칙). 등불은 슬픔 3층의 식생이라 소품으로
쓰지 않는다.

**가늘고 세로로 긴 것은 더 떨어뜨린다.** 갈대·흰 들꽃은 최소 간격을
0.6이 아니라 1.05 / 0.85로 준다. 가까이 모이면 병정처럼 줄지어 선다.
무리 안에서도 키·퍼짐·기울기를 개체마다 크게 달리한다.

**걷는 길을 막지 않는다.** 식생끼리 최소 간격 0.6(캐릭터 지름 0.4 + 여유)을
두어 어디든 비집고 지나갈 틈이 남는다. 계단 발치·상단 반경 1.2, 부두
진입부 1.0은 비운다. 랜드마크도 `userData.keepOuts`에 더하면 배치기가 피한다.

**출발점 둘레는 반경 0.45를 비운다** (2026-09-12). 아이가 로그인해서 처음 보는 자리인데
갈대가 다리를 관통하고 노두가 몸통을 뚫고 나왔다(renders 211·214). 길 마당(0.32) +
식생 여유(0.1)로는 발자국보다 넓게 퍼지는 종류(갈대 줄기 퍼짐 0.26·노두 반폭 0.30)가 닿는다.
0.45 = 캐릭터 반지름 0.2 + 퍼짐 초과분 + 여유. 출발점은 `buildIsland`가 길을 낼 때 정한 점
하나(`userData.start`)이고 `IslandView.freeStart`가 그 점을 그대로 쓴다 — 전에는 같은 규칙을
두 곳에 따로 적어 두었는데, 여유 구역이 기준점과 어긋나면 빗나가므로 하나로 합쳤다.
확인(TEST 8-5): 시드 40개 × 합성 어휘에서 식생 가장자리가 출발점에서 발자국 기준 0.466,
눈에 보이는 퍼짐 기준 0.321 이상 — 캐릭터 반지름 0.2 밖이다.


섬은 **정사각 사분면이 아니라 자연스러운 섬 모양**으로 하되,
방향에 따라 식생이 다르게 자란다.
식생은 **층 위 평지에 놓인다** (2026-09-05). 경사면에 박혀 있으면 안 되고,
걸어다니는 길을 막아서도 안 된다.

| 영역 (좌표) | 감정 예시 | 섬에서 |
|---|---|---|
| 활기 (정서가+, 각성+) | 행복, 설렘, 뿌듯 | 밝은 꽃, 열매나무 |
| 평온 (정서가+, 각성−) | 편안, 감사, 고요 | 큰 나무, 잔디, 물웅덩이 |
| 슬픔 (정서가−, 각성−) | 서운, 외로움, 우울 | 물가 갈대, 안개, **등불** |
| 떨림 (정서가−, 각성+) | 짜증, 화남, 불안, 긴장, 부끄러움 | 바위, 파도 |

**어느 영역이 무성한지가 눈에 보인다는 것이 핵심이다.**
아이가 "내 섬은 이쪽이 무성하네"를 스스로 알아차리는 것이 성찰의 계기가 된다.
감정 분석 그래프보다 직관적이어야 한다.

### 화면 안내 문구 (③-8)
- 나침반 위: **"어느 마음이 많이 자랐나요?"** — 네 단어만 있으면 무엇을
  보라는 건지 알 수 없다. §5의 "어느 영역이 무성한지가 눈에 보인다"를
  아이 말로 옮긴 것이다.
- 어휘 수: **"내가 쓴 마음 말 N가지"**. 누르면 **"새로운 마음 말을 쓰면
  섬이 자라요. 같은 말을 여러 번 써도 늘지 않아요."**
  상시 표시하지 않는 이유는 글이 늘면 섬을 가리기 때문이다.

### 나침반의 연 모양과 축 이름 (2026-09-16)
- **연 모양.** 2026-09-16 전까지 나침반은 이름 넷과 회색 점만이어서 "어느 마음이 많이 자랐나요?"의 답이 화면에 없었다(라벨 목록으로 보였다).
  이제 네 방향으로 그 영역의 마음 말 **종류 수**에 비례한 꼭짓점을 두고 사각 연을 그린다(`compassKite`). 채움은 꼭짓점마다 QC 색이 가운데로 번지는
  방사 그라디언트 넷, 연 모양으로 클립. **숫자는 쓰지 않는다** — 형태로만. 정규화는 내 섬에서 가장 많은 영역 기준(그쪽이 원 가득): 질문이 "어느 쪽이
  무성한가"라서다. 영역 최대치 기준(활기 10·떨림 9·슬픔 6·평온 5)이면 평온이 늘 커 보인다. 0인 방향은 밑둥 0.12R만 남아 "없음"이 보이고 전부 0이면
  작은 마름모. **친구 섬은 연을 안 그린다**(§7 비교 금지 — 목록·숫자를 막은 것과 같은 이유). 원은 104→128px(폰 74 그대로). renders 549·551~553.
- **축 이름.** 세로선(활기↔슬픔) **"기운"**, 가로선(떨림↔평온) **"긴장"**(`AXIS_NAMES`, 사용자 A안). 히어로 소개 섹션과 같은 상수를 쓴다.
  이 십자는 Russell 평면을 45도 돌린 것이라 정서가·각성이 **아니다**(그 둘은 대각선). 에너지·긴장(Thayer) 축으로 읽는 것이 그림과 맞고, 영역 이름
  넷(활기·평온·떨림)이 이미 그 뜻이다. 캡션은 9px, 선 옆에. 폰(74px)에는 자리가 없어 생략. 영역 이름 넷은 그대로 둔다.
- **소개 섹션(히어로)도 같은 날 맞췄다.** 조각 팻말의 영역 이름(활기·평온·떨림)을 빼고 식생 이름만 남기고, 점선 옆에 점선 색 글자로 "기운"·"긴장".
  활기 조각 팻말은 세로 점선 위에 걸려 있어 왼쪽으로 옮겼다. 빈 물 모서리 셋에 특장점 팻말: 어휘(오른쪽 위, 수치는 `EMOTIONS_28`에서 셈 — 30가지·262개),
  비폭력대화(왼쪽 아래), 리포트(오른쪽 아래). 800×600에서는 슬픔 팻말과 겹쳐 아래 둘을 좁히고(210px) 위로 올린다(bottom 130px). 세로 화면은 조각 뒤에 쌓인다. renders 548·554.

**나침반 왼쪽은 "분노"가 아니라 "떨림"이다** (2026-09-06). 정서가 −, 각성 + 영역에는
화남·짜증뿐 아니라 불안·긴장·부끄러움·경악이 다 든다. "부끄러움"에 다가갔는데 영역이
"분노"로 뜨면 아이가 이상하게 느낀다. 이 영역의 공통점은 감정의 종류가 아니라 **마음이
가라앉지 않고 뛰는 몸의 상태**라 그 상태를 가리키는 말을 쓴다. 좌표(`EMOTIONS_28`의 `q`)는
그대로고 이름만 다르다 — 262개 사전과 기존 데이터가 걸려 있다. 나침반 색도 빨강을
버렸다(단어를 바꿔도 화난 느낌이 남는다). 교사용 리포트·차트에는 "떨림(분노·불안)"으로
병기하고 학생 화면에는 "떨림"만 쓴다. 교사 대시보드의 "분노 계열"(`PHRASES.md`)은
사분면이 아니라 감정 계열이라 그대로다.

### 절대 원칙
- **부정 감정도 똑같이 아름다워야 한다.** 슬픔·떨림 영역이 초라하거나 흉하면 안 된다.
  (슬픔 영역에 '등불'을 둔 이유 — 어두운 감정도 섬을 아름답게 만든다는 것을 시각으로 보여준다)
- 아이가 우울한 날을 기록해도 섬이 나빠지지 않아야 한다.
- 방향 표시용 나침반 또는 미니 지도를 두어, 아이가 네 영역을 인지할 수 있게 한다.

---

## 6. 바다 — 학급 온도계

- 우리 반 오늘 감정의 평균 좌표에 따라 **바다 색이 바뀐다.**
  - **맑음 ↔ 흐림**으로 표현한다. 색상(hue)은 그대로 두고 **밝기와 채도만** 바꾼다.
  - 밝은 날 → 투명하고 맑은 청록 / 무거운 날 → 짙고 차분한 남색
  - **빨강/파랑 온도계는 쓰지 않는다.** 빨강·파랑은 각성 수준(뜨겁다/차갑다)으로 읽히는데,
    여기서 보여주려는 것은 정서가(밝은가/무거운가)라 축이 어긋난다.
    또 화난 날과 신나는 날이 둘 다 빨강이 되어 구분이 안 된다.
  - **어느 쪽도 "나쁜 색"으로 보이면 안 된다.** 무거운 날의 바다도 아름다워야 한다.
- 아이가 접속하면 **바다부터 눈에 들어온다.** "오늘 우리 반이 좀 무겁네"를 설명 없이 알게 된다.
- 교사 화면의 워드클라우드와 같은 데이터를 쓴다. 새 수집 없음.

**개인은 반드시 익명.** 누가 어떤 감정인지 절대 드러나지 않아야 한다.

**계산** (⑥-1, 2026-09-06): 오늘 일기를 쓴 학생마다 정서가(`valStd`) 평균 하나 →
그 값들의 **중앙값** → `tone = 0.5 + 중앙값 × 0.35` (0.15~0.85). 평균이 아니라
중앙값인 이유는 한 아이의 극단값이 반 전체 바다 색을 흔들면 안 되기 때문이다.
**오늘 쓴 학생이 3명 미만이면 보통 날(0.5)로 둔다.** 한두 명의 기분이 반 전체
바다가 되면 그건 익명이 아니다. 각성(`aroStd`)은 쓰지 않는다.

**바다 표현 (2026-09-13).** 단색 원판 하나이던 바다에 색 변화·물결·파도선·반짝임을 넣었다. 텍스처 없이(§4)
MeshStandardMaterial에 onBeforeCompile로 색 계산 몇 줄만 끼운다 — 조명·그림자·안개·온도계 톤·안개 보정이 그대로 돈다.
정점은 파도선 링만 움직이고(바깥 가장자리 숨 쉬기) 나머지는 프래그먼트뿐이라 그림자 지도가 어긋나지 않는다.

| 층 | 무엇 | 값 |
|---|---|---|
| 깊은 물 원판 | 잔물결(사인파 셋, 파장 2.6·4.3·7.0, 초당 0.25쯤, 밝기 ±9% — 처음 ±4%는 정지 화면에서 켠 판과 끈 판이 구분되지 않았다) + 반짝임(사인 곱 20제곱·세기 0.45, 빛 오는 쪽 마스크, 느린 무리 마스크) | 기준색 `seaDeep` #2fa4c4 |
| 해안 띠 | 해안선 표본(`waterT`) 그대로 물가에서 2.4 바깥까지, 물가색→얕은색→깊은색, 바깥 끝 투명 | `seaShore` #cfe9dc · `seaShallow` #86d9de — 톤 반응은 절반(밝은 날 흰색이 되던 것) |
| 파도선 | 링 둘(위상 반대)이 **띠째로** 해안 각도마다 위상을 달리해(aAng×2) 5초 주기로 섬 쪽으로 0.18 밀려들었다 물러난다. 밀려들 때 모래 위로 0.10 떠서 안 묻히고 짙어지며(0.75−0.25·push), 바깥 폭(0.08·0.16)도 같이 숨 쉰다. 바깥 끝 투명 | `foam` 0.88/0.96/0.96(흰 포말) |
| 얕은 물 빛 무늬 | 해안 띠 프래그먼트에 사인 둘을 교차시킨 그물(코스틱), 천천히 일렁이며 깊어질수록(aT 0.15→0.75) 사라진다. 세기 0.16, 무거운 날은 같이 준다 | 배너 바닥 무늬와 이어짐 |
| 젖은 모래 띠 | 해안선 안쪽 0.6까지 지형 높이를 따라 3줄로 깐 링, 이 섬 모래색 ×0.55. 포말과 같은 위상의 파도 앞선이 닿은 곳이 어두워지고 1.5초 늦은 앞선을 겹쳐 물러난 뒤에도 잠깐 젖어 있다 마른다. 온전한 섬(detail ≥ 0.75)만 | 최대 불투명 0.5 |
| 바다 화면 얕은 물 | 섬마다 원(8.6) 인스턴스, 중심 거리로 같은 그라데이션. 대용 섬은 이것만, detail 0.75 미만은 파도선 정지·젖은 띠 없음 | |

세 색은 첫 화면 배너 물색(물가 #cbece7·얕은 곳 #a0ede8·중간 #3fc8e3·먼 곳 #16a1d7)에서 잡았다. 무거운 날은 물결 진폭·반짝임도 같이 준다.
**스위치** `SEA_FX` — URL 해시 `#ripple=0` `#sparkle=0` `#foam=0` `#caustic=0` `#wet=0`, `#seafx=0`(전부), `#fxauto=0`(자동 축소 끔). HUD에 `fx …`로 상태가 뜬다.
**자동 축소** 두 화면의 fps 측정이 시작 5초 뒤부터 3초 연속 20 미만이면 uniform 셋을 0으로(재질 재생성 없음, HUD `fx auto-off`).
기준 20은 크롬북 실측 21fps(2026-09-07, 흙길·베벨·소품 전) 바로 아래 — 학교 크롬북에서 다시 재야 한다.
비용(하네스 2026-09-13): 내 섬 98 calls·37k tri(전 36k), 성능 확인 모드 섬 30개 1880 calls·791k tri(전 1805·768k), 둘 다 60fps. renders 389~405(397부터 2판, 402부터 코스틱·젖은 모래).

**안개 색은 그려서 잰다.** 안개는 조명을 받은 바다 색과 같아야 먼 바다에 띠가
안 생긴다. 재질 색에 배율을 곱해 맞추려 했더니 반사광 때문에 톤마다 어긋났다.
섬이 없는 바다 한 조각을 4×4로 그려 읽어 오면 정확하다.

---

## 7. 친구 섬 — 공감과 어휘 학습

### 이동
- 바다에서 친구 섬을 **탭하면 배가 항해**한다. (직접 조작 아님)
- 이유: 아이가 길을 잃으면 안 된다. 3초 안에 도착해야 한다.

### 바다 화면 (⑥-1, 2026-09-06)

**섬 30개는 내 섬을 가운데 두고 거리순 고리 세 겹**으로 놓는다 (반지름 26 / 45 / 64,
고리 안 간격 18.5). 격자는 어느 줄이 앞인지 화면에서 안 읽히고 30개면 뒤가 앞에
가려진다. 고리는 아이소메트릭에서 자연히 앞뒤로 흩어진다.
- 자리는 **학생 id에서만** 정해진다. 매일 같은 자리여야 "저 섬이 누구 섬"이 기억된다.
- id가 작은(먼저 만든) 학생부터 놓아 나중에 들어온 학생이 비켜난다. 기존 자리가 안 움직인다.
- 첫 고리가 26인 이유: 부두 출항이 섬 밖 7.0(중심에서 약 16)에서 끝나는데, 21이면
  그 방향에 선 이웃 섬 해안에 배가 얹혔다(실제로 그랬다).
- 친구 섬은 내 섬과 **같은 함수·같은 시드**(`buildIsland` + `buildVegMeshes`)로 그린다.
  데이터는 `app/isle/<id>`.

**친구 찾기는 세 가지를 겹친다.** 30명이면 눈으로 훑기 어렵다.
- **탭** — 섬을 탭하면 바로 항해한다(§7).
- **이름 검색** — 위 검색 칸. 고르면 카메라가 그 섬으로 가고 물 위 고리가 빛나며,
  아래 배너("○○의 섬 · 배 타고 가기")가 곧 버튼이다(③-8과 같은 방식).
- **명단** — 이름순 목록. 검색과 같은 동작. 저학년은 이름을 치기보다 고르는 게 빠르다.
- **이름표는 기본 켬**(`#tags=0`으로 끔). 화면 크기가 대충 일정하게 확대·축소를 따라 줄고 커진다.
  내 섬 이름표는 같은 크기에 청록 테두리로만 구분한다 — 크기로 강조하면 가까이서 화면을 가로지른다. 누가 누구 섬인지 보여야 "친구 섬에 놀러 간다"가
  된다. 학생 수만큼 캔버스 텍스처가 생기므로 성능 카드에는 남긴다.

**출항을 이어받는다.** 섬 화면이 `onGo("sea", { boatDir })`로 넘기면 바다 화면은
같은 카메라·같은 자리에서 **배가 이미 움직이는 채로** 시작하고, 1.7초에 걸쳐
카메라가 물러나며 이웃 섬이 들어온다. 화면이 바뀌었는데 배가 멈춰 있으면 연결이
끊긴다. 덮개도 섬 화면에서 들어온 방향 그대로 걷힌다.

**항해**는 그 섬 부두 반대편(그 섬 배의 반대쪽)에 댄다. 도중에 섬이 있으면 옆으로
돌아간다 — 배가 섬을 뚫고 지나가면 그림이 깨진다. 1.4~3.0초. 도착하면 카메라가 그
섬을 화면에 담고, ⑥-2 전까지는 "준비 중" 카드가 뜬다.

**돌아오기는 어디서든 한 번.** "내 섬으로" 버튼이 항상 보이고, 도착 카드에도 있다.
돌아오면 캐릭터는 **부두 앞**에 선다(`CHAR_LAST`에 부두 자리를 넣는다) — 배에서 내린
자리다. 출항 중 캐릭터 위치(바다 위)를 그대로 남기면 물 위에 서게 되므로 그때는 남기지 않는다.

### 섬마다 주어진 것 (2026-09-06)

30개가 같은 틀에서 찍어낸 것처럼 보였다. 시드가 바꾸는 것이 해안선 굴곡과 부두·계단
각도뿐이었기 때문이다. **주어진 것은 땅·물·색이고, 만든 것은 그 위에 자란 것이다.**
아래는 전부 `isleTraits(seed)`에서 나오고 감정 어휘와 무관하다. 어휘 식생의 표식
(꽃·열매나무·큰 나무·물웅덩이·갈대·흰 들꽃·등불·바위·파도)과 겹치는 것은 쓰지 않는다.
특히 **바위**(떨림)와 **둥근 물**(평온·잠수)은 물가 장식으로도 넣지 않는다.

| | 범위 | 비고 |
|---|---|---|
| 반지름 | 5.7 ~ 6.6 | 하한이 걱정이었다. 400개 검사 통과 |
| 위층 치우침 | 최대 1.2 (3층은 1.35배) | 효과가 가장 크다. 치우친 쪽은 절벽, 반대쪽은 넓은 평지 |
| 층 비율 | 3층 0.26~0.40, 2층 0.52~0.66 | 뾰족한 섬 / 펑퍼짐한 섬 |
| 해변 폭 | 1층 가장자리 0.74~0.88 | 넓은 모래밭 / 잔디가 물가까지 |
| 잔디 | 색상 ±12°, 명도 −0.09~+0.03 | 채도는 안 건드린다. ±7°로는 지도에서 초록 덩어리 반복이라 넓혔다. 상한 +0.03은 중립 식생(관목 밝기 181)이 잔디(169)보다 밝아야 해서다 |
| 모래 · 흙벽 | 모래 색상 ±5°·명도 ±0.02, 흙벽 명도 ±0.06 | 모래 0.91에서 붉은 채널이 잘린 적이 있어 좁게. 가장 밝은 시드에서 클리핑 확인 — 없음 |
| 모래톱 | 물가에 0~2개 | 섬 밖 물속이라 걷기·식생에 영향 없음 |
| 중립 식생 밀도 | 0.7 ~ 1.3배 | 우거진 섬 / 휑한 섬 |

**층 개수는 바꾸지 않는다.** 광장 표지·식생 규칙·걷기·계단·다리가 전부 3층 전제다.
층 높이·계단·캐릭터 크기는 아이 몸 기준 절대값이다.

**폭 잠금은 우선순위가 있어야 한다** (검사에서 잡힌 것). 층을 치우치면 반대쪽에서
2층 반지름이 1 아래로 내려가는데, 예전처럼 안쪽(E3)을 깎기만 하면 3층 하한 0.55에
걸려 2층 폭이 0.17까지 눌렸다. 순서를 **걷는 폭(1·2층 0.85) > 해변 > 3층 크기**로
바꿨다 — 안쪽을 밀어 올리고, 그래도 모자라면 해변을 줄이고, 마지막에야 3층을 깎는다.

**시드 400개 검사**는 하네스 페이지에서 `buildIsland`를 400번 돌려 1·2층 최소 폭
(허용치 0.835 — `tierBand`가 0.01 간격이라 잠금 0.85가 0.84로 찍힌다), 3층 존재,
세 랜드마크 도달 가능성(0.22 격자 BFS, `walkStep`)을 본다. c·b·d+e 각 단계에서
전부 통과, 도달 불가 0.

**끌어서 둘러보고 휠/두 손가락으로 확대·축소**한다. 확대 범위는 섬 화면 반폭의
1.05배(한 섬)에서 72(30개 전체)까지.

`#sea` 해시로 부두를 거치지 않고 바로 열 수 있다(측정용). `#dev`이면 `window.__sea`로
콘솔에서 `focus(id)` / `sail(id)` / `zoom(v)` / `home()`를 부를 수 있다.

### 성능 카드 (크롬북 측정용)

A·B·D·F는 켜고 C·E는 끈 상태가 기본이다 (2026-09-06, near=0을 브라우저에서 확인한 뒤 결정).
크롬북에서 URL 해시로 하나씩 바꾸면서 어디까지 감당되는지 잰다. 재배포 없이 그 자리에서 조합을 바꾼다.

| 카드 | 켬(기본) | 끔 | 잃는 것 |
|---|---|---|---|
| A 먼 섬 단순화 | `lod=2` — 고리별 detail 0.75 / 0.5 / 0.35 | `lod=0` | 먼 섬의 계단·다리·잠수 지점·광장 표지가 빠진다 |
| B 안개 | `fog=1` — 안개 너머는 아예 안 그린다 | `fog=0` | 바다의 넓이. 30명이 한 반이라는 감각 |
| C 동시 렌더 제한 | **`near=0`(끔)** — 30개 다 그린다 | `near=12` — 가까운 12개만 온전히, 나머지는 대용 섬 | 찾아가는 맛. 근처만 보이는 게 이상해 기본은 끈다 (2026-09-06 브라우저 확인, 여유 있음) |
| D 그림자 | `shadow=0` | `shadow=1&sm=2048` | **가장 크게 잃는다.** §4 "인상의 절반이 조명". 마지막에 끈다 |
| E 이름표 | **`tags=1`(켬)** | `tags=0` | 누가 누구 섬인지 모르면 "놀러 간다"가 아니라 목록에서 고르는 것이 된다. 기본은 켠다 |
| F 픽셀비 | `dpr=1` | `dpr=2` | 가장자리가 거칠다. 잃는 게 제일 적다 |

전부 끈 것(최고 화질): `#sea&lod=0&fog=0&shadow=1&sm=2048&dpr=2`.
하네스 실측(30개 전체 화면): 기본(lod=2) 1489 calls · 515k tri, 전부 끔 1924 calls · 622k tri.
왼쪽 위 HUD가 fps · calls · tri와 카드 상태를 함께 보여준다.
드로우콜이 많으면 CPU, 삼각형이 많으면 GPU 쪽이다.

**크롬북 실측 (2026-09-07) — 기본 설정 그대로 간다.** 학교 크롬북에서 잰 결과다.

| 화면 | 결과 |
|---|---|
| 내 섬 | 21fps, 끊김 없음 |
| 바다 (30개 전부 + 이름표, 기본 카드) | 60fps, 잘 작동 |
| 첫 화면 | 금방 뜬다 |
| `sm=1024` · `shadow=0` · `dpr=1` | 하나씩 켜 봤지만 **차이 없음** |

성능 카드를 켤 이유가 없다. 카드와 HUD는 나중에 기기가 바뀔 때를 위해 남겨 두되,
기본값은 손대지 않는다. 내 섬이 바다보다 낮은 것은 그림자·식생 밀도가 가까이서
온전히 그려지기 때문이고, 21fps에서도 걷기와 전환이 매끄러웠다.
§1의 "성능은 주요 병목이 아니다"가 실측으로 확인됐다. 후처리(bloom 등)를 붙일 때만 다시 잰다.

섬은 **프레임당 하나씩** 만든다. 30개를 한꺼번에 만들면 화면이 멎는다. 만들기 전에는
대용 섬이 서 있고, 데이터가 오면 물속에서 떠오른다. 대용 섬은 밋밋한 접시가 아니라
계단식 3층·흙벽·나무 몇 그루를 시드로 조금씩 달리한 것이다(드로우콜 5).

**인스턴스 메시는 섬 단위로 컬링한다.** 인스턴스 메시의 경계 구는 인스턴스 하나
크기라, 섬 중심이 화면을 벗어나는 순간 식생이 통째로 사라진다. `frustumCulled`를
끄고 섬 중심을 화면에 투영해 보이는 섬만 그린다.

### 친구 섬에서 하는 일

```
1. 친구 섬에 도착한다
2. 내 섬에는 없는 감정 어휘를 발견한다 → 뜻과 설명을 본다
3. "나도 그래"를 누른다 → 친구 섬에 등불이 켜진다
```

여기서 끝. **어휘를 목록에 담거나 심는 동작을 따로 만들지 않는다.**
친구 섬에서 새 어휘를 보고 뜻을 알게 되는 것 자체가 학습이다.
그 뒤 아이가 일기 쓸 때 자연스럽게 그 말을 쓰게 되고,
쓰면 원래 규칙대로 내 섬이 자란다. 앱이 따로 시킬 필요가 없다.

### 왜 "좋아요"가 아니라 "나도 그래"인가
- 좋아요는 **평가**다. "서운함"에 좋아요를 누르는 건 말이 안 된다.
- "나도 그래"는 **공감**이다. 부정 감정에도 자연스럽고,
  누르는 순간 아이가 자기 경험을 떠올리게 된다.

### 조건
- **같은 영역의 감정을 기록한 적 있는 아이만** "나도 그래"를 누를 수 있다.
  - 어휘는 Russell 두 축(정서가 긍정/부정 × 각성 높음/낮음)으로 **네 영역**에 분류된다.
  - 판정은 이미 저장 중인 `valStd`, `aroStd` 좌표의 부호로 하면 된다.
    (예: 친구 섬의 "서운함"이 슬픔 영역이면, 나도 슬픔 영역 감정을 기록한 적 있어야 누를 수 있다)
  - 어휘가 정확히 같을 필요는 없다. "서운함"과 "외로움"은 다른 말이지만 같은 영역이므로 공감이 성립한다.
  - → 거짓 공감을 막으면서도, 흔치 않은 감정이 외면받지 않게 한다.
- **같은 마음 말에는 하루 한 번** (몰아주기 방지). 2026-09-16 전에는 친구마다 하루 한 번이었다 — 다른 마음 말에도 보낼 수 있게 풀었다(사용자)
- 보낸 사람 **이름 표시** (익명이면 책임감이 없어지고, 관계 형성이 목적)

### ⑥-2에서 정한 것 (2026-09-06)

설계서와 코드가 어긋난 곳 셋을 먼저 바로잡았다. **뜻 사전이 없었다** — `EMOTIONS_28`은
이름·키워드·좌표뿐이라 30개 어휘의 뜻을 새로 썼다(`WORDS.md`가 원본, 코드의 `EMO_MEANINGS`로
옮긴다. 학생이 직접 입력한 어휘는 "○○이가 직접 쓴 마음 말이에요"). **일기 가리기 기능이 없었다** —
섬 단위로 새로 만들었다. **등불이 겹쳤다** — 슬픔 3층 표식이 등불이라 공감 표식으로 못 쓴다.

| | 결정 | 이유 |
|---|---|---|
| 도착하면 | **걸어다닌다.** 배가 부두에 닿고 내려서 걷는다(출항의 거꾸로) | 발견은 걸어가서 마주치는 것이지 목록을 훑는 게 아니다. 걷기 가능성은 시드 400개 검사로 확보했다 |
| 잠수 지점 | 있되 **닫혀 있다.** 사다리를 걷어 올리고 "친구의 안으로는 들어갈 수 없어요" | §2 "수면 아래 = 안에 있는 나"를 세계 안에서 말하는 자리. 없애는 것보다 닫힌 편이 뜻이 분명하다 |
| 드러나는 것 | 어휘 종류 전부. 단 아이가 **말 단위로 가릴 수 있다**(내 섬 배너의 "친구에게 안 보이게" → `isle/<id>/v/<키>/h`). 내 섬에서는 그대로 보인다 | §5대로 섬은 "요즘 기분"이 아니라 "알게 된 말의 지도"다. 그래도 짐작은 되므로 선택권을 둔다 |
| 절대 안 보이는 것 | 일기 본문, 횟수, 날짜, 오늘의 감정, 상황, 살펴봐주세요 여부. 나침반 위 자기 성찰 문구도 친구 섬엔 없다 | |
| 가린 말 | 식생은 어휘 전체로 놓고 그리기 전에만 뺀다 | 놓기 전에 빼면 나머지 어휘가 자리를 옮긴다(§5 "기존 어휘가 움직이면 안 된다") |
| 공감 대상 | **특정 어휘(식생).** 다가가서 배너의 "나도 그래" | 조건(같은 영역을 기록한 적 있는 아이만)이 어휘 단위라야 성립한다 |
| 조건에 안 맞을 때 | 버튼을 숨기지 않고 **이유를 보여준다** — "이 마음은 아직 써 본 적이 없어요. 써 본 뒤에 누를 수 있어요" | 숨기면 왜 어떤 말엔 버튼이 있고 어떤 말엔 없는지 모른다. 규칙 자체가 학습이다 |
| 횟수 | **같은 친구의 같은 마음 말에는 하루 한 번, 다른 말이면 여러 번**(2026-09-16). 키 `<날짜>_<보낸사람>_<어휘키>`가 데이터에서 강제한다. 막힌 말에는 "이 마음에는 이미 했어요" | 하루 통틀어 한 번이면 보내는 총량이 줄어 못 받는 아이가 늘어난다. 전(친구마다 하루 한 번)에는 두 번째 마음에 공감할 길이 없었다. 반딧불은 어휘당 3·섬당 8 상한이 있어 화면은 늘지 않는다. 저장은 정리하지 않는다(항목 60바이트쯤, 25명 학급 1년 1.5MB 미만) |
| 받은 표식 | **반딧불.** 공감받은 어휘의 식생 위에 떠다닌다. 어휘당 3개, 섬당 8개, **최근 7일** | 움직여서 세어지지 않고 하나만 있어도 예쁘다. 영구 누적이면 인기 있는 아이의 섬이 해가 갈수록 빛나고 그게 곧 순위다 |
| 보낸 사람 이름 | **받은 아이만, 그날 것만** 본다("오늘 ○○, △△이가 나도 그래 했어요"). 구경하는 아이에겐 반딧불만 | 이름이 보이면 "누가 누구한테"가 반 전체에 지도로 드러난다. 7일치 이름은 목록 길이가 곧 점수다 |
| 돌아오기 | **부두("바다로") + "내 섬으로" 버튼 둘 다.** 버튼은 배가 자동으로 내 부두까지 항해한다 | 어디서든 한 번(§7)이되 순간이동은 아니다. 축의 뜻(§3)을 지킨다 |
| 안내·입력(2026-09-15 저녁) | **친구 섬 투어 2단계**(왼쪽 위 팻말, 한 번 보면 "?"): 식생 앞까지 걷기 → 배너의 "나도 그래". 도착 카드는 두지 않는다(투어와 같은 말 두 번). **터치 방향 패드**(오른쪽 아래, coarse pointer에서만) — 걷기가 방향키뿐이라 태블릿에서 친구 섬이 통째로 죽어 있었다. 배너 문구는 판정(같은 영역)에 맞춘다: "같은 쪽(슬픔) 마음을 아직 써 본 적이 없어요", 보낸 뒤엔 어떤 말에 보냈는지 | 친구 섬에 세계 안 안내가 0개였다. 원칙(개수·이름 없음)은 그대로 |
| 바다에서 고르기(2026-09-15 저녁 · 2026-09-16 더블클릭) | 섬을 **한 번** 누르면 카드, **같은 섬을 400ms 안에 두 번** 누르면(더블클릭·더블탭) 카드 없이 바로 출발(`SEA_DBL_MS`). 조작 안내·바다 투어 문구도 "두 번 누르면 바로 출발". 전(2026-09-15 저녁): 탭하면 **바로 떠나지 않는다** — 카메라가 그 섬으로 가고 링이 켜지고 카드 "○○의 섬 [배 타고 가기] [✕]"가 뜬다. 오늘의 섬·이름 찾기·탭이 전부 같은 길. 부두 전환은 판자 걷기 → 승선(770ms) 뒤에 덮개, 전체 1.4초 | 잘못 눌러도 가 버렸다(§7의 "탭하면 항해"를 고친 것). 덮개가 지연 중에 처음부터 다 덮이던 CSS 버그(fill-mode)도 함께 |
| 내 마음 말 목록(2026-09-15 저녁) | 내 섬 어휘 수 칩을 누르면 영역별 목록, 말을 누르면 뜻. 개수는 제목에 없다. **친구 섬은 목록 없음** — 설명 한 줄만. **친구 섬 칩에는 숫자도 없다**("윤서의 마음 말"). 내 섬 칩의 숫자("내가 쓴 마음 말 15가지")는 둔다 | 친구 말은 걸어가 하나씩 본다(발견). 목록으로 늘어놓으면 종류·수를 한눈에 비교하는 표가 되고, 숫자는 그중 가장 비교하기 쉽다(공감의 비교 금지와 같은 이유). 자기 것을 보는 것은 비교가 아니다 — 어휘가 느는 것을 스스로 아는 것이 이 앱의 목적 |
| 내 마음 말 표(2026-09-16) | 위 목록을 **나침반 모양 3×3 표**로: 활기 위·떨림 왼쪽·평온 오른쪽·슬픔 아래, 가운데 칸에 제목과 수. 칸 크기가 같아 어느 칸이 붐비는지가 보인다. 빈 영역은 점선 칸에 "아직 없어요"(표 모양이 늘 보이게). 폰(<560)은 세로 목록 그대로. 패널 폭 460px. renders 550 | 사용자 ㉠(2026-09-16) |
| 보내는 쪽 연출(2026-09-15 밤) | "나도 그래"를 누르면 내 캐릭터에서 반딧불 하나가 곡선으로 0.8초 날아가 그 식생에 앉는다 + 짧은 "휙"(woosh 2.4배). 토스트는 그대로. 움직임 줄이기면 바로 앉는다. 반딧불 지오메트리·재질은 인스턴스 반딧불과 한 벌 | 누르면 토스트와 점 하나뿐이라 "보냈다"보다 "저장됐다"였다. draw call은 날아가는 동안 +2, 개수·이름은 여전히 없다 |
| 받는 쪽 알아채기(2026-09-15 저녁) | 지난 방문 뒤 새 반딧불이 있을 때만 한 줄 "반딧불이 왔어요. 빛나는 꽃에 가 보세요"(개수·이름 없음), 새 것만 헤일로 크게. 기준은 기기 localStorage(마지막 본 공감 시각). 없으면 아무 말 없음 | 실시간 알림이 아니라 섬을 열 때 한 번 읽은 값이다. 노란 점이 무엇인지 몰라 "닿았다"가 끊겨 있었다. 어휘 수 칩 말풍선에 "반딧불은 친구가 '나도 그래' 한 마음" 범례 |

**소외를 막는 장치.** 공감 기능이 있는 한 못 받는 아이는 생긴다. 막을 수 있는 것은 셋이다 —
차이가 **보이지 않게**, **비교되지 않게**, **분포가 쏠리지 않게**.
- 아이 화면 어디에도 개수가 없다. 반딧불은 움직이고 상한이 있고 7일이면 사라진다.
- **바다 지도에서는 반딧불이 안 보인다.** 섬에 내려야 보인다. 지도에서 보이면 30개를 한눈에 비교하는 화면이 된다.
- 빈 상태 문구를 쓰지 않는다. "아직 아무도 안 왔어요"는 없다. 반딧불 없는 식생은 그냥 식생이다.
- **오늘의 섬.** 바다에 "오늘은 ○○의 섬에 놀러 가 볼까요?" 하나가 뜬다. 최근 7일 받은 공감이 적은
  아이에게 기울여 뽑는다(가중 추첨, 시드는 나+오늘). 아이는 개수를 못 보고 아이마다 다른 섬이 뜨므로
  "추천된 아이 = 못 받는 아이"로 읽히지 않는다. 강제가 아니고, 안 누른 것을 기록하지 않는다.
- **교사에게만 보인다.** 대시보드 '살펴봐 주세요' 아래에 "받지 못한 학생 / 보내지 않은 학생"(최근 7일,
  이름만). 보내지 않은 쪽을 같이 보는 이유는 소외가 양쪽에서 시작되기 때문이다. §9의 선대로 정보까지만.
  **이름은 소수일 때만 보인다** — 반의 절반 이하이면서 8명 이하. 넘으면 "아직 대부분 주고받지 않았어요"
  한 줄로 대신한다. 29명이 나열되면 명단이지 정보가 아니다(2026-09-06).

**인증이 없어 콘솔로는 개수를 알 수 있다. 화면에는 안 보인다.** 개수는 저장하지 않고 읽는 쪽이
셀 뿐이며 아이 화면은 세지 않는다. 그러나 방문자가 반딧불을 그리려면 친구의 `app/emp`를 읽어야
하므로 규칙으로 막을 수도 없다. **완전 차단은 인증이 필요하고 이 단계 밖이다** (2026-09-06, 수용 결정).
기록 접근 통제와 계정 정보 보호를 나누는 기준은 §13.

**동시 쓰기.** 30명이 같은 데이터를 건드리는 유일한 기능이다. 원칙 하나 — 읽고-고쳐-쓰기가 없다.
모든 쓰기는 자식 노드 하나를 `update()`로 놓는 것이고 키가 서로 다르므로 겹치지 않는다.

| 동작 | 경로 | 방식 |
|---|---|---|
| 나도 그래 | `app/emp/<받는 id>/<YYYY-MM-DD>_<보낸 id>_<어휘 키>` = `{ n, q, l: 어휘 키, ts }` (2026-09-16. 전: `<날짜>_<보낸 id>`, 옛 키도 그대로 읽힌다) | 부모에 `update`. 같은 순간 두 아이 → 키가 달라 둘 다 남는다. 같은 아이가 같은 말에 두 번 → 같은 키를 덮어 하루 한 번. 읽을 때는 앞 두 조각만 키에서 떼고 어휘는 값의 `l` |
| 반딧불·이름 | `app/emp/<내 id>` / `app/emp/<친구 id>` | 섬을 열 때 **한 번** 읽는다. 실시간 리스너를 달지 않는다 — 보는 중에 늘면 그게 알림이 되고 알림은 기다리게 만든다 |
| 가리기 | `app/isle/<내 id>/v/<키>/h` | 내 노드에만 `update`. `isle/$studentId` 규칙이 하위 트리를 덮어 규칙 추가 없음 |
| 오늘의 섬 · 교사 | `app/emp/<반 전원>` | 읽기만(30번). 개수를 저장하지 않는다 |

방문자가 쓰는 곳은 `emp` 한 곳뿐이다. 친구의 `isle`에는 절대 쓰지 않는다.
날짜 키는 기기 시간이라 시계가 틀린 기기는 최악의 경우 하루 두 번이 된다. 받아들인다.

### 절대 원칙
- **개수를 세지 않는다.** "받은 응원 12개"가 뜨면 인기투표가 된다.
  등불이 하나둘 켜지는 식으로만 표현한다.
- **친구의 일기 내용은 절대 볼 수 없다.** 감정 어휘와 섬의 분위기만 보인다.
- **어휘를 배울 기회가 한쪽에 몰리지 않아야 한다.**
  기존 게임(탐험 연습하기)이 주된 학습 경로로 유지된다.
  친구 섬은 추가 경로일 뿐이며, 친구가 찾아오지 않아도 불리해지지 않아야 한다.
- 아이가 **특정 감정을 섬에 드러내지 않을 선택권**이 있으면 좋다.
  (기존 일기 '가리기' 기능과 같은 발상)

---

## 8. 광장 섬

- 중앙에 위치.
- **오늘의 질문**: 교사가 필요할 때만 올린다. 상시가 아니다.
  - 올라오면 **팝업으로 알림**
  - 예: "요즘 우리 반에서 고맙다고 말하고 싶은 순간이 있었나요?"
  - 답변은 **한 칸에 자유롭게** 쓰고 마음 말 칩을 골라 붙인다(2026-09-16 개편. 전: NVC 관찰 / 느낌 / 바람 세 칸 — 저학년에게 칸이 셋이면 무엇을 어디에 쓸지 막혔다). 옛 세 칸 답은 `plazaAnsText`가 한 글로 이어 읽는다
  - **교사가 전부 볼 수 있고 숨길 수 있어야 한다**
- **평소**: 우리 반이 오늘 많이 쓴 감정 어휘를 표시 (학급 온도계와 같은 데이터)

**자유 게시판으로 만들지 않는다.**
초등학생 자유 게시판에서는 특정 아이 지목·은근한 배제·소외가 실제로 발생한다.
감정 앱에서 이런 일이 생기면 아이가 상처받고 연구도 무너진다.

### ⑤에서 정한 것 (2026-09-07)

광장은 **"오늘"의 섬**이다. 내 섬이 누적된 어휘의 지도라면 광장은 오늘만 보여준다.
아이가 쓰는 글은 교사가 올린 질문에 대한 답뿐이고, 답은 세 칸 틀 밖으로 나가지 못한다.
아이끼리 직접 주고받는 것(댓글, 좋아요, 자유 글)은 하나도 없다. 지목과 배제는 "이름"과
"누가 썼는지"에서 생기므로 그 둘을 데이터 구조에서부터 막는다.

| | 결정 | 이유 |
|---|---|---|
| 도착하면 | **3D 디오라마 + 카드.** 고정 카메라, 걷기 없음. 질문·답·어휘는 화면 아래 카드 | 세계관이 이어지고 걷기가 없어 구현이 가볍다. 바다 지도에 광장 섬을 놓지 않는다 — 고리 배치가 내 섬 중심이라 자리가 없고 표지에서 바로 가면 한 번이다 |
| 형태 | **돌계단 마당(A안).** 물에서 바로 올라온 둥근 돌 단이 안으로 두 단 내려가 모닥불에서 끝난다. 잔디·모래·흙벽이 없고 포장돌과 나무 테두리뿐. 네 방위 기둥에 등불과 활기·평온·슬픔·떨림 **삼각 페넌트**(색 바탕·흰 글씨). 어휘 팻말은 네모 베이지 판·검은 글씨 — "슬픔"은 방위 이름이자 어휘라 둘이 나란히 서면 같은 것으로 읽혀, 모양으로 갈랐다 | 첫 판은 잔디·모래를 그대로 써서 "층 하나짜리 내 섬"으로 보였다. **내 섬은 위로 올라가 표지에서 끝나고 광장은 안으로 내려가 불에서 끝난다** — 형태만이 아니라 뜻이 다르다. 섬은 땅이고 광장은 자리다. 내려가는 두 단이 곧 둘러앉는 자리라 아무도 없어도 "모이는 곳"이 읽힌다 |
| 모인 아이들 | **오늘 답을 쓴 수만큼 캐릭터**가 불 둘레에 선다(안쪽 10 · 가운데 16 · 바깥 8). 실시간이 아니라 숫자다. 익명 키로 옷 색·키를 조금씩 달리하고, **내 것만 카메라 쪽 앞자리**에 선다. 아직 안 썼으면 앞자리가 비어 있다 | 누가 누군지는 안 보인다. 답을 쓰면 그 자리에서 하나 더 선다. 오늘 일기를 쓴 수가 아니라 답을 쓴 수인 이유 — 질문 없는 날에도 사람이 서 있으면 답을 쓰는 동기와 어긋난다 |
| 가는 길 | 내 섬·친구 섬의 **광장 표지**에서 옆으로 전환(부두와 같은 수평 연출). 돌아오면 캐릭터는 표지 앞 | 친구 섬의 표지도 같은 광장이다(반 공용) |
| 질문 없는 날 | 오늘 반이 많이 쓴 어휘가 **바깥 단 화분 12칸**에 자란다. 어휘 하나가 상자 하나, 상자 안에 사분면 식생(꽃·열매/나무/흰 들꽃·갈대/바위). 방위는 내 섬 나침반과 같고 같은 방위의 말이 그 둘레에 모인다. 순위는 상자·식생 크기로만. 카드에 칩(12개). 게시판은 없다 | 사람이 가져다 심은 것이라 "오늘 우리가 가져온 말"로 읽힌다. 12칸 격자라 **이름표가 겹치지 않는다**(자유 배치 첫 판에서 겹쳤다). 상한 12, 카드 칩도 12로 맞춘다. 매일 다시 자란다 |
| 질문 있는 날 | 게시판이 서고 카드에 질문. 안 썼으면 답하기, 썼으면 내 답 + 고치기 | |
| 수명 | **교사가 마감할 때까지** 열림. 반에 하나만 | 며칠에 걸쳐 답할 수 있다 |
| 알림 | 모달이 아니라 **한 줄 배너**(일기·내 섬 화면). 탭하면 광장. 본 질문 id는 브라우저에 남겨 한 번만 | §8의 "팝업"은 흐름을 막지 않는 것으로 해석했다 |
| 세 칸 | 한 화면에 관찰 → 느낌 → 바람, 칸당 120자. 관찰 + 느낌(칩 또는 글)은 필수, 바람은 선택 | 저학년은 바람이 어렵다 |
| 느낌 칸 | **감정 어휘 칩 + 한 줄.** 내 섬에 있는 말이 앞, 없는 말(기본 사전)이 뒤. 없는 말은 점선 테두리이고 탭하면 뜻 | 광장에서 새 말을 만나는 것이 어휘 확장의 경로. 분석 없이 사분면을 얻는다 |
| **자라는가** | **자라지 않는다.** 답변에 어휘·사분면만 남고 섬 요약에는 아무것도 쓰지 않는다. 저장 뒤 한 줄 "이 마음을 일기에도 써 보면 섬에 자라요" | 친구 섬과 같은 규칙 — **만나는 곳은 여럿이어도 자라는 곳은 일기 하나다.** 칩은 글보다 싸서 광장이 섬을 키우면 양치기가 되고, §5의 출처 규칙이 둘이 된다 |
| 이름 검사 | 저장 전에 세 칸을 반 명단과 대조한다. **세 겹**이다 — ① 세 글자 이상 이름은 그대로 찾고, **두 글자 이름은 뒤에 조사(이가·가·한테·에게·랑·은·는…)가 붙을 때만** 잡는다. ② 경고에 **"친구로 바꾸기"** 버튼("하늘이가" → "친구가"). ③ **두 번째 막히면 저장하되 숨김(s=2, nmf)**으로 간다. 아이에게는 "이름이 들어 있어 선생님이 확인한 뒤에 보여요", 교사에게는 "확인 필요" | 처음엔 무조건 막았다. 그러면 정말 그 말을 쓰고 싶은 아이(오탐)가 계속 막힌다. "두 번 누르면 그래도 저장"은 이름을 쓰려는 아이도 통과시켜 막는 뜻이 없다. **막는 기준을 "쓰기"에서 "공개"로 옮겼다** — 쓸 수는 있고, 광장에는 교사 확인 없이 절대 나가지 않는다. 내 이름은 지목이 아니라 뺀다(2026-09-07) |
| 다른 아이 답 | **교사가 고른 답만 이름 없이.** 기본은 비공개. 보이는 조건 셋 — 올라간 답이 3개 이상, 순서는 아이마다 다르게, 내 답을 쓴 뒤에만 | 한둘이면 누구 것인지 짐작된다. 같은 순서면 "첫 번째 답"이 이름이 된다. 쓰기 전에 보면 베낀다 |
| 수정 | 마감 전까지 가능. **고치면 비공개로 돌아가고 광장 사본이 지워진다**(재승인) | 승인된 내용과 원본이 어긋나면 안 된다 |
| 교사 | 대시보드 "광장" 패널. 분류별 예시에서 고르거나 직접 쓰기, "이름은 쓰지 않기" 안내 토글(타인 이해·관계와 바람은 기본 켬), 답변 목록(이름·세 칸), 상태 셋 **비공개 / 광장 / 숨김**, 마감, 지난 질문 | 숨김은 아이 화면에 "선생님이 확인하고 있어요"로 바뀌고 수정이 잠긴다 — 아이와 이야기할 계기. 삭제는 없다(기록이 판단의 근거). 새로고침으로만 읽는다 |
| 평소 어휘 | 일기 저장 때 `dayMark`가 익명 키 아래 **오늘 쓴 어휘**도 남긴다. 아이는 그날 노드 하나만 읽는다. **2명 이상이 쓴 말만**, 최대 8, 순위로 크기만, 숫자 없음. 참여 3명 미만이면 "오늘은 아직 조용해요" | 반 전원의 일기 노드를 아이가 읽게 하지 않는다. 한 명만 쓴 드문 말은 누구인지 짐작된다. 문턱은 바다 온도계(§6)와 같다. 오늘 이후 데이터부터 쌓인다 |

**저장 경로와 동시 쓰기.** 원칙은 ⑥-2와 같다 — 읽고-고쳐-쓰기가 없고 쓰기는 전부 자기 키 하나다.

```
app/plaza/<반코드>/cur                 현재 질문 id 또는 null            교사만 쓴다
app/plaza/<반코드>/q/<qid>             { t, cat, nm, ts, open, closed }  교사만 쓴다
app/plaza/<반코드>/a/<qid>/<학생id>     { o, f, fl [{l,q}], w, ts, s }    내용은 학생, s(0/1/2)는 교사
app/plaza/<반코드>/pub/<qid>/<익명키>   { o, f, fl, w }                   교사가 승인할 때 복사. 이름·id·시간 없음
app/plaza/<반코드>/n/<qid>/<익명키>     1                                 답을 쓴 아이. 카드·게시판의 "N명이 답했어요"(2026-09-16 전에는 이 수만큼 캐릭터)
app/day/<반코드>/<날짜>/<익명키>/w/<어휘키> { l, q }                       오늘 쓴 어휘(익명)
```

- 학생 30명이 같은 질문에 답해도 키가 학생 id라 겹치지 않는다. 수정은 같은 키를 덮는 것이다.
- 학생의 수정은 내용 필드 + `s: 0`(이름으로 두 번째 막힌 답은 `s: 2` + `nmf`) + `pub` 사본 삭제 + `n/<qid>/<익명키>`를 **한 `update`**로 놓는다(원자적). 교사의 승인도 `s` + `pub` 사본을 한 `update`로. 교사가 숨김을 풀면 `nmf`도 걷는다.
- 아이 광장 화면은 `pub`만 읽는다. `pub`의 키는 `anonKey`라 학생 id가 없다. `a`에는 이름이 있으니 아이 쪽 코드가 닿지 않는다.
  콘솔로 `a`를 열어 볼 수는 있다 — ⑥-2에서 수용한 것과 같은 한계.
- `dayMark`는 익명 키 노드를 통째로 놓지 않고 `v`·`a`·`w/<키>`를 자식 경로로 쓴다. 통째로 놓으면 `w`가 지워진다.
- **Firebase 규칙에 `plaza` 경로를 열어야 한다.** `day` 하위의 `w`는 기존 규칙 안이다.

### 오늘의 질문 예시

**감정 인식**
- 요즘 나를 가장 많이 웃게 한 건 뭐였나요?
- 최근에 "이건 좀 아니다" 싶었던 순간이 있었나요?
- 하루 중 마음이 가장 편해지는 때는 언제인가요?

**타인 이해** (사회정서역량과 직결)
- 친구가 어떤 행동을 했을 때 고마웠나요?
- 말은 안 했지만 속상했던 적이 있나요?
- 우리 반에서 누군가 힘들어 보였던 적이 있나요? *(이름은 쓰지 않기)*

**관계와 바람**
- 우리 반이 이랬으면 좋겠다고 생각한 게 있나요?
- 친구에게 부탁하고 싶은데 말 못 한 게 있나요?
- 요즘 누군가에게 고맙다고 말하고 싶은 순간이 있었나요?

**어휘 확장**
- 오늘 내 기분을 색깔로 말한다면?
- "기분이 좋다" 말고 다른 말로 표현해본다면?

### 질문 작성 기준
- 있었던 일과 그때 마음을 한 글로 답할 수 있는 질문이어야 한다(2026-09-16 전: NVC 세 칸).
- **상황을 묻되, 사람을 묻지 않는다.**
  - "언제 화가 났나요?" → 괜찮다
  - "누가 나를 화나게 했나요?" → 안 된다
- 특정 인물이 드러날 수 있는 질문에는 **"이름은 쓰지 않기"** 안내 문구를 함께 띄운다.

---

## 9. 교사 기능 (추가)

2차 심사에서 **"교사가 이 학생을 어떻게 해야 하는지가 없다"**는 지적을 받았다.
현재 '살펴봐주세요'는 학생 이름만 띄우고 끝난다.

**단, 앱이 교사에게 조치를 지시해서는 안 된다.**
"이렇게 하세요"가 아니라 **"이런 정보가 있습니다"**까지가 선이다.
이 선을 넘으면 "선별이지 진단이 아니다"라는 근거가 무너진다.
최종 판단과 개입은 언제나 교사가 한다.

### ① 왜 떴는지 근거 보여주기

'살펴봐주세요'에 뜬 학생 옆에 이미 저장된 데이터를 함께 표시한다. 새 수집 없음.

```
김○○  — 최근 7일 중 5일
자주 나온 감정: 외로움(3), 서운함(2)
그때의 상황: 친구(4), 학교(1)
```

담임이 "요즘 힘들어?"가 아니라 **"요즘 친구들이랑 좀 그랬어?"**라고 물을 수 있게 된다.
상담의 성패가 여기서 갈린다.

### ② 대화 시작 문구 제안

지시가 아니라 **예시**로 제공한다. 그 학생의 감정·상황 데이터에서 뽑아내므로 매번 달라진다.

```
이렇게 말을 걸어볼 수 있어요
 · "요즘 친구들이랑 지내는 거 어때?"
 · "혼자 있고 싶었던 날이 있었어?"
```

쓸지 말지는 교사가 정한다. 강제하거나 체크리스트로 만들지 않는다.

### ③ 데이터 내보내기

- **개별 학생 기록**을 PDF로 내보낸다. (상담·보호자 면담용)
- 담기는 것: 기간별 감정 분포, **마음 말이 늘어났어요**(2026-09-16), 자주 나온 감정 어휘, 상황(교사는 처음·나중 두 칸), 참여 현황. 살펴봐 주세요 이력은 넣지 않는다 — 우울 신호가 종이에 찍혀 보호자에게 가면 진단으로 읽힌다
- **일기 본문은 담지 않는다.**
  아이가 교사에게 쓴 글을 보호자가 보는 것은 다른 문제다.
  감정 분포와 어휘 수준까지만 내보낸다.

### ④ 그 외
- 광장에 오늘의 질문 올리기 / 답변 관리 / 답변 숨기기 — ✅ ⑤ (2026-09-07). 상태 비공개/광장/숨김, 마감, 지난 질문
- **공감("나도 그래")을 받지 못하는 아이 확인** — 소외 조기 발견
- **「요즘 달라진 아이」** (2026-09-14) — **2026-09-16 제거, 판정 코드 없음**(`patternShifts`·`SHIFT`·`PHRASES_SHIFT`와 카드·투어 단계를 지웠다. 수업일 셈 `shiftSchoolDayFn`만 「말 걸어봤어요」가 계속 쓴다. 시연 데이터의 사례 셋(10·12·16번)은 일기를 흔들지 않기 위해 그대로). 아래는 되살릴 때를 위한 기록이다. — "힘들어 보인다"(살펴봐 주세요)가 아니라 **"패턴이 달라졌다"**. 우울 신호가 없어도 뜨고, 작성이 끊긴
  아이는 신호가 잡힐 데이터가 없어 살펴봐 주세요에는 뜰 수 없으므로 따로 둔다. 살펴봐 주세요 아래 카드, 같은 아이가 둘 다에 떠도 된다.
  기준은 `SHIFT` 상수 셋: ① 어휘 단조 — 이전 14일 어휘 5개 이상 → 최근 7일 2개 이하(최근 2편 이상일 때만, 원래 적던 아이 제외).
  ② 작성 끊김 — 마지막 작성 뒤 수업일 3일. 주말 제외, 학급 달력이 없으니 **그날 쓴 아이가 정원의 10% 이하인 평일은 학교 안 간 날**로 본다.
  ③ 기복 — 최근 7일 중 **같은 날 안에** 긍정(활기·평온)과 부정(떨림·슬픔)이 함께 잡힌 날 3일 이상. 사분면만 다르면(즐거움+편안도 셈)
  시연 학급 30명 중 11명(37%)이 걸려 긍정·부정 갈림으로 좁혔다(`swingSplit`). 문구는 PHRASES.md「특수 상황」 그대로, 접어 두고 눌러야 펼친다.
  세로 띠는 한 색(순번 교대는 심각도로 오독된다). 시연 학급 실측: 단조 0·끊김 0(데이터에 사례가 없다 — 최근 7일 어휘 최소 3, 마지막 뒤 수업일 최대 1)·기복 5~6.
  합성 사례(하네스 `shiftdemo=1`: 하준 최근 7일을 '피곤'만으로, 도윤 최근 6일 삭제)로 ①②가 걸리는 것을 확인했다. renders 465~468.
  PHRASES.md의 "긍정 계열이 급감했을 때"는 아직 판정이 없다.
- 기존 기능 유지: 오늘의 현황 / 개별 현황 / 살펴봐주세요

### ⑤ 「말 걸어봤어요」 (2026-09-15, ①②③ 단계)

교사가 살펴봐 주세요를 보고 아이에게 말을 걸었는데 "괜찮아요"로 닫히면 며칠 뒤 다시 간다. 25명 담임이 누구에게 언제 다시 갈지 기억할 수 없어 앱이 그것만 돕는다. 면담 기록이 아니다 — **저장되는 것은 학생 id와 시각뿐**이고 상담 내용은 적지 않는다(아래 "만들지 않기로 한 것").

- 살펴봐 주세요 카드의 학생 옆에 「말 걸어봤어요」 버튼(2026-09-16까지는 요즘 달라진 아이 카드에도 있었다). 누르면 카드에서 사라진다(표시는 학생당 하나).
- **말 건 뒤 수업일 사흘**이 지나면 살펴봐 카드 맨 위 「다시 볼 때예요」(호박색 띠)로 올라온다. 원래 조건을 다시 채우는지와 무관하다 — 나아진 아이는 4일 기준을 못 채워 목록에서 빠지는데 그 아이야말로 다시 봐야 한다. 지난 신호(살펴봐 사유·대화 문구)는 접어 둔다. 버튼 둘: 「다시 걸어봤어요」(사흘을 다시 셈) · 「지켜볼게요」(표시를 지움, 조건에 맞으면 보통 행으로).
- 수업일은 「요즘 달라진 아이」 작성 끊김의 `shiftSchoolDayFn`을 그대로 쓴다(주말 제외, 그날 쓴 아이가 정원의 10% 이하인 평일은 학교 안 간 날). 시작점만 말 건 날(`talkSchoolDays`). 시연 시계(9월 16일 고정)에서도 같은 셈이다.
- 저장: `app/tpriv/<교사아이디>/<해시>/talk/<학생id> = 시각`. 자격과 같은 "경로가 곧 비밀"(§13) — 로그인 때 계산한 해시를 **세션 상태에만** 둔다. 학생 명단(`stu_<코드>`)에 두지 않는 이유: 바다·광장이 읽는 경로라 "살펴봐 주세요에 올랐던 아이"가 학생에게 샌다.
- 시연 계정은 dbRef 관문이 메모리에만 쓰므로 누르면 그 세션에서만 사라지고 새로고침하면 돌아온다. 시연 시계가 고정이라 「다시 볼 때예요」는 세션 안에서 못 본다 — 보여 주려면 생성기에 사례(예: 9월 11일 금요일에 말 건 학생)를 넣어야 한다(미정).
- 겸사겸사: 살펴봐 주세요와 달라진 아이가 각각 25명 일기를 읽던 것을 한 로더(`diaryById`)로 합쳤다.
- **③ 비교 데이터(2026-09-15 저녁).** 「다시 볼 때예요」만으로는 두 번째 대화가 첫 번째보다 나아지지 않는다 — 말 건 뒤로 무엇이 달라졌는지 보여야 교사가 다르게 말을 건다.
  행의 안내문 아래에 세 줄(`talkCompare`, 이미 읽은 `diaryById`로만 계산하므로 추가 읽기 없음). 조치 제안은 없다 — "이런 정보가 있습니다"까지.
  ```
  그 뒤로 쓴 일기      4편 (5일 동안)
  주요 감정            실망 · 말 걸기 전 7일 중 2일 → 그 뒤 5일 중 0일 · 줄었어요
  새로 나온 긍정 감정  열정 9.11(금) · 감사 9.12(토)
  ```
  - 창: 말 건 시각을 경계로 **앞은 달력일 7일**(`TALK_BEFORE_DAYS`), 뒤는 오늘까지. 창이 달라 오해가 없게 두 길이를 둘 다 적는다("전 7일 중 n일 → 뒤 N일 중 m일"). 하루에 여러 번 써도 1일.
  - 주요 감정: 앞 7일에 가장 여러 날 나온 살펴봐 감정(`SAD_EMOS`). 없으면 앞 7일에 가장 여러 날 나온 감정. 앞 7일에 일기가 없으면 "견줄 수 없어요".
  - 늘고 줄음은 **글자로만** — `TALK_CMP_MARK = "word"`(줄었어요 · 늘었어요 · 그대로예요). 색으로 판단을 주지 않는다(위 선). 나빠졌을 때(외로움 3일 → 4일)도 그대로 보인다 — 교사가 다르게 접근할 정보다.
  - 새로 나온 긍정 감정(HA·LA 사분면): 말 건 뒤 일기에 나왔고 **그 아이의 일기 전체 기간**에 없던 것(`TALK_NEWPOS_DAYS = 0`; 최근 한 달로 좁히면 오래전에 한 번 쓴 말이 다시 잡혀 "처음"의 뜻이 약해진다). 처음 나온 **날짜와 요일**("9.15(월)", 최대 `TALK_NEWPOS_MAX` 3개, 날짜순). 이 줄이 핵심이다 — 해결중심 상담의 예외질문 "지난 월요일에 뿌듯하다고 썼던데 그날 뭐 했어?"의 근거라 날짜가 정확해야 하고 교사와 아이는 날짜보다 요일로 기억한다. 말 건 날 오후 일기는 "그 뒤"라 말 건 날짜도 나올 수 있다.
  - 없을 때: `TALK_NEWPOS_EMPTY = "text"` — "아직 없어요"로 둔다. 세 줄이 늘 같은 자리에 있어야 훑기 쉽고, 줄이 빠지면 빠진 건지 안 나온 건지 헷갈린다.
  - 그 뒤로 쓴 일기가 없으면 첫 줄만 "아직 없어요 (N일 동안)" — 0일을 "줄었어요"로 읽히지 않게 나머지 두 줄은 뺀다.
  - 세 값은 2026-09-15 사용자 확정(전체 기간 · word · 아직 없어요 · 기간 표현은 긴 쪽). 확인: 시연 학급 25명 전원 계산표, 줄었음(하늘)·늘었음(예린)·새 긍정 없음(연우)·일기 없음(도윤), 읽기 수 54 그대로, 콘솔 0 — renders 524, 하네스 shootTalk3.js.
- **④ 시연 사례(2026-09-15 저녁).** 시연 시계가 고정이라 세션 안에서는 「다시 볼 때예요」를 못 보므로 생성기가 심는다(`seed-data.js` `D.TALK = [23, 24]`, `seed-gen.js` `talkDayOf`: 기준일을 포함해 평일 셋을 거꾸로 센 뒤 그 앞 평일 정오 — 기준일이 바뀌어도 수업일 딱 3일). `rebuild`가 패널의 교사 비밀번호로 해시를 구해 `tpriv/<교사>/<해시>/talk`에 set(전에 있던 표시는 지워진다). 일기는 안 바꾸고 기존 흐름에서 골랐다 — 소율 23(실망 1일→0일, 새 긍정 감정 2~3개)·건우 24(피곤 1일→1~2일). 살펴봐(11·18)·달라진 아이(10·12·16)·A·B는 피했다(표시가 그 카드에서 아이를 빼므로). `check.js`가 14조합 전부에서 수업일 3·좋아짐/나빠짐·겹침 없음을 검사한다.

### 만들지 않기로 한 것
- **면담 기록 기능** — 담임이 상담 내용을 앱에 또 입력할 시간이 없고,
  상담 내용을 앱에 남기는 것 자체가 부담이 된다.
- **"누가 많이 받았나"를 모아 보여주는 화면** — 아이용이든 교사용이든 만들지 않는다(⑥-2). 순위가 된다.
- **공감 실시간 알림** — 기다리게 만든다. 섬을 열 때 한 번 읽는다.

---

## 10. 기술적 선행 조건 ⚠️

**저장 구조 개선이 다른 모든 작업보다 먼저다.**

현재 구조:
- 일기 한 줄 추가에 `sSet(dKey(id), [...entries, ne])` — **배열 전체를 덮어쓴다**
- 두 기기에서 동시에 쓰면 **나중 쓰기가 앞의 것을 통째로 날린다**

친구 섬 공감, 광장 답변은 **여러 명이 같은 데이터를 동시에 건드린다.**
현재 구조로는 반드시 데이터 유실이 발생한다.

→ 일기를 `app/diary_<id>/<ts>` 형태의 **개별 자식 노드로 분리**하고,
   섬·광장 데이터도 같은 방식으로 설계한다.

**이 작업을 나중에 하면 위에 올린 기능을 전부 다시 손봐야 한다.**

---

## 11. 작업 순서

진행 상황 (2026-09-07 갱신)

| 순서 | 작업 | 상태 |
|---|---|---|
| ① | 저장 구조 개선 | ✅ 완료. 일기를 `app/d2/<id>/<key>` 자식 노드로 분리 |
| ② | 교사 대시보드 — 근거 표시 + 대화 문구 제안 | ✅ 완료. 문구표는 `PHRASES.md` |
| ⑦ | 데이터 내보내기 (PDF) | ✅ 완료. 교사용은 일기 본문 미포함 |
| ③-0 ~ ③-8 | Three.js 인라인, 계단식 지형, 조명·색, 식생, 캐릭터·걷기, 랜드마크 3개, 전환 연출, 다리, 반지름 6.0 | ✅ 완료 (세부는 이전 표 참고: 2026-09-05 갱신본은 git 이력에 있다) |
| ③-4 | 섬 30개 성능 하네스 (`#fleet`) | ✅ 완료 |
| ③-9 | 흙길 — 시작점에서 잠수 지점·부두·광장 표지로 이어지는 길. 층이 다르면 계단을 지난다 | ✅ 완료 (2026-09-10, 렌더 129~130 · TEST 0-C). **경로**: 시작점(freeStart와 같은 규칙)에서 각 목적지까지 그 층 평지 안의 최단 경로. 같은 층이면 직선에 가깝고, 위층 벽에 막히면 팽팽한 줄처럼 벽에서 0.3 띄워 돈다(`taut` — 이웃 둘을 잇는 직선 위로 당기기 500회). 층이 다르면 계단 발치를 향해 곧장 가고 상단에서 다시 곧장 간다. 1층 목적지(부두·잠수 지점·계단 A) 중 시작점에서 같은 쪽으로 도는 것들은 가까운 곳까지 한 길로 가다 갈라진다. 완만한 굽이 하나(진폭 0.09~0.15)만 얹는다. **그리기**: 지형 위 리본 메시 하나(`paths`, 정점 알파로 가장자리 번짐, polygonOffset). 폭 0.30(캐릭터 지름의 0.75배) 고정 — 오솔길. 잔디가 주인공이고 길은 흔적이다. 띠가 0.5 아래로 좁으면 띠를 넘지 않는 만큼만 줄인다. 색은 모래·흙벽 사이 0.48(해변 위에서도 보인다). 도착점은 디딤판 바깥 0.16까지 진하고 0.12 번지는 마당 원판(부두 0.80·잠수 1.00·광장 0.80, 벽에 걸리는 방향은 줄임). 식생은 `fits`가 `pathHit`(길+마당)으로 피한다. 지형 높이(`profileAt`)·걷기(`walkY`)는 그대로. 먼 섬(detail<0.6)에는 길이 없다. 시드 40개 8 ms/섬. **버린 것**: 2026-09-09 첫 판은 띠 가운데를 따라 정점 색만 바꿨다 — 층을 도는 등고선으로 보였고, 격자 간격 탓에 0.6보다 좁아지지 않아 좁은 띠를 통째로 채웠다. 리본 첫 판(폭 0.68·굽이 진폭 0.3·마당이 디딤판 안에서 사라짐)은 흙 지대로 보였고 곧은 길도 가장자리를 도는 듯했다 |
| ⑥-0 | 바다·친구 섬 저장 계층 (`app/isle` `app/day` `app/emp`) | ✅ 완료 |
| ⑥-1 | 바다 화면 (친구 섬 + 학급 온도계) | ✅ 완료. 고리 세 겹 배치, 검색·명단·탭·이름표, 출항 이어받기, 항해·도착, 바다 색(중앙값·3명 미만 0.5), 성능 카드 6장 |
| ⑥-1b | 섬마다 주어진 것 (변주) | ✅ 완료. 반지름 5.7~6.6·층 치우침·층 비율·해변·색·모래톱·중립 밀도. 시드 400개 검사 통과 |
| ⑥-2 | 친구 섬 방문 + 공감 | ✅ 완료. 배로 도착·걷기, 어휘 배너(뜻·나도 그래·조건), 반딧불(7일·상한), 그날 이름, 가리기, 오늘의 섬, 교사 "받지/보내지 못한 학생"(소수일 때만 이름), 나침반 "떨림" |
| — | 크롬북 측정 | ✅ **2026-09-07 완료.** 내 섬 21fps·바다 60fps·첫 화면 빠름. 카드(`sm=1024`·`shadow=0`·`dpr=1`)를 켜도 차이 없어 **기본 설정 그대로** 간다(§7 성능 카드) |
| ⑤ | 광장 | ✅ 완료 (2026-09-07). 돌계단 마당(안으로 내려가 불에서 끝난다) + 카드, 화분 12칸 어휘, 답 쓴 수만큼 캐릭터, 오늘의 질문(NVC 세 칸·느낌 칩·이름 검사), 교사 승인 답만 익명 공개(3개 이상), 교사 패널(올리기·마감·비공개/광장/숨김), 배너. **광장은 섬을 키우지 않는다.** 규칙에 `plaza` 경로 필요 |
| — | 사운드 (파도·발소리·전환음) | ✅ 코드 완료 (2026-09-07). `SND` 관리자, 기본 음소거, 3D 화면 오른쪽 위 "소리" 버튼(기기에 기억). 파도 루프(내 섬·바다·광장), 발소리 3종 번갈아, 잠수 물보라·출항·탭. 파일은 `sounds/README.md`의 이름대로 `.ogg`+`.m4a`, 없으면 그 소리만 건너뛴다. **음원 파일은 아직 없다** — CC0만, 출처는 `sounds/CREDITS.md` |
| — | 허브 승격 (내 섬을 첫 화면으로) | ✅ 완료 (2026-09-07). `ISLE_HUB` 한 줄, 되돌리기도 그 줄. 내 섬 오른쪽 위는 로그아웃, 일기 화면 버튼은 "내 섬으로". WebGL 없음·렌더 실패·느림(10fps 미만 6초, 괜찮아요는 그날 하루) 세 겹으로 일기 화면으로 보낸다(§3) |
| ⑧ | 통합 테스트 + 예비 | ⬜ 목록은 **`TEST.md`**(교사 1·학생 2+문턱용 1, 9단계). Firebase 규칙 `isle`·`emp`·`day`·`plaza` 반영됨 |
| ⑨ | 계정 정보 보호 (비밀번호 해시·경로 비밀·규칙) | 🔶 1~3단계 완료 (2026-09-09). 4단계 규칙 조이기(`RULES_STEP4.json`)는 2026-09-10. 범위와 한계는 **§13** |
| ⑩ | 심사용 시연 학급 (가상 데이터 생성기) | 🔶 시작 (2026-09-09). 결정·방법·한계는 **§14** |

**보류 중인 검토**: `WORDS.md` 뜻 사전(선생님 검토 → `EMO_MEANINGS` 반영), 콘솔 노출 수용(§7에 명시).

**걷기를 지형 직후에 시험하는 이유**: ③-6이 이 단계의 관문이다.
걷기가 어색하면 그 위에 올린 것이 전부 무너진다.
식생을 다 얹고 나서 "걷기가 안 되네"가 되면 되돌리기가 비싸다.
지형이 서면 **아주 단순한 캐릭터로 걷기만 먼저** 확인한다.

**②를 앞에 둔 이유**: 저장 구조·3D와 무관해서 언제든 할 수 있고, 하루면 끝난다.
심사에서 직접 지적받은 부분이라 먼저 확보해두는 편이 안전했다.

---

## 12. 작업 규칙

- **한 번에 하나씩.** 여러 기능을 한꺼번에 요청하지 않는다.
- **단계마다 커밋한다.**
  ```
  git add .
  git commit -m "② 3D 내 섬 기본 형태"
  ```
- **각 단계마다 브라우저에서 실제로 확인**한 뒤 다음으로 넘어간다.
- ③ 단계에서 **반드시 학교 크롬북으로 성능 확인**. 여기서 무거우면 뒤에 뭘 올려도 안 된다.
- 기존에 잘 돌아가는 기능을 깨뜨리지 않는다. 수정 범위를 명시적으로 좁혀서 요청한다.

---

## 13. 계정 정보 보호 — 인증 없이 막는 것과 못 막는 것 (2026-09-09)

컨설팅 지적. 페이지를 열면 로그인 전에 `app/t-schools-v3`를 통째로 읽었고, 거기에 **모든 학교의
교사 아이디·비밀번호가 평문**으로 있었다. `app/stu_<코드>`의 학생 비밀번호도 평문이었다.
콘솔만 열면 누구나 볼 수 있었다.

**한 문장으로 가르는 기준.**
**계정 정보(비밀번호·교사 계정·학교 목록)는 이번에 막는다. 기록(일기·대화·섬·공감·광장 원본)의
접근 통제는 인증 도입 단계다.** 컨설팅·심사에서 물으면 이 둘을 섞지 않고 답한다.
§7·§8에서 "콘솔로 볼 수 있다"고 수용한 것은 전부 뒤쪽(기록)이고, 이 절은 앞쪽(계정)만 다룬다.

### 방법 셋

| | 무엇 | 왜 |
|---|---|---|
| ① 해시 | 비밀번호는 PBKDF2-SHA256(입력, 소금 = 역할:학교코드:아이디, 10만 회)의 **64자 hex만** 저장. 비교는 클라이언트가 같은 계산을 해서 한다 | 서버 코드 없이 된다. HTTPS라 `crypto.subtle`을 쓸 수 있고 크롬북에서 한 번에 0.1~0.3초 |
| ② 경로가 곧 비밀 | 자격은 `…/<아이디>/<해시>` 노드 하나. 규칙에서 **부모는 읽기 금지, 마지막 해시 노드만 읽기 허용**. 로그인 = 그 경로 하나를 읽어 null이 아니면 성공 | 규칙은 "누가"는 몰라도 "경로를 정확히 아는가"는 안다. 해시 목록을 내려받을 수 없으니 오프라인 대입이 안 되고, 교사 아이디·학교 코드도 열거되지 않는다. **로그인 전 읽기가 0**이 된다 |
| ③ 규칙으로 고정 | 교사 자격·학교 공개 정보는 `!data.exists()`로 **한 번 쓰면 못 바꾼다**. 옛 배열에 `password` 키가 오면 거부 | 교사 계정 탈취와 학교명 변조를 막는다. 평문이 다시 저장되는 길을 막는다 |

### 경로

```
app/sch/<학교코드>/pub                 { schoolName }                          코드를 알면 읽음. 생성 후 고정
app/tcred/<교사아이디>/<해시>          { schoolCode, schoolName, teacherName }  부모 읽기 금지. 생성 후 고정
app/scred/<학교코드>/<아이디>/<해시>   true                                    부모 읽기 금지. 쓰기는 열림
app/stu_<학교코드>                     기존 배열에서 password 필드만 없앤다     바다·광장·교사 화면이 읽는다
app/t-schools-v3                       이전이 끝나면 읽기·쓰기 모두 닫는다
app/tpriv/<교사아이디>/<해시>/talk/<학생id>  말 걸어봤어요 시각(§9-⑤)              부모 읽기 금지. 해시는 세션에만. 규칙: talk/$sid는 숫자
```

- 교사 아이디 중복 검사는 따로 읽지 않는다. `tcred/<아이디>`에 쓰기를 시도해 규칙에 막히면 "이미 사용 중".
- 학생 로그인: `sch/<코드>/pub` → 해시 계산 → `scred/<코드>/<아이디>/<해시>` → 성공하면 `stu_<코드>`에서 내 항목(id·이름·학년반).
- 교사 로그인: 해시 계산 → `tcred/<아이디>/<해시>`가 곧 학교 정보. 그 다음 `stu_<코드>`.
- 학생 비밀번호 변경·교사의 초기화(1234로): `scred/<코드>/<아이디>`를 `{ <새 해시>: true }`로 통째로 놓는다(옛 해시가 같이 지워진다).
- 아이디는 경로의 일부가 되므로 `. # $ [ ] /`를 쓸 수 없다(`keyOk`). 옛 계정에 이런 아이디가 있으면 이전되지 않고 폴백으로만 로그인된다.
- 마이그레이션 패널은 **로그인한 교사의 학교만** 훑는다. 교사 누구나 전국 학교 목록을 보던 구조 자체가 문제였다.

### 규칙 조각 (기존 규칙의 `app` 아래에 넣는다)

1단계(새 경로 추가, 옛 경로는 그대로):

```json
"sch": {
  "$code": {
    "pub": {
      ".read": true,
      ".write": "!data.exists()",
      ".validate": "newData.hasChildren(['schoolName']) && newData.child('schoolName').isString() && newData.child('schoolName').val().length <= 60"
    }
  }
},
"tcred": {
  "$tid": {
    ".write": "!data.exists()",
    "$h": {
      ".read": true,
      ".validate": "$h.length == 64 && newData.hasChildren(['schoolCode','schoolName','teacherName']) && newData.child('schoolCode').isString() && newData.child('schoolName').isString() && newData.child('teacherName').isString()"
    }
  }
},
"scred": {
  "$code": {
    "$lid": {
      ".write": true,
      "$h": {
        ".read": true,
        ".validate": "$h.length == 64 && newData.isBoolean()"
      }
    }
  }
}
```

4단계(이전 확인 뒤, `RULES_STEP4.json`): `t-schools-v3`를 `".read": false, ".write": false`로, `stu_*` 항목에
`"password": { ".validate": false }`를 넣는다. 조각의 `$stu`는 기존 규칙에서 `stu_*`를 받는 와일드카드
이름으로 바꿔 **그 안에 합친다**(한 층에 `$` 와일드카드는 하나만 허용된다). 명단은 배열이라 경로가
`stu_<코드>/<번호>/password`이므로 두 단계 아래에 둔다. 넣기 전에 모든 학교에서 3단계 "보호 적용"을
눌렀는지 확인한다 — 평문이 남은 항목이 있으면 그 뒤 명단 저장(학생 추가·삭제)이 통째로 거부된다.

**전제.** `app` 노드나 `$` 와일드카드에 `.read: true`가 걸려 있으면 ②가 무너진다. Firebase 규칙은
위에서 준 읽기 권한을 아래에서 뺏지 못한다. 기존 규칙이 경로를 하나씩 열거하는 방식이어야
하고(지금까지 "규칙에 없는 경로는 PERMISSION_DENIED"였으므로 그럴 가능성이 높다), 넣기 전에 확인한다.

### 이전 순서 (학생들이 로그인 중이라 순서가 중요하다)

| 단계 | 하는 일 | 상태 |
|---|---|---|
| 1 | 규칙에 새 경로 추가. 옛 경로 유지 (`RULES_STEP1.json`) | ✅ 2026-09-09 적용. REST로 확인: 부모 401, 해시 잎 200, 잘못된 모양 401, `app` 루트 401 |
| 2 | 코드 배포. 로그인은 새 경로 먼저, null이면 옛 배열에서 평문 비교. 옛 방식으로 성공하면 그 자리에서 해시를 쓰고 평문을 지운다. 옛 배열은 **로그인 버튼을 누른 뒤에만** 읽는다 | ✅ 2026-09-09 커밋 `7db7073`. 같은 날 브라우저 확인(`TEST.md` 0-A): 로그인 전 `t-schools-v3` 읽기 없음, 기존 교사 로그인 → `tcred` 생김, 기존 학생 로그인 → `scred` 생김 |
| 3 | 교사 대시보드 "비밀번호 보호 적용" 버튼을 학교마다 한 번. 반 전원의 해시를 쓰고 평문을 지운다. **교사 계정은 그 교사가 한 번 로그인해야 옮겨진다** — 4단계 전에 실제로 쓰는 교사 전원이 새 코드로 로그인했는지 확인한다(안 하면 4단계 뒤 콘솔 처리) | ⬜ |
| 4 | 다음 날 규칙 조이기(`t-schools-v3` 닫기, `password` 거부). 하루 두는 이유는 GitHub Pages 캐시로 옛 페이지를 붙든 기기 | ⬜ |
| 5 | 옛 경로 폴백 코드 제거 (대회 뒤) | ⬜ |

### 막는 것 / 못 막는 것

| 조치 | 막는 것 | 못 막는 것 |
|---|---|---|
| ① 해시 | 콘솔·네트워크·덤프에서 비밀번호 읽기. 교사 비밀번호처럼 긴 것은 실질적으로 안전 | 짧은 학생 비밀번호 추측 |
| ② 경로 비밀 | 해시 목록 내려받기, 학교·교사 목록 열거, 로그인 전 노출 | 아이디를 아는 사람이 온라인으로 한 번에 하나씩 찍어보는 것. Firebase는 속도 제한이 없다 |
| ③ 교사 고정 | 교사 계정 덮어쓰기(탈취), 학교명 변조 | 교사가 비밀번호를 잊으면 앱에서 못 바꾼다. **관리자가 Firebase 콘솔에서 노드를 지우고 다시 만든다** (2026-09-09, 수용) |
| ③ password 거부 | 평문 재저장 | 남의 학생 자격 노드를 덮어쓰거나 해시를 하나 더 얹는 것. 지금도 열려 있고 인증 없이는 못 막는다 |

### 알고 있는 한계 (숨기지 않는다)

- **일괄 등록 기본 비밀번호 "1234"는 그대로 둔다** (2026-09-09 결정). 해시해도 네 자리는 만 번 안에
  뚫린다. 아이들이 긴 비밀번호를 쓰기 어렵다는 현실을 택했다. 대신 교사의 학생 목록 화면에
  "첫 로그인 뒤 비밀번호를 바꾸게 해주세요" 한 줄을 둔다.
- 반 명단(`stu_<코드>`)은 바다·광장·교사 화면이 필요로 하므로 계속 누구나 읽는다. 명단에 학생 id가
  있으니 급우가 콘솔에서 `d2/<id>`·`chat_<id>`·`isle/<id>`·`emp/<id>`·`plaza/…/a/<qid>/<id>`를 읽는
  것은 여전히 가능하다. §7·§8의 수용 결정과 같다.
- 익명 키(`anonKey`)는 id에서 바로 계산되므로 id를 아는 사람은 되돌릴 수 있다. 같은 한계.

### 인증 도입 단계에서 할 일 (이번 범위 밖)

Firebase Auth(익명 로그인이면 아이가 이메일 없이 된다) + 규칙에 `auth.uid`와 학생 id의 매핑을
두어 `d2`·`chat`은 본인과 담임만, `isle`·`emp`는 같은 반만 읽게 한다. 그때 `stu_<코드>`에서
id를 빼고, `anonKey`에 서버 소금을 넣는다. 그 전까지는 위 한계를 안 채로 운영한다.

---

## 14. 심사용 시연 학급 — 가상 데이터를 어떻게 만들고 어떻게 밝히는가 (2026-09-09)

컨설팅에서 "킬러 특징이 없다"는 지적을 받았다. 기능이 없는 것이 아니라 **빈 화면을 보여준 것**이
문제였다. 심사위원이 열었을 때 30명이 3주를 써 온 학급이 보여야 한다. 실제 아이의 글과 기록은
한 줄도 쓰지 않는다. 그래서 **가상 학급 하나를 생성기로 만들고, 만든 방법을 저장소에 그대로 둔다.**

### 왜 이 방식인가

- **섬은 본문 분석으로 자란다.** 저장 때 `analyzeEntry`가 본문에서 어휘를 찾아 `analysis.hits`에
  넣고, 섬·바다·친구 섬은 그 결과와 요약 노드(`isle`)만 읽는다(§5). 시연 본문에 어휘 키워드가
  실제로 들어 있어야 하고, 요약 노드도 같이 써야 한다.
- **"오늘"과 "지난 7일"에 묶인 것이 많다.** 바다 색과 광장 화분은 오늘 노드만(§6·§8), 살펴봐주세요와
  반딧불은 최근 7일만 본다(§7·§9). 시연 데이터는 한 번 만들고 끝이 아니라 **심사일 아침마다
  기준일을 넣어 다시 만든다.**
- **학교 코드가 곧 격리 단위다.** 명단·일기·섬·반딧불·광장이 전부 학교 코드나 학생 id 아래라, 시연
  학교를 따로 두면 실제 학급과 섞일 경로가 없다.
- **새 규칙은 필요 없다.** 시연이 쓰는 경로는 전부 기존 경로다(`stu_`·`scred`·`sch`·`tcred`·`d2`·
  `isle`·`day`·`emp`·`plaza`). 4단계 뒤에도 명단에 `password`를 쓰지 않으니 막히지 않는다.

### 결정 (2026-09-09)

| | 결정 | 이유 |
|---|---|---|
| 격리 | 학교 코드 `DEMO01`, 교사 아이디 `demoteacher`(비밀번호는 **저장소에 두지 않는다** — 공개 저장소라 히스토리에 남으면 영구히 조회된다. 패널 입력란에서 받아 처음 한 번 자격을 만든다), 학생 id는 `700000000001`~`700000000025` 고정(2026-09-14, 30→25명 — 5학년 한 반 기준), 로그인 아이디 `d01`~`d25`, 비밀번호 `1234` | 시각값 id와 한눈에 구분된다. 생성기는 코드가 `DEMO01`일 때만 돌고, 지우는 것도 그 아래만 지운다 |
| 학교 이름 | **"마음 바다 체험 학급"**(2026-09-15 저녁, 전 "시연 학급 (가상 데이터)") | 교사 헤더·로그인 화면·학생 일기 제목 아래에 항상 보인다. 가상임은 학생 띠("○○은 가상 학생이에요")가 말한다 |
| 띠 | 코드가 `DEMO01`이면 학생·교사 화면 위에 한 줄 **"시연용 가상 데이터 · 실제 학생이 아닙니다"** | 이름이 가명이라도 실명처럼 보인다. 띠가 그것을 가린다 |
| 학생 이름 | 가명 25개 — **바닷가 낱말**(물결·파도·여울…구름, 2026-09-16 사용자 A안. 전에는 하늘·바다·지우… 같은 흔한 이름이었다). 누가 봐도 가명이면서 서로 이어져야 하고, 광장 칸 이름 "바람"과 앱 이름 "바다"는 피한다. 7번 "탐험이"(체험 간판 학생)만 예외. 「요즘 달라진 아이」 사례 셋(산호 10·해마 12·등대 16)은 번호를 새로 받지 않고 기존 자리를 대신한다(2026-09-14). 역할은 전부 번호로 매여 있어 이름을 바꿔도 판정은 같다 | "학생1"이면 화면이 죽는다. 띠와 학교 이름이 가상임을 말한다 |
| 재생성 | **첫 화면**의 시연 패널(주소 `#seed`일 때만) 버튼 하나. **2026-09-14부터 시연 계정의 시계는 `DEMO_CLOCK`(2026-09-16 수 16:00, 시각은 세션 안에서 흐른다)에 고정**되어 아침마다 다시 만들지 않는다 — `nowTs()`가 시연 계정일 때만 그 값을 돌려주고, 달력 계산과 저장 타임스탬프가 전부 그 관문을 거친다. 실제 학급은 진짜 시각이다. 패널의 기준일 기본값도 `DEMO_CLOCK`이다. **3단계(같은 날)**: 시연 계정·체험 모드는 `dbRef()` 관문의 메모리 오버레이로 쓰기가 DB에 닿지 않고(읽기는 원본 위에 세션 메모리를 덮는다), **4단계**: 띠에 "저장되지 않아요"를 덧붙이고 ✅ 안내 뒤에 꼬리를 단다(문구 `MEM_NOTES`), **5단계**: 재생성·오늘치 다시 쓰기는 교사 비밀번호(`tcred/<아이디>/<해시>` 잎 확인)가 맞아야 돈다. 교사 대시보드에 두지 않은 이유 — 시연 교사 계정 자체를 이 패널이 만들므로, 로그인 뒤에 두면 처음 한 번을 만들 곳이 없다. 인증이 없어 DB 쓰기는 어차피 누구나 가능하니 패널을 로그인 뒤에 둬도 보호가 되지 않는다. **기준일**을 넣으면 그 날을 D로 삼아 D−20~D를 다시 만든다. 심사가 며칠에 걸치면 매일 아침 한 번 | 오늘·7일 창이 기준일을 따라온다. 심사위원이 전날 쓴 것은 지워진다(의도) |
| 오늘의 바다 | 기본은 **약간 밝은 날**(체육대회 연습). 패널에 **"오늘 일기만 다시 쓰기: 밝은 날 / 무거운 날"** 버튼. 오늘치 일기와 오늘 노드만 다시 쓰고 나머지는 그대로 | "무거운 날도 아름답다"(§6)를 심사 중에 보일 수 있다. 오늘 노드의 값만 바꾸는 편법이 아니라 **일기를 바꿔 색이 따라오게** 한다. 심사 계정 A·B의 오늘 일기는 건드리지 않는다 |
| 심사 계정 | **학생 A(`d07`)**: 어휘 15개쯤, 이번 주 반딧불 5~6개 중 오늘 2개, **광장 답은 아직 안 씀**. **학생 B(`d21`)**: 어휘 25개, 광장 답을 이미 씀. **교사** | A는 첫 화면(내 섬)부터 배너·잠수·바다·친구 섬·광장까지 흐름을 직접 겪는다. 광장은 직접 써야 이름 검사·칩·세 칸이 보이므로 A는 비워 둔다. 완성된 광장과 가장 자란 섬은 B로 본다 |
| 진입 순서 | 학생 A → 교사. `DEMO.md` 한 장에 계정과 순서 | 교사 화면은 학생이 쓴 것의 결과라 뒤가 맞다 |

### 생성기 (`demo/seed.js`)

**앱 안에서 돈다.** 주소 끝 `#seed`일 때만 `index.html`이 첫 화면 아래에 패널을 그리고
`demo/seed-data.js`·`seed-gen.js`·`seed.js`를 그때 불러온다. 밖에서 REST로 쓰지 않는 이유는 분석기·요약 노드·익명 키·일기 키·
비밀번호 해시가 전부 앱 안에 있어서, 다시 구현하면 어긋날 자리가 많기 때문이다. 별도 파일로
두는 이유는 **생성 방법이 저장소에서 그대로 읽히게** 하기 위해서다.

세 겹으로 만든다. 무작위는 부자연스럽다.

1. **성격표 30줄** — 기본 정서(밝음·보통·가라앉음), 쓰는 빈도(매일·격일·주 2회), 목표 어휘 수,
   자주 쓰는 말 서너 개. 어휘 수는 5·8·12·15·18·22·25 계단으로 배분한다(5~7에 3명, 8~12에 8명,
   13~17에 10명, 18~22에 6명, 23~25에 3명). 빈도가 높은 아이가 대체로 어휘도 많되 예외 둘.
2. **반 사건 달력** — D부터 거꾸로 세는 등교일 번호로 놓는다. 개학 첫 주 긴장, 수학 시험 날
   떨림과 다음 날 뿌듯·실망, 비 온 날 지루함, 체육대회 연습 주 신남. 월·금 효과는 실제 요일로.
   주말은 비운다(한둘만 집에서 쓴다). 같은 날 여러 아이가 같은 방향으로 움직여야 바다와
   대시보드가 "반"으로 읽힌다.
3. **말 쓰는 버릇** — 자주 쓰는 말이 대부분을 차지하고 새 말은 가끔. 첫 주에 새 말이 많고 뒤로
   갈수록 줄어 학습 곡선처럼 보인다. 한 일기에 어휘 1~3개, 가끔 0개.

**본문**은 사람이 쓴 문장 조각을 조합한다. "상황 한 문장 + 마음 한 문장 + 마무리 반 문장" 세 칸,
칸마다 60~80개를 어른이 초등학생 말투로 미리 쓴다. 상황 문장에는 `SITUATION_MAP`의 상황
키워드를, 마음 문장에는 `EMOTIONS_28`의 키워드를 넣는다. 조합한 문장은 **앱의 분석기에 그대로
통과시켜 의도한 어휘가 잡히는지 생성 단계에서 검사**하고, 어긋나면 버린다. 부정어·타인 주어
때문에 빠지는 문장은 라이브러리에서 뺀다.

**살펴봐주세요**는 셋으로 만든다. 뚜렷한 아이 하나(외로움·슬픔과 "혼자", 친구 상황, 7일 중
5일 — 하루는 밝은 날을 섞는다), 경계선 하나(피곤·우울과 잠 이야기, 공부 상황, 딱 4일), 그리고
**슬픈 날이 하루뿐이라 뜨지 않는 아이 여럿**. 뜨지 않는 아이가 있어야 선별이 과민하지 않다는
것이 보인다.

**반딧불**은 최근 7일치를 사회 관계처럼 만든다. 60%쯤이 하나 이상 보냈고, 받는 쪽은 고르지 않게
(몇은 많이, 몇은 0 — 교사 카드에 이름이 뜬다). 보낸 아이가 그 영역의 말을 가진 경우만 만든다
(§7 조건). A는 이번 주 5~6개, 그중 오늘 2개.

**광장**은 열린 질문 하나("타인 이해", 이름 안내 켬, D−2 게시)에 답 18개 — 승인 12·비공개 5·
이름이 들어 있어 확인 필요 1. 지난 질문 둘은 마감. A는 답이 없고 B는 있다.

**쓰는 순서**: `DEMO01` 아래 비우기(`d2`·`isle`·`emp`·`day`·`plaza`·`scred`·명단) → 명단·자격
→ 일기 → 섬 요약 → 오늘 노드 → 반딧불 → 광장. 교사 자격(`tcred`)과 학교 공개 정보(`sch`)는
한 번 쓰면 못 바꾸는 규칙이라 **처음 한 번만 만들고 그 뒤로는 건드리지 않는다.**

**자연스럽게**: 빠진 날, 한 줄짜리, 감정이 안 잡히는 일기, 등교 시간대에 흩뿌린 저장 시각.
근거 화면도 너무 깔끔하지 않게.

### 체험 간판 = 시연 학생으로 들어가기 (2026-09-14, C안)

첫 화면의 "로그인 없이 체험하기" 간판은 2026-04 원본부터 있던 **일기 전용 체험 모드**(`page === "trial"`)로
갔다. 3D 작업 뒤에도 그대로 남아, 심사위원이 이 간판부터 누르면 섬·바다·광장을 영영 못 봤다. 그래서 간판을
**시연 학생 A 탐험이(`DEMO01`/`d07`, `DEMO_TRIAL`; 2026-09-16 전 이름 "하준")으로 자동 로그인**하는 길로 바꿨다(`demoLogin` → `studentLoginWith`).
로그인 창의 학생 탭에도 같은 길 "계정이 없나요? 시연 학급으로 들어가기 →"를 두었다가 2026-09-17 「심사용」 체크박스로 대신하고 뺐다(아래).

- 시연 계정이라 쓰기는 세션 메모리에만 남고(`MEM_ONLY`), 띠가 뜬다. 여러 심사위원이 같은 계정을 써도 세션이 섞이지 않는다.
- **못 들어가면**(시연 학급이 없거나, DB가 안 닿거나, http라 해시를 못 만들면) 간판은 예전 체험 모드(일기만)로 떨어지고,
  로그인 창 링크는 그 자리 안내문에 이유를 띄운다. 예전 체험 모드는 이 대비로만 남는다.
- 로그인에 성공하면 로그인 창을 닫는다(전에는 열린 채 남아 로그아웃 뒤 첫 화면에 창이 떠 있었다).
- 알고 있는 한계: 간판 그림의 글자("로그인 없이 체험하기 / 계정 없이 써볼 수 있어요")는 그대로다 — 그림이라 코드로 못 바꾼다.
  시연 학급이 실제 Firebase에 있어야 한다(2026-09-14 확인: `app/sch/DEMO01/pub`·명단 있음). 하네스 확인은 `renders/harness/shootTrial.js`, renders 441.

### 로그인 창 「심사용」 체크박스 (2026-09-17)

교사 대시보드는 서류의 계정을 쳐야 들어갈 수 있어 서류를 안 읽은 심사위원은 못 본다. 로그인 창 탭 아래에
**「심사용」 체크박스**를 두었다. 처음 열면 켜져 있고 시연 계정이 채워져 있으며 칸은 못 고친다(readOnly, 어두운 칸).
끄면 칸이 비고 직접 친다. 탭을 옮기면 그 탭의 시연 계정으로 바뀐다(학생: `DEMO01`/`d07`/`1234`, 선생님: `demoteacher`/`DEMO_TEACHER.pw`).

- **끈 것은 기기에 남는다**(localStorage `judge=0`). 실제 학급 학생·교사가 매번 끄지 않게. 다시 켜면 지운다.
- 체크된 채 학생으로 들어가면 체험 간판과 같은 체험 사용자(`trialUser`, "체험 끝내기").
- **선생님 탭은 `DEMO_TEACHER.pw`가 있을 때만 체크박스가 보인다.** 이 페이지는 누구나 소스를 볼 수 있으므로(저장소도 공개) 거기 넣는 비밀번호는 공개된 것과 같다.
  시연 교사 자격은 한 번 쓰면 못 바꾸므로(§13 ③), 지금 비밀번호가 다른 곳에도 쓰는 것이면 Firebase 콘솔에서 `tcred/demoteacher`를 지우고
  시연 전용 비밀번호로 다시 만든 뒤(#seed 패널 "시연 학교 만들기" → "전체 다시 만들기") 그 값을 넣는다. 넣기 전까지 선생님 탭은 전과 같다.
- 열리는 것은 가상 학급 `DEMO01`뿐이고 쓰기는 세션 메모리에만 남는다(`MEM_ONLY`). 비밀번호를 아는 사람이 할 수 있는 것은 #seed 패널의 재생성(결정적 생성기라 같은 데이터)과
  `tpriv/demoteacher/<해시>` 밑 직접 쓰기(REST)뿐이다. 학생 쪽 경로는 DEMO.md의 학생 비밀번호로 이미 같은 정도로 열려 있다.
- 교사 로그인도 성공하면 창을 닫는다(전에는 학생만 닫아 로그아웃 뒤 첫 화면에 창이 남았다).
- 하네스 `renders/harness/shootJudge.js`(`JUDGE=1 node gen.js`가 교사 비밀번호 "harness"를 넣는다), renders 574~581.

### 조작이 아니라 시연이라는 것을 어떻게 밝히나

- 화면: 학교 이름과 띠(위 표).
- 저장소: `demo/seed.js`에 성격표·달력·문장 라이브러리가 그대로 있고, 이 절에 방법과 날짜가 있다.
- 제출물: "시연 계정의 데이터는 생성 스크립트로 만든 가상 데이터이며 실제 학급 데이터는 포함되지
  않았습니다" 한 줄. `DEMO.md`에도 같은 줄.

### 알고 있는 한계

- 날짜 키는 **기기 시간**이다(§6, `TEST.md`). 심사위원 기기가 다른 시간대면 "오늘"이 어긋날 수
  있다. 국내 심사를 전제한다.
- 재생성은 심사위원이 그날 쓴 것을 지운다. 의도이지만, 심사 중에는 누르지 않는다.
- 교사 자격은 재생성하지 않으므로 시연 교사의 비밀번호를 바꾸면 §13과 같이 콘솔에서 처리한다.
- 익명 키가 id에서 나오는 한계(§13)는 시연 학급에도 같다. 가상 데이터라 문제는 없다.

### 단계

| 단계 | 하는 일 | 상태 |
|---|---|---|
| 1 | 이 절 | ✅ 2026-09-09 |
| 2 | 문장 라이브러리·성격표·달력. 분석기 통과 검사 | ✅ 2026-09-09. `demo/check.js`가 `index.html`에서 분석기를 잘라내 상황×마음 전 조합, 어휘 수, 살펴봐주세요(정확히 둘), 바다 색, 반딧불, 광장을 기준일 7개×기분 2개로 검사. 분석기의 버릇 둘을 피해서 썼다 — 한 글자 토큰("두"·"쓸"·"안"·"이")은 어떤 어휘든 접두어로 잡히고, 감정 앞 4토큰 안에 사람 단어로 시작하는 토큰("그림"·"얘기"·"할머니"·"친구들하고")이 있으면 목적격 조사가 없는 한 남의 감정으로 빠진다 |
| 3 | `demo/seed.js` (비우기→명단·자격→일기→섬 요약→오늘→반딧불→광장) + 오늘 다시 쓰기 | ✅ 2026-09-09. 경로마다 따로 쓰고, 규칙에 막히면 한 층 아래 자식으로 다시 쓴다(`putDeep`·`wipe`). 막힌 경로는 로그에 남는다 |
| 4 | `index.html`: 띠 한 줄, `#seed` 로더, 시연 패널 | ✅ 2026-09-09. 띠는 오른쪽 위 버튼 아래(아래 가운데는 식생 배너와 겹쳤다) |
| 5 | 브라우저 확인: A → B → 교사. 크롬북에서 바다 30개 | 🔶 헤드리스 크롬 + 메모리 DB 스텁으로 2026-09-09 통과(패널 두 버튼, A 섬·바다·광장, 예린 섬, B 광장, 교사 대시보드, 무거운 날 바다 — 콘솔 오류 0, 바다 픽셀 87·182·184 → 75·145·146). **실제 Firebase 확인은 `TEST.md` 0-B** |
| 6 | `DEMO.md` | ✅ 2026-09-09 |
